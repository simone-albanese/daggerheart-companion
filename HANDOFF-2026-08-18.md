# Handoff — 18 agosto 2026

**Leggi questo per primo.** È lo stato a freddo del progetto dopo la sessione del 18 agosto, in cui
tutte le voci aperte sono state riverificate contro il codice e il proprietario le ha decise una per
una. Sostituisce `HANDOFF.md` e `docs/handoff/reflow-handoff.md` come punto di ripartenza e li
contraddice in più punti **di proposito** — dove succede, è segnato.

Questo documento è passato per **due giri di lettura a freddo** da parte di revisori senza il
contesto della conversazione. Il primo ha trovato undici affermazioni false, due delle quali erano
già state propagate nel repo; il secondo ne ha trovate altre otto, quasi tutte introdotte dalle
correzioni del primo. Sono tutte sistemate qui e segnalate come **[corretto]** dove il lettore
potrebbe aver già visto la versione sbagliata altrove — compresa la regola su jsdom di §12, che
adesso è misurata e smentisce anche il backlog.

> **Aggiornato più tardi lo stesso giorno: i passi 1 e 2 di §10 sono eseguiti.** L'albero di lavoro
> è pulito, i quattro elementi di §3 sono su `main` in due commit, i due fix di §5.4 sono stati
> portati con `cherry-pick`, e la pulizia dei rami è fatta. La suite è a **2504 su 108**. Le
> sezioni §1, §2, §3, §5, §7 e §10 sono corrette di conseguenza e portano il segno **[fatto]**.
> Il passo 3 — la sera col telefono — è **rimandato per decisione del proprietario**, quindi il
> cancello H-9 resta chiuso e il lavoro prosegue solo fuori da esso.
>
> **Poi sono stati fatti anche i passi 4 e 5**, e per strada è saltata fuori una cosa che nessun
> documento diceva: **la riscrittura di storia aveva lasciato 171 citazioni di commit che puntavano
> a commit non raggiungibili da `main`**, tenute in vita dal solo ramo `backup-pre-rewrite`. Sono
> state rimappate tutte. Vedi §5.1 e §13.
>
> **Poi il proprietario ha chiesto di chiudere tutto**, e una passata a **quattordici lane
> parallele** ha chiuso ogni voce fuori dal cancello H-9: le richieste ① ③(fetta) ⑥ di §9, le
> voci 1, 2, 3, 10, 11, 15, 22, 26, 27, 29, 31, 32, 33, 34, 35, e il randomizer per tier. Suite
> da 2515 a **2688 su 119 file**. Ogni lane è passata da un verificatore avversariale che ha
> *eseguito* le mutazioni: **sei su quattordici sono tornate non sane**, una con un difetto
> vero. Tutto corretto prima del push. Ciò che resta comincia al passo 7 di §10, e **§15 ha
> l'unica domanda ancora aperta**.

---

## 0. Cos'è questo progetto, in cinque righe

**Daggerheart Companion** — scheda personaggio digitale e cassetta degli attrezzi per il GM del
gioco di ruolo *Daggerheart*. Local-first: nessun account, nessun server, nessuna telemetria,
funziona in aereo. React + TypeScript + Vite, dati in IndexedDB, PWA installabile. I contenuti
regolistici vengono dall'**SRD 1.0**, ridistribuibile sotto DPCGL; il Core Book **no** — quello
l'utente lo importa dal proprio dispositivo.

**Due persone compaiono in questo documento.** Il **proprietario** è chi scrive il codice e prende
le decisioni. **Giorgio** è il suo GM di playtest: manda vocali e messaggi dopo le sessioni al
tavolo, e le sue osservazioni diventano voci di backlog. Giorgio non decide; segnala.

**Far partire l'app:** `npm install` poi `npm run dev`. Deploy pubblico:
<https://simone-albanese.github.io/daggerheart-companion/>

---

## 1. Le sei cose da sapere prima di toccare qualsiasi cosa

1. ~~**Niente di questa sessione è committato, e questo documento compreso.**~~ **[fatto]** I
   quattro elementi sono su `main` in due commit — `9371872` il docblock, `115bda2` i documenti.
   `git ls-files docs/` adesso risponde e un `git clean -fd` non porta più via niente. Vedi §3.
2. **Non fidarti di un `- [ ]` non barrato in `BACKLOG.md` P1–P4.** Tredici caselle sono già
   spedite e non barrate; l'elenco esatto, e la trappola delle P4, sono in §6. **Il resto di
   `BACKLOG.md` resta la fonte migliore** — è l'unico posto dove i difetti 16/17/18 sono descritti.
3. ~~**Non cancellare `a2-play-cockpit` senza aver prima portato due fix su `main`.**~~ **[fatto]**
   I due fix sono su `main` (`73e184d`, `84bf907`), il ramo è cancellato, il tag
   `safety/a2-play-cockpit` resta. **Quei due difetti non sono più vivi.** §5.4 dice com'è andata.
4. **I test si lanciano dalla radice, con la toolchain del progetto.** Il Node di sistema è la 26 e
   nasconde `localStorage` a jsdom: una suite verde con quello è più debole di CI, ed è già costato
   un deploy rosso. La forma portabile, che **avvisa** se il major è sbagliato:
   ```
   cd "$(git rev-parse --show-toplevel)" && . ./env.sh && npx vitest run
   ```
   Due avvertenze che non si vedono: `.tools/` è **gitignorato**, quindi un clone non ce l'ha e va
   creato come spiega `env.sh` (scompatta il Node del major in `.nvmrc` sotto `.tools/node/`);
   e da una sottocartella `vitest` esce con `No test files found` — il percorso assoluto sistemava
   il Node, non quello.
5. **Non scomporre in sillabe i nomi dei PDF né del Core Book** per costruire il generatore. §9⑥.
6. **`Manuali/` sono 321 MB di PDF acquistati, gitignorati, e fuori da ogni tag e da ogni bundle.**
   Nessuna rete di sicurezza li copre. Sono l'unico bene non recuperabile del progetto.

---

## 2. Stato del repository, verificato oggi

| | |
|---|---|
| `main` | **[fatto]** nove commit sopra `3dff11f` — i quattro di §3 e §5.4, la rimappatura delle citazioni, i passi 4 e 5, e questo aggiornamento |
| Test | **2515 verdi su 108 file** — 2499 · +5 dai cherry-pick di §5.4 · +11 dai tre difetti dello store (§10 passo 5) |
| `tsc` | pulito (`npm run typecheck`) |
| CI / Deploy | erano verdi su `3dff11f`; **da riverificare dopo il push** di questi cinque |
| Node | `.nvmrc` 24 · sistema **26** (rotto per i test) · `.tools/node/bin` **24.19.0** ← usa questo |
| Working tree | **[fatto]** pulito |
| Rami e worktree | **[fatto]** 27 rami → **2** (`main`, `backup-pre-rewrite`) · 23 worktree → **0** · i tre tag `safety/*` intatti · `Manuali/` verificato a 321 MB e 7 file dopo la rimozione |
| Vocabolario | «**il reflow**» = i cinque commit `8def497`..`3dff11f` che hanno rifatto il foglio Play sul telefono · «**il cockpit**» = il layout Play a tre colonne da ≥1180 px (`Play.tsx:29-36`) · «**P0..P4**» = le bande di gravità di `BACKLOG.md` (P0 = può perdere un personaggio) |

**I documenti in-repo sono scaduti**, ed è il motivo per cui esiste questo file: `HANDOFF.md` è
**tredici commit indietro** *(erano otto: mancano tutti e cinque quelli del reflow e i cinque di
oggi — il suo banner di supersessione, invece, è aggiornato)*, `AUDIT-HANDOFF.md` quindici,
`Architecture.md` e `BACKLOG.md` sono pre-reflow. In `HANDOFF.md` ogni paragrafo sui «163 commit non
pushati» è testo morto: `origin/main == main`, e `3dff11f` non è più la punta di nessuno dei due.

> **[corretto]** La prima stesura sosteneva che il conteggio degli orfani di `HANDOFF.md` («22»)
> fosse smentito dal codice, che ne avrebbe 24. **Falso: sono 22.** `tests/harness/orphans.test.ts`
> ha esattamente 22 chiavi in `DELIBERATE` e asserisce l'inclusione in **entrambe** le direzioni,
> quindi i due insiemi coincidono per costruzione; il file è verde. Il numero di `HANDOFF.md` era
> giusto. Il banner in cima a quel file **è già stato corretto e dice la cosa giusta: non toccarlo.**

---

## 3. ~~La prima cosa da fare: committare~~ — **[fatto]**

`git status --porcelain` dava **quattro** voci, e insieme erano la catena di ripartenza. Sono su
`main` in due commit: `9371872` per il docblock di `shared/types.ts`, `115bda2` per i documenti e
per il banner di `HANDOFF.md`.

```
 M shared/types.ts      +27   il docblock su Countdown.notes (§4)
 M HANDOFF.md           +17   il banner di supersessione — già corretto, va solo committato
?? HANDOFF-2026-08-18.md      questo documento
?? docs/                      handoff/{reflow-handoff.md, il suo predecessore, DECISIONI-2026-08-18.md}
```

Il perché, che resta valido la prossima volta: **finché non sono committate, «nel repo» è falso** —
`git ls-files docs/` non restituisce nulla, su un clone non c'è niente, e `git clean -fd` cancella
tutto. Il documento applica a sé stesso la regola che impone al codice: una decisione che vive solo
nell'albero di lavoro non è una decisione che qualcun altro può trovare.

**Convenzione dei messaggi**, letta da `git log` (non esiste un `CONTRIBUTING.md`): oggetto in
inglese, imperativo, **nessun prefisso** tipo `docs:` o `feat:`, spesso due clausole unite da «and».
Esempi reali: *«Make each track one card with a 38px number, and close the band's hole»*, *«Raise the
smallest type on the sheet, and turn the companion's box into a rule»*.

**Diretti su `main`, non in una lane.** Gli ultimi cinque commit (il reflow) sono andati dritti; le
lane erano il modo di lavorare di prima.

---

## 4. Il docblock su `Countdown.notes` — cosa dice e perché

`Countdown.notes` è salvato, riletto, riscritto e **disegnato da nessuno, di proposito**. La prosa
che serve al tavolo è formattata e appartiene a una scena, non a un orologio: prende una riga sua
(voce 14). Una seconda superficie di note più povera accanto sarebbero due feature che si
contraddicono.

La parte che serve davvero a chi verrà dopo: **cancellare il campo non è una pulizia.**
`readCountdown` ricostruisce l'oggetto campo per campo e scarta ogni chiave che non nomina, quindi
una build senza `notes` lo cancellerebbe da ogni campagna salvata alla prima scrittura dopo 400 ms —
una modifica di schema travestita da riordino.

Verificato prima di scriverlo: `grep -rn 'countdown.notes' src/` → 0 · la fixture v1 porta
`'advance on every failed crossing'` · `tsc` pulito e 2499 test verdi dopo la modifica.

---

## 5. La pulizia dei rami — istruzioni eseguibili

### 5.1 Cosa protegge cosa, esattamente

| Rete | Copre | Non copre |
|---|---|---|
| Tag `safety/a2-play-cockpit` | i 7 commit di quel ramo | tutto il resto |
| Tag `safety/a2-play-reflow` | `55678fe` | " |
| Tag `safety/backup-pre-rewrite` | quel ramo | " |
| Bundle in `~/Documents/dh-safety/` | gli stessi tre rami | " |
| **Essere antenato di `main`** | i 19 rami di 5.2 | — |

> **[corretto]** La prima stesura diceva «un `git branch -D` non può orfanizzare i commit finché i
> tag esistono», e per `a2-play-reflow` era **falso**: il ramo non era né taggato né antenato, e il
> suo unico commit `55678fe` non era raggiungibile da nulla. Il tag ora esiste. Verifica:
> `git merge-base --is-ancestor 55678fe safety/a2-play-reflow`

Verifica dei bundle e ripristino:
```
git bundle verify ~/Documents/dh-safety/SAFETY-a2-play-cockpit.bundle
git fetch ~/Documents/dh-safety/SAFETY-a2-play-cockpit.bundle a2-play-cockpit:recuperato
```

### 5.2 Chi si può cancellare — elenco nominale, non pattern

I worktree stanno in `~/Documents/dh-wt/`, `~/Documents/dh-wt2/`, `~/Documents/dh-wt3/`.
Il criterio è **`git cherry main <ramo>`** (uguaglianza di patch-id), *non* `git log main..<ramo>`:
c'è stata una riscrittura di storia, quindi `git log` mostra 111 commit su rami che non hanno **una
sola patch** che `main` non abbia già. Chi verifica con lo strumento sbagliato conclude che questa
tabella mente.

**Antenati di `main` — 19** *(erano dichiarati 17: **[corretto]**)*:
`a2-banners` `a2-cards` `a2-clearall` `a2-erase` `a2-gear` `a2-levelup` `a2-onboarding` `a2-play`
`a2-play-pips` `a2-shell` `a2-wizard` · `a3-cockpit` `a3-docs` `a3-docs2` `a3-docs3` `a3-polish`
`a3-reflow2` `a3-safearea` `a3-targets`

**Non antenati, ma zero patch uniche — 4:** `wt-damage` `wt-gm` `wt-rename` `wt-rest`

**Una patch unica, contenuto già su `main` — 2:**
- `wt-p53` → `65de51a`, che sposta **`ruleText.ts`** in `src/ui/shared/` *(non `srdReference.ts`:*
  ***[corretto]***). Il file c'è.
- `a2-play-reflow` → `55678fe`, la stessa correzione al `.gitignore` che `main` ha via `2fdeeab`.

**In attesa — 1:** `a2-play-cockpit`. Vedi 5.4.

**`backup-pre-rewrite`:** verificato che **non contiene nessun file assente da `main`**. Nulla da
recuperare, ma è una rete deliberata: cancellalo solo se lo vuoi davvero.

> **[corretto — e questa è la ragione vera]** «Nulla da recuperare» era giusto sui *file* e fuori
> bersaglio sul lavoro che quel ramo stava facendo. Misurato il 18 agosto: teneva in vita **tutti e
> 94** gli SHA che `BACKLOG.md` citava e che non erano raggiungibili da `main` — orfani zero. Su un
> clone fresco `git show 1a7ba19` falliva; qui funzionava solo per via di quel ramo. **Cancellarlo
> insieme agli altri ventisei avrebbe reso irrisolvibili 171 citazioni in silenzio**, e nessuno se
> ne sarebbe accorto finché non avesse cliccato uno SHA. Le citazioni sono state **rimappate**
> (§13), quindi adesso il ramo non è più portante — ma tiene ancora `65de51a`, citato in §5.2, e
> tutta la storia pre-riscrittura. Se lo cancelli, quelli spariscono.

### 5.3 Sicurezza della rimozione — provato, non dedotto

**Dodici** worktree contengono un symlink `Manuali` (`dh-wt2/{erase,gear,onboarding,play}` e tutti
gli otto `dh-wt3/*`); **tutti e 23** contengono un symlink `node_modules`. *(La prima stesura diceva
tredici: **[corretto]**.)*

Provato su un repo finto con una cartella-esca: **`git worktree remove --force` non segue i
symlink** — l'esca è sopravvissuta. Nessuna cancellazione può toccare i 321 MB.

**Ma `git worktree remove` senza `--force` fallisce su sei dei 23**, perché contengono file
modificati o non tracciati. Serve `--force`, e serve saperlo prima.

```
# 0. fotografa lo stato, così è ricostruibile
git worktree list > ~/Documents/dh-safety/worktrees-prima.txt
# 1. rimuovi i worktree (--force è necessario, ed è sicuro rispetto ai symlink)
for w in ~/Documents/dh-wt/* ~/Documents/dh-wt2/* ~/Documents/dh-wt3/*; do
  git worktree remove --force "$w"
done
git worktree prune
# 2. cancella i rami sicuri (NON a2-play-cockpit — vedi 5.4)
git branch -D a2-banners a2-cards a2-clearall a2-erase a2-gear a2-levelup a2-onboarding \
              a2-play a2-play-pips a2-play-reflow a2-shell a2-wizard \
              a3-cockpit a3-docs a3-docs2 a3-docs3 a3-polish a3-reflow2 a3-safearea a3-targets \
              wt-damage wt-gm wt-p53 wt-rename wt-rest
```

> **[fatto]** Eseguito il 18 agosto, e i numeri della tabella hanno retto: i 23 worktree sono usciti
> tutti con `--force` senza un errore, `Manuali/` è stato riverificato **dopo** a 321 MB e 7 file, e
> `node_modules` a 117 voci — nessun symlink seguito, come la prova prometteva. Nelle tre radici
> `dh-wt*` sono rimaste solo cinque cache `.vite/deps` orfane, 40 KB in tutto, rimosse anche quelle.
> Oltre a `worktrees-prima.txt` c'è ora `~/Documents/dh-safety/branches-prima.txt` con la punta e
> l'oggetto di ogni ramo cancellato: non serviva a niente di quanto sopra, ma costava zero.
> **`backup-pre-rewrite` è stato lasciato in piedi**, come questa sezione consiglia.

### 5.4 ~~`a2-play-cockpit`: GO, ma prima porta due fix~~ — **[fatto]**

Sette commit senza equivalente su `main`, verificati **confrontando il difetto col sorgente di `main`
di oggi**, non i commit fra loro. Cinque sono chiusi; **due riparano difetti vivi adesso**, e in
entrambi i casi il blocco su `main` è **byte per byte identico al codice pre-fix**.

**① `2cb1f38` — la porta delle Condizioni non si può premere.** Nel cockpit il chip `+ NAME` è
l'**unica** via d'accesso al dialogo delle Condizioni ed è l'ultimo figlio dello scroller orizzontale
silenzioso dei chip (`Conditions.tsx:305-382`, `overflowX: 'auto'`). Con due condizioni nominate la
riga eccede il proprio contenitore e la porta finisce oltre il bordo, disegnando **zero pixel**. Il
commit misura *«593px in 428»* e la porta *«a x 922.8, 120.8px oltre»* — usa **queste** cifre, che
sono nel commit, non quelle della prima stesura, che non stanno da nessuna parte **[corretto]**.
In più `Conditions.tsx:274-275` afferma ancora *«il chip + NAME è la porta, e c'è spazio per
entrambi»*: un docblock che il codice smentisce, quindi già oggi un difetto.

**② `8bd0e91` — `TO VAULT` è un bersaglio da 56×10.** `Play.tsx:2346-2374`: un
`<button className="t-meta">` nudo dentro una banda costruita per un controllo alto
`max(34px, var(--control))`. Cinque sullo schermo insieme, unica azione per carta del cockpit, e
tutti e cinque condividono lo stesso nome accessibile. `Play.tsx:440` dice ancora *«il cockpit non
scorre e ha un mouse»*, smentito da `Play.tsx:29-36` nello stesso file.

Gli altri cinque: tre superati da `main` per altra strada, uno superato due volte, e uno
**inesistente** — riparava una disposizione creata da un altro commit dello stesso ramo, mai arrivata
su `main`.

**Come portarli — verificato in un clone usa-e-getta:**
```
git cherry-pick 2cb1f38 8bd0e91     # si applica PULITO su 3dff11f, nessun conflitto
```
Suite dopo il cherry-pick: **2504 passati su 108 file**, verde — i due commit portano **+5 test**.
*(In un clone nudo vedrai `2499 passed | 5 skipped`: `tests/import/coreRulebook.test.ts` si salta
quando `Manuali/` non è raggiungibile. Non è una regressione.)* Poi `git branch -D a2-play-cockpit`.

**Non c'è motivo di paralizzarsi:** il tag `safety/a2-play-cockpit` tiene i commit anche se cancelli
il ramo per primo, quindi il cherry-pick funziona lo stesso. L'ordine consigliato serve solo a non
dimenticarsene.

> **[fatto]** Eseguito il 18 agosto, e la previsione era esatta in ogni cifra: i due si sono
> applicati **puliti** (`73e184d`, `84bf907`), la suite è passata da 2499 a **2504 su 108** verde e
> `tsc` è rimasto pulito. `git cherry main a2-play-cockpit` segnava poi i due con `-` e i cinque
> restanti con `+`, esattamente la ripartizione descritta qui sopra; il ramo è cancellato e il tag
> resta. **Un guadagno non previsto:** i due commit portavano con sé anche la correzione dei due
> docblock che li contraddicevano — «*And there is room for both*» a `Conditions.tsx` e «*The
> cockpit does not scroll*» a `Play.tsx` sono adesso frasi al passato che spiegano perché erano
> false. Quei due difetti di prosa sono chiusi insieme ai difetti di pixel.

---

## 6. Le voci di backlog già spedite — e la trappola delle P4

> **[corretto due volte]** La prima stesura diceva «19», la seconda «23». Il numero onesto di
> caselle **effettivamente barrabili** è **13**, e sono tutte in P1/P2/P3. Il perché è in fondo a
> questo paragrafo, ed è una trappola in cui sono caduto io per primo.

```
P1-2  P1-3  P1-4
P2-4 (premessa scaduta dal reflow)   P2-5
P3-1  P3-2  P3-3  P3-5  P3-6  P3-8  P3-9  P3-11
```

**Perché le P4 non sono nell'elenco.** In `BACKLOG.md` la banda P4 (righe 1401-1572) è una lista
piatta di `- [ ]` **senza etichette**: gli id `P4-1`, `P4-2`… sono *posizionali*, contati a mano, e
in tutto il file la stringa `P4-` compare **tre volte**. P4-1..P4-5 e P4-9..P4-13 sono già dentro la
tabella Done a `BACKLOG.md:3012` — non c'è nessuna casella da barrare. E soprattutto:

- **`P4-6` non va barrata.** `BACKLOG.md:3026` la nomina esplicitamente fra le voci «*not to be
  marked done by anybody reading this table*». Se sei convinto che sia chiusa (oggi
  `grep -rn 'TODO\|FIXME\|HACK' src/` è a zero), allora la riga da correggere è **quella**, non la
  casella.
- **`P4-14` non esiste.** Era un fantasma di una stesura precedente.

Più **due caselle in P2/P3 dove il lavoro è già nel codice**: i quattro handler spostati sulla riga
di pip (`src/ui/shared/Track.tsx:206-229`) e i dialoghi che ora gestiscono il focus
(`BACKLOG.md:954`, che ne dice cinque — i call-site di `useDialog(` oggi sono **sette**).
E una voce chiusa per decisione che indossa ancora una casella aperta: i generatori di nomi
(`BACKLOG.md:2129`), che però ora vanno **riaperti** — §9⑥.

**`P3-1` è la più urgente:** non compare in nessuna delle tre tabelle «Done» in fondo a
`BACKLOG.md` (righe ~2972, ~3032, ~3086), quindi a freddo non esiste **nessuna** traccia che sia
stato fatto, ed è la prima che verrà ricostruita.

---

## 7. Le decisioni e i costi

Registro completo delle risposte: **`docs/handoff/DECISIONI-2026-08-18.md`** (nel repo da
`115bda2`). Le motivazioni scritte esistono solo per le voci 8, 20 e 23; per le altre la risposta è
la decisione.

**Totale: ≈ 189–195 ore** *(era 190–196: la voce 42 è scesa da 2 h a 1, vedi sotto)* — Giorgio
≈ 67 h · GM screen e backlog **≈ 47–53 h** · le sei richieste nuove ≈ 58 h · il blocco nota 16 h.
**Senza prezzo:** le voci 7 e 8, i due cherry-pick di §5.4, la proposta grande di ①, e le dieci prove
di §8 che non siano H-6.
**Non ancora deciso, benché conteggiato:** ③ (11,6 h, verdetto «serve una decisione») e la proposta
grande di ①. Il totale è quindi un tetto, non un impegno.
**Doppi conteggi noti**, da sciogliere prima di pianificare: gli insets di `GmBar` stanno sia nella
voce 19 sia nella fetta 0 di ①; ~~metà della voce 42 la esegue già il commit del passo 1 di §10~~
**[sciolto]** — quel commit c'è, quindi la voce 42 vale ~1 h invece di 2; e la riparazione di 1,2 h
in ③ è probabilmente lo stesso lavoro della voce 10.

### 7.1 Le richieste di Giorgio

| № | Cosa | Ore | Ancora |
|---|---|---|---|
| 1 | ~~Anteprima schede in sola lettura **e** bottone «apri lo scontro»~~ **[fatta]** — ma l'anteprima è sugli **avversari**, non sulle schede dei giocatori: §15 | ~~2 + 5~~ `ecf8017` |
| 2 | ~~Conferma **sempre**~~ **[fatta]**, e severa come deciso: conferma anche a tavolo vuoto | ~~+0.3~~ |
| 3 | ~~`×3` → gruppi; dire il `+1D4` fuori dal builder~~ **[fatta]** — `×3` era *sbagliato*, non conciso: `count` sono **gruppi** grandi come il gruppo | ~~0.5~~ `ecf8017` |
| 4 | Builder environment **+ overlay nel `.dhbackup`** | 16 + 4 | riscrive `Architecture.md` §4 |
| 5 | Counter come **template** di countdown, non contatore con stato | 3 | `gmStore.addCountdown` |
| 6 | **Rinomina** riga, poi numeri sulla riga del *tipo* | 1 + 5 | nessun rename esiste oggi |
| 9 | Merchant come **strumento GM** (nessun bump) | 7 | `campaigns.ts:176-185` |
| 10 | ~~Rendere la tabella *Average Costs*~~ **[fatta]**, e ha riparato anche le altre | ~~1~~ |
| 11 | ~~Randomizer per tier **solo su armi e armature**~~ **[fatta]** | ~~6.5~~ `0e9886b` |
| 12 | URL come **quinto tipo di `SessionItem`** con campo `href` | 16 | *non* un nuovo `LINK_KIND` (oggi sono quattro); impone di riscrivere il docblock a `shared/campaigns.ts:89-98`, che vieta gli URL esterni |
| 13 | Le sei mitigazioni di sicurezza, **nel reader** | dentro le 16 di №12 | |

### 7.2 GM screen e backlog

| № | Cosa | Ore | Ancora |
|---|---|---|---|
| 15 | ~~Errore di scrittura campagna pubblicato sulla shell~~ **[fatta]** | ~~7.5~~ `bfdc0d8` | `gmStore` `writeError`, `App.tsx` |
| 16 | ~~**`renameCampaign` non arriva sul disco**~~ **[fatto]** | ~~1–3~~ `b9dbe87` | `src/ui/gm/gmStore.ts:539-546`, `:755`; `BACKLOG.md:2031`. Oggi `MenuSheet.tsx:63-66` mura il caso («*only the open campaign can be renamed here*»), quindi non c'è una strada utente: è un difetto di store, non un bug visibile |
| 17 | ~~guardia mancante sulla **cancellazione**~~ **[fatta]** · creazione attiva dopo scrittura fallita **[da decidere, §14]** | ~~2–3~~ **~1** | `src/ui/gm/gmStore.ts:704-744`; `BACKLOG.md:2038` |
| 18 | ~~Le riparazioni ricalcolate a ogni avvio~~ **[fatto]** | ~~1–2~~ `b9dbe87` | `src/store/campaigns.ts:41`; `BACKLOG.md:2047` |
| 19 | Insets orizzontali: `GmBar`, cinque overlay, la ✕ di `GmSheet`, `GmSheet.tsx:77` | 4 | **aspetta la misura, §8** |
| 22 | ~~Chiudere `BACKLOG.md:2003` e `:2157`; **aprire** CLOSE/FAR/VERY FAR~~ **[fatta]** | ~~1.75 + 2~~ `ed8d2fa` | `Play.tsx:1409` |
| 24 | La prosa del reflow: quattordici punti su sei file + test anti-deriva | 6 | |
| 26 | ~~I due difetti nel docblock di `Carried`~~ **[fatta]** | ~~2~~ | `Disclosure.tsx:40-43`, `:68` |
| 27 | ~~**No** al taglio degli stepper, **sì** all'anello~~ **[fatta]** | ~~2.75~~ | `src/ui/shared/Counter.tsx:330` |
| 29 | ~~Banner della party board + copertura delle due azioni scoperte~~ **[fatta]** | ~~4.5~~ | `PartyBoard.tsx:166` |
| 30 | ~~Barrare le 13 caselle di §6 (+ decidere su `P4-6`)~~ **[fatto]** — 32 caselle, 15 voci | ~~1~~ `ee3c8ac` | `BACKLOG.md:3012`, `:3026` |
| 31 | ~~Il chip ARM che cicla a 3 mentre il motore ne spende 1~~ **[fatta]** | ~~0.75~~ | `Vitals.tsx:623` vs `damage.ts:58-62` |
| 32 | ~~**Riscrivere la frase** di P1-6~~ **[fatta]** — al secondo tentativo: la prima riscrittura era falsa a sua volta | ~~0.2~~ `f33a14b` | `codec.ts:947` |
| 33 | ~~Le tre porte del nome unico~~ **[fatta]** — e ha scoperto un buco vero nell'import, chiuso in `cc71252` | ~~4~~ `a6d91c1` | `BACKLOG.md:1765` |
| 34 | ~~P3-12 **riclassificata P1**~~ **[fatta]** · P2-6 e P2-3 restano | ~~3~~ `ed8d2fa` | `DamageRoll.tsx:371` |
| 35 | ~~Import dinamico del QR~~ **[fatta]** | ~~2–4~~ `0e7d1a2` | vedi nota sotto |
| 42 | ~~Richieste in `BACKLOG.md` P5 · handoff del reflow nel repo~~ **[fatta]** — P5-9 | ~~2~~ | `115bda2`, `1a89fa2` |

> **Nota su №35 [corretto]:** i 71 KB gzip sono esatti, ma **il QR è già dietro import dinamico** nel
> bundle costruito: il chunk d'ingresso importa staticamente solo `srd`. Il lavoro vero è togliere
> l'import statico dai sorgenti di `PartyBoard.tsx:21-23` perché resti vero. Meno urgente di come era
> presentato.

**Chiuse senza lavoro:** 21 (misurare la tastiera — si sposta sul blocco nota) · 25 (la reach: «aria
vera» vale anche per quella) · 28 (320/344 e i due banner: solo prosa, dentro la voce 24).
**La 23 non è chiusa senza lavoro:** quattro dei cinque no reggono, ma il quinto è stato ribaltato e
vale le 9 h della richiesta ⑤.

### 7.3 Ribaltate in corsa — contraddicono le tabelle sopra

- **№ 20 annullata.** Niente registro, niente annulla, niente storia. `CAMPAIGN_MIGRATIONS` **resta
  vuota per quanto riguarda la 20**, e `Countdown.notes` resta non disegnato (§4).
- **№ 14 allargata.** Da stopgap da 2 h a blocco nota con testo formattato, esportabile e
  importabile. **16 h, e impone il bump di schema** — vedi la nota qui sotto.
- **№ 23 contro la richiesta nuova ⑤.** Alla 23 il proprietario ha detto no a `BACKLOG.md:1980`
  (SEARCH); poi ha chiesto la ricerca delle regole in SHOW. **Vince la seconda.** Quindi i no della
  23 sono quattro, non cinque.

> **Il bump di schema, detto una volta sola perché due sezioni lo raccontavano in modi
> incompatibili.** La voce 20 non lo paga più. Ma le voci **12** (riga URL) e **14** (riga nota)
> aggiungono ciascuna un nuovo tipo di `SessionItem`, e una build vecchia che legge un tipo che non
> conosce lo avvolge come `unreadable` e il debounce da 400 ms **riscrive quella lettura**. Quindi il
> salto 1→2 serve, e **serve una volta sola per entrambe**: vanno decise insieme, non una per volta.

### 7.4 Ancora aperte — nessuno ci ha risposto

- **№ 7 — «scena» è un luogo o una rissa?** Nel codice una scena ha un `environmentRef` e un
  encounter ha un `roster`: sono due cose diverse. Giorgio ha scritto entrambe le versioni.
  **Prossima azione: chiedere a Giorgio**, non leggere altro codice — nessuna lettura può deciderlo.
  **Blocca la 8, e con essa qualunque annidamento.**
- **№ 8 — deciso, ma condizionato alla 7.** *Se* si annida, l'annidamento sarà una **preferenza
  vera**: entrambe le rese, entrambi i modelli di riordino, e una campagna scritta annidata dev'essere
  corretta riletta piatta. Non costruire l'annidamento senza questo.

---

## 8. La sera col telefono — cosa sono le prove

Sono **undici**, e vengono dalla sezione «Needs a human, two devices and a dim room» di
`BACKLOG.md:2752`. *(Il registro delle decisioni parla di «otto prove» alla voce 39: sono un'altra
lista — le otto di `AUDIT-HANDOFF.md` §7, di cui le domande §4 del reflow ne coprivano solo due. Le
due liste si sovrappongono e si fanno nella stessa serata.)*

| | Prova | Perché conta |
|---|---|---|
| H-1 | Passaggio via QR, stanza buia, due dispositivi | P0 |
| H-2 | «Aggiungi a Home» su iOS e il ponte pasteboard | |
| H-3 | Offline, a freddo | |
| H-4 | Il backup scrive davvero un file | P0 |
| H-5 | Il ripristino non si mangia lavoro più recente | P0 |
| **H-6** | **Eviction iOS: iOS cancella i dati dopo più di una settimana di non uso** | **scade — vedi sotto** |
| H-7 | Home indicator su cinque schermate e due flussi di Build | dà anche la misura di §7 voce 24 |
| H-8 | Un pollice su Play, al tavolo, su un 393×852 | |
| **H-9** | **`env(safe-area-inset-left/right)` in orizzontale** | **il cancello** |
| H-10 | Stessa rotazione con Display Zoom (693×320) | stesso telefono, un interruttore |
| H-11 | Un dito su un portatile touch | **decide** il token `--control` (H-9 lo *blocca*, H-11 lo *risolve*: servono entrambe) |

**H-9 è il cancello.** Finché quel numero non esiste, la voce 19, la colonna Play, i rail dei Cards,
gli overlay e il token `--control` valgono zero oppure sono il difetto ergonomico più grosso aperto,
e nessuno sa quale. **Non fare lavoro sugli insets prima.**

**H-6 costa latenza, non ore.** Installa la PWA su un iPhone, annota la data, non aprirla per otto
giorni, poi guarda se i dati ci sono ancora. Quindici minuti oggi danno la risposta fra una
settimana; non iniziarla significa non eseguirla mai.

La sonda per H-9 è una **pagina usa-e-getta** servita dal portatile in Wi-Fi (decisione 38): una
riga permanente in Info farebbe fallire un test che verifica che le dichiarazioni di inset in linea
siano esattamente una.

> **[rimandato — 18 agosto]** Il proprietario ha scelto di rinviare l'intera serata, **H-6
> compresa**. Due conseguenze da tenere in mente e non da riscoprire: il cancello H-9 **resta
> chiuso**, quindi la voce 19, la fetta 0 di ①, i rail dei Cards, gli overlay e il token
> `--control` non si toccano; e H-6, non essendo partita, non ha un orologio che gira — la sua
> risposta non arriverà otto giorni dopo una data, arriverà otto giorni dopo il giorno in cui
> qualcuno la farà partire. Il lavoro prosegue solo fuori dal cancello.

---

## 9. Le sei richieste nuove

| | Richiesta | Ore | Verdetto |
|---|---|---|---|
| ① | Navigazione GM migliore | **3** (solo la fetta 0) | la proposta grande ribalta decisioni già prese |
| ② | Alternativa comprensibile a SHOW | **4.8** | rinominare, ma **non** in LOOK UP |
| ③ | Tips in inglese su regole e app | ~~11.6~~ **10.4** | la fetta da 1,2 h è **fatta** · il resto serve ancora una decisione |
| ④ | I pannelli dei due PDF nella home del DM | **8** | lavoro di superficie, non di contenuto |
| ⑤ | Ricerca regole + indice in SHOW | **9** | nessun indice da costruire |
| ⑥ | ~~Generatore di nomi~~ **[fatto]** | ~~22~~ `7457da6` | 15.325 stringhe, enumerate per intero nel test |

**① — la fetta 0, e il conflitto risolto.** La ristrutturazione grande riapre tre caselle già
spuntate (`BACKLOG.md:1820-1836`, `:1987-1990`) e contraddice `Reference.tsx:8-16`. La **fetta 0**,
3 h, non decide niente: chip SCENE incondizionato, copy dello stato vuoto, il cluster di docblock che
dicono «cinque» su una lista di quattro (`Gm.tsx:17`, `:23`, `GmBar.tsx:87`, `GmSheet.tsx:14`), e i
due insets orizzontali di `GmBar`. **[corretto]** Quei due insets sono **una parte** della voce 19,
non la sua chiusura, e **aspettano H-9** come tutto il resto degli insets: se fai la fetta 0 prima
della misura, fai tre quarti di essa e lasci fuori gli insets.

**② — SHOW.** Verbo transitivo senza oggetto sullo schermo, definito per negazione:
*«SHOW: the two tools no row can open»*. **LOOK UP è già speso**: `Reference.tsx:2` e `:12` lo usano
come la ragione scritta per cui il riferimento non sta sulla barra. Il costo non è l'etichetta, sono
**114 occorrenze** da rileggere a mano (47 in `src`, 39 nei test, 28 nei doc).

**③ — la premessa era falsa.** Le sezioni SRD **non sono irraggiungibili**: `ADD → LINK → Rule` le
offre tutte e 75 e la riga stampa il testo intero. *(«Raggiungibile» ha due sensi in questo
documento: dal menu ADD — tutte e 75 — e come argomento proprio dello schermo Reference — 18. La
frase di ④ usa il secondo.)* Il guadagno immediato è un difetto in quella porta:
`SessionBody.tsx:363` usa `paragraphs()` da solo, quindi ogni elenco stampa un `- ` letterale e ogni
tabella esce con le pipe — in **38** sezioni su 75 (34 con elenchi, 7 con tabelle, **3 con entrambe**),
non in tutte **[corretto]**. Sono 1,2 h, ed è una **fetta dentro** le 11,6, non un lavoro in più.

**④ — quasi tutto è già nei dati.** La tabella *Average Costs* p69 combacia riga per riga col
pannello RESOURCE EXAMPLES, tutte e dodici. Non ci sono: le quattro liste di nomi (→ ⑥) e la colonna
**metrica** della tabella Range & Distance, che il PDF ha convertito da sé — metterla in app sarebbe
la app che scrive una regola. **Discrepanza da arbitrare contro l'SRD:** i due PDF non concordano
sulla scala di Difficoltà (Landscape 5 = Easy, Portrait 5 = Very Easy).
**Difetto trovato di rimbalzo:** `src/engine/rest.ts:35-99` ha il testo delle mosse di downtime
**digitato dentro il repo**, mentre `rules['downtime']` p41 le contiene tutte.

**⑤ — nessun indice da costruire.** Le 75 sezioni pesano ~45 KB gzip dentro un chunk che `index.html`
già precarica, e una scansione completa di titoli e corpi gira in **~0,11 ms** *(le prime due
stesure dicevano 0,019 ms e poi lo attribuivano ai soli titoli: **[corretto]**, sbagliate entrambe —
i soli titoli costano ~0,0015 ms)*. Resta comunque quattro ordini di grandezza sotto la soglia
percepibile: la conclusione non cambia, il numero sì.

**⑥ — il generatore, e la lezione che ne è uscita.** Le quattro liste del Core Book (47/43/18/20
voci) non sono nell'SRD: zero su 128. `README.md:284` e `Architecture.md:1394` — l'SRD è
ridistribuibile sotto DPCGL, il Core Book no.

Il primo tentativo di tabelle, **enumerato per intero** (268.871 stringhe producibili), emetteva
cinque voci del Core Book alla lettera (`Hollow Keep`, `Idle Fiend`, `Watcher's Ravine`,
`Wicked Smile`, `Alvyon`) e cinque parole dell'SRD già spedito (`Human`, `Hydra`, `Siren`, `Vines`,
`History`). Il campione casuale sembrava pulito **per fortuna**: trenta estrazioni non trovano una
stringa su 190.000.

**Le due regole, che sono promesse diverse e vanno entrambe nel docblock del modulo:**
- la regola di **provenienza** (non scomporre le loro liste) **non è verificabile dall'output**;
- la regola di **collisione** (non emettere niente che il dataset già contenga) **lo è, in modo
  esaustivo, e va testata**: il test enumera tutto lo spazio producibile e verifica che
  l'intersezione con ogni `"name"` di `data/srd-1.0.json` sia vuota.

Perciò la firma è `(rng, taken) => string`, non `(rng) => string`. Registro giusto: i nomi di
Daggerheart sono corti e botanici sopra cognomi del mondo reale. Campione reale delle tabelle
corrette: *Quince Vinedale · Iris Wyndholm · Foxglove Oakstone · Hazel Merethorpe · Yarrow Salter ·
Larkspur · Rix · Sedgetide · Astermouth · Valley of Quiet Trade · The Gilded Chapel*.

**Costo non contato altrove:** aggiungere `'names'` a `GmRegion` tocca `Architecture.md:649-660`,
intitolato *«L'unica eccezione, e perché è una sola»*. Aggiungerne una seconda obbliga a riscrivere
§6.1.

**Il blocco nota (№ 14) — 16 h.** Tre vincoli non negoziabili: **niente `contentEditable`** (oggi non
c'è un solo `dangerouslySetInnerHTML`/`innerHTML` in tutto `src/`, e non esiste nessuna CSP — GitHub
Pages non può mandarne); **non estendere `ruleText.ts`**, che sta sul percorso di lettura dell'SRD;
la centratura **non esiste in markdown**, quindi la sintassi va inventata e finisce dentro il file
esportato.

---

## 10. L'ordine consigliato

Questo è l'ordine **delle prime settimane**, non di tutto: le richieste ②④⑤⑥ (43,8 h), il blocco
nota (16 h) e le voci 4, 5, 9, 10, 11, 12, 13, 22, 24, 26, 27, 29, 31, 32, 33, 42 vengono dopo.

1. ~~**Committare i quattro** di §3.~~ **[fatto]** — due commit, `9371872` e `115bda2`.
2. ~~**Cherry-pick dei due fix** (§5.4), poi la pulizia dei rami (§5.2–5.3).~~ **[fatto]** — suite
   2504 verde, `tsc` pulito, 27 rami → 2, 23 worktree → 0, `Manuali/` intatto.
3. ~~**La sera col telefono** (§8).~~ **[rimandato dal proprietario]** — resta **un cancello**, e
   H-6 non è partita. Finché non c'è la misura di H-9, niente lavoro sugli insets. **La ripartenza
   vera è il passo 4.**
4. ~~**Barrare le 13 caselle** di §6, `P3-1` per prima, e correggere `BACKLOG.md:3026` se decidi che P4-6 è chiusa.~~
   **[fatto]** — sono risultate **32 caselle su 15 voci**, non 13: il conteggio di §6 era per *voce*,
   non per casella, e P2-2 (tre caselle sotto un'intestazione già barrata) non era nell'elenco.
   Ognuna verificata contro il sorgente prima di barrarla. **Tre non sono «fatte» e lo dicono:**
   il punto Beastbound di P1-4 è **confutato**, non costruito; P2-4 ha la **premessa scaduta** dal
   reflow; P4-6 **non ha oggetto** — zero marcatori in cinque cartelle e nessun commit in 381 che
   ne abbia mai cambiato il conteggio, quindi `BACKLOG.md:3026` è stato corretto, non la casella.
   `P3-1` ha adesso una riga nella tabella Done: non ne aveva in nessuna delle tre.
5. ~~**I tre difetti dello store** (16, 17, 18)~~ **[fatto, tranne metà della 17]** — un commit,
   `gmStore.ts` + `campaigns.ts` + tre file di test. I tre difetti erano **lo stesso buco**:
   `writeActive` raccoglieva solo la campagna attiva, quindi non esisteva alcuna strada per
   scrivere una campagna che il GM non stesse guardando. C'è ora un insieme `aside`. **Resta
   aperta la prima metà della 17** — `createCampaign` che rende attiva la campagna anche a
   scrittura fallita — e va **decisa**, non implementata: vedi §14.
6. ~~Poi, per rapporto valore/rischio: ③ la fetta da 1,2 h · 34 (P3-12) · 15 · 1+2+3.~~
   **[fatto, e molto oltre.]** In una passata a quattordici lane parallele sono
   state chiuse anche le voci **10, 11, 22, 26, 27, 29, 31, 32, 33, 35** e la
   richiesta **⑥**. Suite da 2515 a **2688**. Ogni lane è stata riletta da un
   verificatore avversariale che ha *eseguito* le mutazioni invece di leggerle:
   sei lane su quattordici sono tornate **non sane**, e una portava un difetto
   vero (§14 non c'entra — vedi `cc71252`: la porta del nome unico scartava un
   record locale *tenuto*, quindi un secondo Ilya atterrava senza rinomina).
   Tutto corretto prima del push.
7. **Quel che resta**, in ordine: ② (4,8 h, 114 occorrenze a mano) · ④ (8 h, ma
   prima va arbitrata la scala di Difficoltà fra i due PDF) · ⑤ (9 h) · le voci
   4, 5, 6, 9, 12, 13, 14, 24 · e la fetta grande di ③, che **non è approvata**.
   Le voci **12 e 14** condividono un solo bump di schema e vanno decise
   insieme: è l'unica cosa in tutto il piano che cambia un formato salvato per
   sempre, e non è stata toccata di proposito.

**Dipendenze:** la 8 aspetta la 7 (e la 7 aspetta Giorgio) · la 19, la fetta 0 di ① e il token
`--control` aspettano H-9 · le voci 12 e 14 condividono **un solo** bump di schema e vanno decise
insieme · la 6 fa la rinomina **prima** dei numeri.

---

## 11. Dove vivono le cose

| Cosa | Dove |
|---|---|
| Registro decisioni | `docs/handoff/DECISIONI-2026-08-18.md` — **nel repo da `115bda2`** |
| Handoff del reflow | `docs/handoff/reflow-handoff.md` — **nel repo da `115bda2`** |
| Trascrizione Giorgio | `Giorgio modifiche DM/w2/transcript-...md` (gitignorato, resta locale) |
| PDF schermo GM | `Manuali/GM Screen Daggerheart - *.pdf` (gitignorati — non ridistribuibili) |
| Bundle di sicurezza | `~/Documents/dh-safety/*.bundle` |
| Tag di sicurezza | `safety/a2-play-cockpit`, `safety/a2-play-reflow`, `safety/backup-pre-rewrite` |
| Worktree | ~~`~/Documents/dh-wt/`, `dh-wt2/`, `dh-wt3/`~~ — **non esistono più**; lo stato di prima è in `~/Documents/dh-safety/worktrees-prima.txt` e `branches-prima.txt` |
| Rig di misura | **fuori dal repo e locale a questa macchina**: `~/.claude/projects/-Users-simonealbanese-Documents-Daggerheart-Companion/audit-harness/` (`cdp.mjs` + `fixtures.json`). Su un clone non esiste; se serve a qualcun altro va spostato sotto `tools/`. |

---

## 12. Le regole che vincolano chiunque costruisca qui

- Pavimento **44 px coarse / 34 px fine**, **dichiarato inline** — jsdom legge solo gli stili inline,
  un'altezza che arriva da una classe o da `align-self: stretch` **vale 0** in un test.
- **Ogni test deve fallire contro il codice pre-fix**; dì quale mutazione uccide.
- **Un docblock che il codice smentisce è un difetto.** Quando un numero si muove, cercalo ovunque.
- **jsdom, `env()` e `max()` — misurato oggi con una sonda, e il risultato smentisce sia il backlog
  sia le prime due stesure di questo documento.** Non esiste la regola semplice che tutti citano.
  Ecco cosa sopravvive davvero (`el.style.setProperty`, poi rilettura dell'attributo `style`):

  | Dichiarazione | jsdom |
  |---|---|
  | longhand · `env(...)` nudo | **perso** |
  | longhand · `calc(0px + env(...))` | **tenuto** ← l'unica forma affidabile |
  | longhand · `max(10px, env(...))` | **perso** |
  | shorthand · `8px calc(20px + env(...))` | **perso** |
  | shorthand · `calc(env(...) + 8px) 0 0` *(il vero `GmSheet.tsx:77`)* | **tenuto** |
  | `min-height: max(44px, 34px)` | **tenuto ma ridotto a `calc(44px)`** |
  | `min-height: max(44px, var(--control))` | tenuto intatto |

  Due conseguenze che nessuna delle fonti in circolazione ha:

  1. **Il fallimento delle shorthand non riguarda `env()`.** Riguarda il *mescolare* valori `calc()`
     e valori semplici: `padding: calc(1px + 2px) 4px` si perde, e non c'è nessun `env()` dentro.
     `calc(...)` da solo passa, `calc(...) calc(...)` passa, `4px 5px 6px 7px` passa; mescolati no.
     Perciò **`GmSheet.tsx:77` non è un sito nascosto**: jsdom lo legge benissimo. La diagnosi del
     backlog («essendo una shorthand») è sbagliata, e lo era anche la mia correzione.
  2. **Un pavimento scritto `max(44px, 34px)` perde l'alternativa**: jsdom lo riduce a `calc(44px)`.
     Con un `var()` dentro sopravvive. Riguarda esattamente il primo punto di questo elenco.

  **Regola operativa: scrivi `calc(0px + env(...))` su una longhand, e per qualunque altra forma
  misura la stringa esatta invece di dedurla.** Il docblock a `Header.tsx:362-364` («a `padding`
  shorthand carrying an `env()` is dropped whole») è **falso** e va corretto; gli altri tre
  (`Header.tsx:142`, `LicenceFooter.tsx:124`, `attribution.test.tsx:323`), che parlano di `env()`
  nudo, hanno ragione.
- `env(safe-area-inset-bottom)` pagato **esattamente una volta** per schermata.
- Mai `git add -A` in un worktree: `node_modules` e `Manuali` sono symlink.
- **Le decisioni estetiche si prendono dalle immagini, non dalle descrizioni.** Costruisci la
  variante come override a runtime sull'app viva, fai lo screenshot, e falla scegliere.
- Misura il testo con un `Range`, mai con `scrollWidth`.
- **Il proprietario legge e risponde in italiano**, e vuole le scelte come domande con il costo di
  ciascuna. Dai l'aritmetica, poi una raccomandazione.
- **Uno SHA citato in un documento va verificato antenato di `main`, non solo esistente.** §13 dice
  perché, e costa un comando: `git merge-base --is-ancestor <sha> main`.

---

## 13. Le citazioni morte — trovate e riparate il 18 agosto

La riscrittura di storia citata in §5.2 ha un secondo effetto che nessun documento nominava. Ogni
commit citato nei documenti prima di quella riscrittura puntava al **gemello vecchio**, e nessuno di
quei gemelli è raggiungibile da `main`. Misura di partenza, su 285 hash fra backtick:

| documento | citazioni | su `main` | morte |
|---|---|---|---|
| `BACKLOG.md` | 230 | 87 | **143** |
| `HANDOFF.md` | 50 | 23 | **27** |
| `Architecture.md` | 5 | 4 | **1** |

**Su un clone fresco non si risolvevano affatto** — `git show 1a7ba19` falliva. Funzionavano qui, e
solo qui, perché il ramo `backup-pre-rewrite` le teneva tutte e 94 (le distinte di `BACKLOG.md`),
orfani zero. §5.2 raccomandava di tenerlo per una ragione diversa e più debole; la ragione vera era
questa, e non era scritta da nessuna parte.

**Rimappate tutte** in `0274adf`, con quattro verifiche per hash e non una: il soggetto del commit
morto combacia con **esattamente un** commit su `main`; l'abbreviazione scritta al suo posto risolve
a quel commit e a nessun altro; il soggetto del sostituto è identico byte per byte; e il sostituto è
antenato di `main`. Tutti e 99 gli hash distinti hanno passato tutte e quattro. **Zero saltati, zero
indovinati** — un hash che avesse trovato due soggetti, o nessuno, sarebbe stato lasciato stare e
segnalato.

Dopo: 328 citazioni sui cinque documenti, e le uniche sei fuori da `main` sono in **questo** file,
che le cita proprio perché sono fuori — `55678fe`, `65de51a`, e i due sorgenti del cherry-pick.
Tutte e quattro ancorate da un tag `safety/*` o da `backup-pre-rewrite`, **verificato dopo** la
cancellazione dei rami e non dedotto prima.

---

## 14. ~~L'unica cosa aperta dei passi 4 e 5~~ — **decisa, `d7cc7e9`**

**La prima metà della voce 17: `createCampaign` rende attiva la nuova campagna anche quando la sua
scrittura è fallita.** Il backlog la elenca come difetto. Prima di costruirla, il quadro è cambiato,
e in un modo che rende la scelta reale invece che obbligata.

**Perché era così, e perché la difesa reggeva.** Il commento nel codice dice che senza quella riga
`dirty` resta falso, quindi il flush successivo esce a `if (!dirty)`: TRY AGAIN non scriveva niente,
`pagehide` non scriveva niente, e la campagna sopravviveva solo se un cambiamento successivo e
scorrelato se la portava dietro per caso. Renderla attiva **era** il meccanismo del ritentativo.

**Cos'è cambiato.** L'insieme `aside` introdotto dalla voce 16 sa scrivere una campagna che nessuno
sta guardando. Il ritentativo non dipende più dal fatto che sia aperta. **Per la prima volta
l'alternativa è costruibile.**

**Cosa resta da decidere, ed è una domanda su cosa significhi il tap, non sullo store:** un GM che
ha premuto NEW CAMPAIGN e si ritrova sulla vecchia lavagna con una striscia rossa può leggerlo come
«il tap non ha funzionato» — che è vero — oppure come «l'app mi ha ignorato». Le due opzioni:

- **lasciare com'è** (0 h): il GM entra nella campagna nuova, la striscia dice che esiste solo in
  questa scheda, il ritentativo la scrive. Nessuna riga di codice.
- **restare sulla vecchia** (~1 h ora che `aside` esiste, era impossibile prima): il tap non porta
  da nessuna parte, la campagna sta nell'elenco, `aside` la scrive quando lo storage riprende.
  Costa un test nuovo e una frase che spieghi perché lo schermo non è cambiato.

> **Deciso: si lascia com'è** (`d7cc7e9`). Il tap ha detto NEW CAMPAIGN e una lavagna arriva; il
> fallimento è una frase su quella lavagna, dove uno schermo che non cambia si legge come «il tap è
> stato ignorato» — e l'app non sa distinguere le due letture, quindi non deve creare l'ambiguità.
> Non si rischia niente in nessuno dei due casi: la campagna che si lascia atterra per prima, sul
> `flushGm` in testa alla stessa funzione, quindi il caso peggiore è una campagna vuota da rifare.
> **Il ragionamento sta accanto alla riga che riguarda, e dice di non riaprirla come «la campagna è
> resa attiva comunque»: è vero, ed è la risposta.**

---

## 15. La sola domanda aperta dalla passata a quattordici lane

**Richiesta ① di Giorgio, prima metà: l'anteprima è sugli avversari, non sulle schede dei
giocatori.** L'agente che l'ha costruita non ha scelto in silenzio — ha costruito e poi segnalato,
che è il comportamento giusto, ma la scelta è tua.

**Il fatto che la decide:** una riga-incontro porta un `roster` di riferimenti ad avversari e una
lista `combatants`. **Non porta nessuna scheda di giocatore.** Quelle stanno sulla party board, che
è un'altra riga e un altro componente (`PartyBoard.tsx`).

**Cosa dice Giorgio:** «*non ti dà la possibilità di aprire le schede dell'incontro*». L'analisi
della trascrizione già nel repo (`Giorgio modifiche DM/w2/transcript-2026-08-17-19.08.58.md`,
«Righe portanti» 1) legge quella frase come «*le schede degli avversari*».

Quindi la lettura letterale — anteprima delle schede dei personaggi — non ha nulla da mostrare a
quell'ancoraggio. Cos'è stato costruito: ogni voce del roster che si risolve è una disclosure che
disegna `AdversaryBlock` sotto di sé, **senza `action`**, quindi senza un solo bottone, input o
select — la sola lettura è una proprietà del componente, non una promessa del file.

- **Se intendevi gli avversari:** è fatta, non c'è niente da decidere.
- **Se intendevi le schede dei giocatori:** è un lavoro diverso su un file diverso, e va aggiunto.
  Non è una correzione di quello che c'è.

**Due cose trovate di rimbalzo e non toccate di proposito**, entrambe da una riga, perché fuori
dagli ancoraggi di ③: `Encounter.tsx` `AddButton` stampa ancora `IN ×3` nel badge del picker (cella
da 46px, «3 GROUPS OF 4» non ci sta, e il suo `aria-label` tace sul fatto che un tap aggiunge un
gruppo intero); e `session.ts` `describeItem` riassume una riga chiusa come «3 PLANNED», che sono
unità di roster e non avversari — conciso, non falso, ma è lo stesso numero. Chiuderle muove le
attese di `tests/gm/session.test.ts`.
