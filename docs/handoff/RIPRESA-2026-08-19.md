# Messaggio di ripresa — da incollare all'inizio della prossima sessione

Il file è tutto qui sotto: si copia dalla riga dopo il separatore fino in fondo.
Scritto il 19 agosto 2026 alla pausa, con `main` a `a29f284`.

**Se lo riprendi molto più tardi**, i tre numeri che invecchiano sono lo SHA, il
conteggio dei test e la distanza da `origin`. Si rimisurano in dieci secondi —
`git log --oneline -1`, `. ./env.sh && npx vitest run`,
`git rev-list --count origin/main..main` — e il messaggio dice già di farlo.

---

Riprendiamo il Daggerheart Companion.

Leggi `HANDOFF-2026-08-19.md` nella radice del repo, e dentro quel file vai a
**§1 e §2 prima di ogni altra sezione**: §1 dice che il lavoro è in pausa con
l'albero pulito, §2 è lo stato misurato. Poi **§6**, che è l'unica cosa che
riguarda il futuro. `HANDOFF-2026-08-18.md` e `HANDOFF.md` esistono ancora ma
sono superati e portano un cartello che lo dice — di `HANDOFF.md` **è mantenuto
solo il cartello**, e tre frasi sotto sono false apposta.

Le sei cose da sapere subito:

1. **NON C'È NIENTE IN VOLO.** Nessun workflow, nessun worktree, nessun ramo da
   unire, niente a metà. `main == origin`, albero pulito. È il primo handoff da
   settimane che non apre con «c'è un'onda in volo»: non devi aspettare nulla e
   non c'è lavoro di qualcun altro da non rifare.

2. **L'AUDIT DELLE 80 VOCI DI BACKLOG È FATTO E NON APPLICATO.** Sta in
   `docs/handoff/BACKLOG-AUDIT-2026-08-19.md`. Dieci corsie hanno giudicato
   tutte e 80 le voci `- [ ]` di `BACKLOG.md` contro l'albero: **41 risultano
   già chiuse**, di cui **30 rette** da uno scettico indipendente, **1
   confutata**, e **10 marcate `[NON verificato]`** perché due corsie di
   confutazione non hanno fatto in tempo a girare. `BACKLOG.md` è **intatto**.
   Applicare le 30 rette è il lavoro col miglior rapporto valore/fatica che ci
   sia adesso. **Le 10 non verificate non si spuntano** senza rifare quel giro.

3. **LA CONFUTAZIONE DI `:1653` È LA RAGIONE PER CUI QUEL GIRO ESISTE.** La voce
   sembrava chiusa da `ScreenBoundary.tsx`, e invece `AppBoundary.tsx` — il
   boundary sopra `useStats()`, l'Header, la TabBar e i banner — porta ancora il
   difetto identico: la frase citata verbatim, `componentStack` consegnato a
   `console.error` e scartato, `{error.message}` da solo senza modo di copiarlo.
   **Va spezzata, non spuntata.** Chiudere una voce davvero aperta cancella
   lavoro dal piano, perché nessuno rilegge una casella spuntata. Il default,
   ogni volta che sei in dubbio, è **lasciare aperto**.

4. **NON FIDARTI DI UN RAPPORTO, RIESEGUI.** È la lezione che è costata di più a
   questo progetto e vale ancora: i rapporti dicono «verde e finito» anche
   quando non lo sono. E per capire se un'onda è viva o morta **l'unico segnale
   che conta è una riga `result`**: il silenzio non prova niente, i file agente
   mancanti non provano niente (gli agenti di verifica non scrivono
   trascrizione), e «il processo padre è sparito» non scatterà mai, perché
   `/clear` cambia l'id di sessione ma non il processo.

5. **I test si lanciano SOLO dalla radice del worktree**:
   `. ./env.sh && npx vitest run` e `npm run typecheck`.
   Il Node di sistema è 26 e nasconde `localStorage` a jsdom: una suite verde
   sotto quello è **più debole** di quella di CI. Baseline alla pausa:
   **3084 test su 128 file**, verdi, `tsc --noEmit` pulito, Node **v24.19.0**.

6. **Stato alla pausa:** `main` = `a29f284`, versione **0.3.0**, pubblicata.
   **CI e deploy entrambi verdi.** Rami: `main` e `backup-pre-rewrite` (si
   tiene). `Manuali/` 321 MB / 7 file; `node_modules`, `.tools` e `Manuali` sono
   directory vere nella radice — **sono symlink solo dentro i worktree**, ed è
   per quello che `git add -A` è vietato.

Cosa fare, nell'ordine di §6:

- **applicare l'audit** del punto 2 — le 30 rette, poi rifare la confutazione
  sulle 10 non verificate, poi spezzare `:1653`;
- **il Layout B**, la home del DM a schede (THE NIGHT / REFERENCE): **deciso il
  19 agosto e mai costruito**, zero righe. È il pezzo più grande e più
  chiaramente approvato. Prima di scrivere codice conviene un giro di
  progettazione contro `GmBar` e `Reference.tsx`, perché l'argomento che ha
  deciso B — la fascia 560–820 che il pollice copre a 393×852, e «read rather
  than pressed» — è anche il vincolo che la costruzione deve rispettare;
- **③**, ma solo dopo aver **rifatto la stima**: la sua premessa era falsa e il
  difetto che descriveva era chiuso da `4bdf694`. Le 1,2 h che cita non
  esistono.

Quattro decisioni sono già prese e **non vanno ridiscusse** (§5, e sono
registrate in `docs/handoff/DECISIONI-2026-08-18.md`): la home del DM è il
Layout B; l'ottavo topic del Riferimento è `costs` con la striscia ordinata per
larghezza; SHOW a una voce sola **si lascia com'è**, difetto accettato; il
mercante è la terza porta di SHOW, spegnibile.

Cosa non toccare, e sono decisioni, non sviste (§8):

- **il cancello H-9 resta chiuso**: inset orizzontali, rail dei Cards, overlay,
  `--control`, voci 7, 8 e 19;
- **le sezioni rilasciate di `CHANGELOG.md`**: quattro righe dentro `## 0.2.0`
  dicono cose ora false e restano, perché un changelog è in append e correggere
  un rilascio falsifica il registro;
- **le frasi dentro `HANDOFF.md`**: si mantiene solo il cartello;
- **la regola di `SESSION_ITEM_KINDS` non è esaurita**: la lacuna adesso è il
  solo `unreadable`, e il prossimo kind che arriva al record senza schermo
  aspetta allo stesso modo — entra nel menu nella stessa modifica che gli dà il
  form, mai prima;
- **mai `git add -A`.**

Se vuoi andare più veloce, §7 elenca le lezioni in ordine di costo e le manopole
sono gratis. Ma non tagliare il giro avversariale: è ciò che ha impedito a ogni
difetto di questa sessione di arrivare su `main`, ed è ciò che ha preso `:1653`.
