# Handoff — l'audit del 3–4 settembre, e cosa manca per chiudere le due PR

**Al 6 settembre 2026.** L'audit contro `Manuali/DH_SRD_2_2026_08_25.pdf` è finito e scritto in
`docs/handoff/AUDIT-2026-09-04.md` (su questo ramo). Le riparazioni stanno in **due rami**, uno
pubblicato come PR verde e uno pubblicato come PR che era **in bozza** finché mancavano dodici correzioni
di prosa: chiuse il 6 settembre (§3), bozza tolta. Chi riprende parte dalla §0 e dalle decisioni in §7.

I commit si citano **per oggetto, mai per SHA**. `git log -1` prima di fidarsi di un hash scritto qui.

**UNIRE È IL DEPLOY** (`gh pr merge`). Il «vai» si dà **una volta per passo**.

---

## 0. Stato, misurato il 6 settembre

```
main                       «Merge pull request #76 …/consegna-2026-09-02»   (f722fe9, = origin/main)
leggibilita                14 commit, PR #77, CI pull_request success sul commit esatto (9c115ed)
riparazioni                37 commit (5 merge di corsia + 6 di sweep + la chiusura dei residui),
                           suite 205 file / 5006 test, tsc pulito — PR #78 pronta, non più in bozza (§3)
dataset                    data/srd-2.0.json ricostruito una volta (Spellbound Armor: stress null);
                           lo SHA del PDF combacia con il lock in tools/loadSrd.ts
```

I worktree: `/Users/simonealbanese/Documents/dh-wt/leggibilita` e `…/dh-wt/riparazioni`, con i tre
symlink (`node_modules`, `.tools`, `Manuali`). I cinque worktree `fix-E1…E5` sono stati rimossi; i
rami `fix-E*` esistono ancora e sono tutti dentro `riparazioni`.

## 1. Cosa ha trovato l'audit

- **152 rilievi** verificati (119 confermati riproducendoli, 33 già noti), **138 distinti** dopo
  l'unione dei doppioni: 4 S1, 31 S2, 73 S3, 30 S4. Zero refutati.
- **Il dataset è fedele al libro campo per campo** in ogni collezione. I difetti gravi erano nel
  motore (Help an Ally sommava i dadi; lo Stress non pagabile costava 1 HP per punto; gli ibridi
  Beastform costavano 1 Stress; Brawler's Strike non esisteva) e nelle schermate (uno scudo da Edit
  non risincronizzava l'Armatura; «Erase everything» annullato dal flush su `pagehide`).
- **Leggibilità**: l'85,9% dei caratteri visibili era sotto i 12px, il 71% in un ruolo a 11,5px,
  tutto in px. Misurato sul rig, 48 casi (`~/.claude/projects/…/audit-harness/audit-2026-09-04/lanes/R2/`).

Il rapporto intero, con ogni id, codice, pagina e fix proposto: `AUDIT-2026-09-04.md`. Le prove di
ogni rilievo (evidenza del cercatore + output eseguito dal verificatore) sono in
`~/.claude/projects/-Users-simonealbanese-Documents-Daggerheart-Companion/audit-harness/audit-2026-09-04/findings/<id>.json`.

## 2. Cosa è stato costruito

- **PR #77 `leggibilita`** — rampa in `rem`, `.t-read`/`.t-hint` al posto di `.t-dense`, inchiostri
  AA, altezze fisse → soglie, guardia `tests/ui/inlineSizes.test.ts`. Misura A/B contro main:
  caratteri sotto i 12px 85,9% → 9,5%, nodi sotto i 10px 332 → 0, testo vivo sotto AA 16 → 0, zero
  bersagli sotto i 44px. Verificata due volte sul rig (`ok: true` alla seconda), suite 197 / 4868.
- **PR bozza `riparazioni`** — i 30 rilievi S1/S2 in cinque corsie, ognuna con test di regressione e
  un verificatore che ha rieseguito la riproduzione con la riparazione applicata e tolta; poi lo
  sweep (prosa resa falsa dalle riparazioni, conteggi SRD 1 nei docblock, testi del lane R3, tre
  regole GM minori, il rapporto). Le cinque corsie si sono unite **senza conflitti**.

## 3. I dodici residui — CHIUSI il 6 settembre, con la loro coda

I dodici residui della §6 sono riparati, più i loro compagni per cifra. Sono serviti **tre giri di
corsie** (4 + 3 + 1 corsie, ognuna con un verificatore che ha riletto la pagina estratta o rieseguito
il conteggio, e ha refutato dove serviva): il primo ha chiuso i dodici e trovato ~30 compagni fuori
dai file delle corsie; il secondo ha chiuso quelli e trovato la famiglia dell'indice di ricerca
(849 → 1451 record, 780 → 1369, 69 → 82 sezioni, 2 142 → 2 382 interrogazioni, «34 of the 69» → 42
di 82, 869/255 righe → 961/296); il terzo l'ha chiusa con una lista finita di venti voci e il
divieto di allargarla. In tutto **62 file, prosa soltanto**: docblock, commenti JSX, titoli e
messaggi di test. Nessuna riga di codice e nessuna asserzione è cambiata (controllato con un diff
filtrato: le uniche righe non-commento sono blocchi `{/* */}` di Scene.tsx e un messaggio di
`expect`). Le misure in Chrome/ms prese sull'SRD 1 sono **marcate** come tali, non ri-derivate; i
conteggi sono ri-derivati con script sul dataset (i comandi sono nei journal delle ondate, §5).

Un tredicesimo residuo trovato leggendo Scene.tsx: i conteggi della riga «MOTIVES & TACTICS» erano
già 34/63/123 ma la conclusione diceva ancora «37 is one line, 58 and 92 are both two»; con 123
caratteri la riga più lunga è di **tre** righe (45px). Corretto in prosa (decisione del 6 set).

**Restano, riportati e NON riparati** (lista chiusa del terzo giro; vanno con gli S3 visibili):
- `shared/parsers/rules.ts` ~:356 e `tests/.../equipmentProse.test.ts` ~:31: «the 69 sections of
  SRD 1.0 and zero of the 74 of SRD 2.0 contain the word» — il 74 può essere una cifra di parse
  prima dell'isola dell'equipaggiamento (82 dopo); le due frasi condividono il numero e vanno
  mosse insieme, dopo aver capito quale conteggio descrivono.
- `tests/ui/domainCardView.test.ts` :190 e :236: soglie `26/27` nelle asserzioni (le 27 carte di
  livello 1 dell'SRD 1 come denominatore) — codice, non prosa; passano; da ri-derivare a parte.

Lo stato della PR: bozza tolta, CI da confermare sul commit esatto (§0).

## 4. Le regole macchina che hanno morso questa volta

- **Il limite di sessione dell'account uccide un'ondata a metà.** Il 3 settembre 300 agenti su 377
  sono morti sul limite (reset a mezzanotte); il 4 settembre altri quattro (reset alle 5); il 6 la
  corsia di riparazione sul **limite di Fable**. La ripresa funziona: `Workflow({scriptPath,
  resumeFromRunId})` rigioca dalla cache ogni `agent()` il cui `(prompt, opts)` è identico al byte.
  Tenere invariati i prompt delle corsie già consegnate; cambiare solo gli stadi non ancora eseguiti.
- **Il proprietario ha chiesto di spendere meno token.** La verifica magra che ha retto: i rilievi
  già `known` non si rieseguono; gli altri si verificano **a gruppi di cinque per corsia** (sforzo
  alto se c'è un S1/S2, medio altrimenti); alla sintesi si passano i campi troncati. Costo totale
  dell'audit più le riparazioni: circa 18 milioni di token di sotto-agenti.
- **Una correzione può essere sbagliata, e questa volta lo era lo sweep.** «La frase è a p53 anche
  nell'SRD 2» era il motivo con cui lo sweep ha *rifiutato* una correzione giusta (S8b-6 diceva
  p54). L'ha preso il verificatore leggendo la pagina. Un folio si scrive solo dopo aver letto il
  piè di pagina della pagina estratta.
- **Uno sweep di numeri lascia in piedi i vicini.** Sette dei dodici residui sono la stessa cifra
  vecchia a sei righe da quella corretta, nello stesso file. Il `grep` di compagnia va fatto per
  cifra, non per frase.
- Le altre regole restano: `. ./env.sh >/dev/null 2>&1 && node -v` deve dire v24; mai la porta
  5199; un rosso sotto carico non è un reperto; `mergeStateStatus: CLEAN` non è un controllo verde.

## 5. Dove sta tutto

- Pagina del rapporto (privata, la condivide il proprietario):
  https://claude.ai/code/artifact/a6512463-9dc1-480e-8890-fccec7db2011
- Dati stabili dell'audit: `~/.claude/projects/-Users-simonealbanese-Documents-Daggerheart-Companion/audit-harness/audit-2026-09-04/`
  (i 152 rilievi con i verdetti, i casi e lo script di misura del testo, il brief delle corsie, i
  corpi delle due PR, lo script dell'ondata magra).
- I journal delle ondate, con l'output eseguito di ogni agente:
  `~/.claude/projects/…/d2eabd75-61f0-4ab5-aee2-dc92217d1777/subagents/workflows/wf_406fbc3e-c66/`
  (audit), `wf_10474191-8be/` (leggibilità), `wf_bf865f13-b97/` (riparazioni), `wf_62cb0fde-dea/` (sweep).

## 6. I dodici residui, come li ha scritti il verificatore (CHIUSI il 6 set, vedi §3)

```
[1] FALSE FOLIO (sweep's own skip reason is wrong): S8b-6 said the card-exchange sentence is on p54, the sweep refused it claiming 'the exchange sentence and clear any marked traits are on p53 of SRD 2 too'. The extracted pages say otherwise: scratchpad/srd2/pages/p053.txt carries 'clear any marked traits' (twice) and no 'exchange'; p054.txt line 13 carries 'You can also exchange one domain card you've previously acquired for a different domain…' (footer 'Daggerheart SRD 54'). The section `leveling-up` has sourcePage 53 (it starts there), but the sentence itself is on p54. So four sites keep a wrong folio for that exact quotation: src/engine/levelUp.ts (two docblocks quoting '…different domain card of the same level or lower." Folio 53'), src/ui/build/LevelUp.tsx (the second, near the end of the file), shared/types.ts (the LevelUpPlan exchange field). The fifth site, LevelUp.tsx's 'clear any marked traits… Folio 53', is correct. The commit message of 971367b and AUDIT-2026-09-04.md §7 both repeat the false claim ('è a p53 anche nell'SRD 2').

[2] FALSE FOLIO written by the sweep: src/engine/levelUp.ts applyLevelUp docblock now says "folio 22's *\"whenever\"* covers that route too" (was folio 18). The sentence 'Whenever you gain a new Experience, your companion also gains one' is on p21 (p021.txt line 21, footer 'Daggerheart SRD 21'), and in the dataset it is in `ranger-companion` (sourcePage 21), not `leveling-up-your-companion` (22) — the latter does not contain 'whenever' at all. Should read folio 21.

[3] SURVIVOR in a sweep-edited comment: src/ui/gm/Scene.tsx CombatantCard fold comment — the sweep changed 'all 129 adversaries' to 264 in one sentence, and six lines below the same paragraph still says 's.dataset.adversaries is exactly the 129 - verified, and none of the 129 has an empty motives or an empty features'. Counted: 264, 0, 0.

[4] SURVIVOR in a sweep-edited file: src/ui/gm/Scene.tsx MINIONS-strip comment still says 'the longest string this can build over the shipped book is 118: a three-digit count and Burning Heart of the Woods, the longest of the 19 environment names' (and '57 characters at its shortest and 118 at its longest' a few lines up). SRD 2's longest environment name is 'Convergence, the City of Portals' (32 chars; 47 environments), so that string is 124, not 118 — the two-line conclusion may still hold (124 < 2x63) but the figures are SRD 1.0's and unmarked.

[5] SURVIVOR in a sweep-edited file: src/ui/player/Cards.tsx — 'two primed controls in a grid of 189 is worse than none' (armed-recall comment) and 'with nothing on screen saying so, and 7 of 9 domains with it' (the filter-fold comment the sweep edited one line below to 'how many of the 210 survived'). Counted: 210 cards, 10 domains (Dread has 21 cards).

[6] SURVIVOR in a sweep-edited file: src/ui/build/GearPicker.tsx header still says 'Neither draws anything on data/srd-1.0.json, which fences nothing: the 204 weapons, 34 armors and 120 items above carry no set and no module' while the line above it now reads '391 weapons, 85 armors and 240 items'. Counted: 391 / 85 / 240 (120 loot + 120 consumables).

[7] SURVIVOR in a sweep-edited file: src/ui/build/Wizard.tsx StepHeritage docblock — 'eighteen ancestry cards stood between the top of the page and the nine communities' is unmarked (the sweep marked the sibling sentence at the top of the file 'on SRD 1.0's lists' and rewrote the same sentence in creation.ts, but not this one). Counted: 24 ancestries, 15 communities.

[8] SURVIVOR in a sweep-edited file: src/ui/gm/ReferenceTables.tsx header — 'The ADD -> LINK -> Rule door offers all sixty-nine sections' is a live claim about the link picker; counted 82 rules sections. (src/ui/shared/moments.ts's 'reasoning for all sixty-nine rows, ratified… on 26 August' is historical and acceptable; RuleSearch.tsx/Search.tsx's sixty-nine are the search lane's, as the sweep says.)

[9] UNMARKED SRD 1.0 MEASUREMENTS the sweep neither re-derived nor marked (the rule it applied to Wizard/GearPicker/Cards/srdReference): src/ui/shared/DomainCardView.tsx 'Measured against the real SRD - 189 cards, median 275 characters, longest 655'; shared/campaigns.ts 'makeCombatant over the shipped dataset's 129 adversaries mints a body of 171 to 267 bytes of JSON, median 208'. Both describe the 'shipped' dataset and are SRD 1.0's.

[10] SURVIVORS IN TEST DOCBLOCKS (the sweep's re-count did not reach tests): tests/ui/gear.test.ts test title 'is the whole SRD: 204 weapons, 34 armors, 60 pieces of loot and 60 consumables' while its body asserts 391/85/120/120; tests/engine/matrix.test.ts header '3240 sheets: all eighteen subclasses crossed with all eighteen ancestries' while the code iterates dataset.subclasses x dataset.ancestries (26 x 24 x 10 = 6240 rows) and '3240' is repeated in four comments; tests/ui/gearRandomiser.test.tsx ('all 204 weapons and all 34 sets of armor… 60 loot entries or 60 consumables', '167 of the 204 weapons'); tests/ui/cards.test.tsx 'grid of 189 cards'; tests/ui/playSheet.test.tsx '31 SRD cards whose Recall Cost is 0' (33) and '38 adversaries and 9 environments call for a reaction roll' (97 / 26); tests/ui/sessionRule.test.tsx and tests/ui/screens.test.tsx 'Average Costs table, p.69' (p91); tests/gm/restControl.test.tsx 'downtime p.41' (p52); tests/engine/damage.test.ts 'a reading of p.71' (p93); 'folio 18' for the companion sheet in tests/ui/attack.test.ts, tests/ui/companion.test.tsx (x3), tests/ui/cardAllowance.test.ts, tests/engine/companion.test.ts, tests/ui/srdReference.test.ts (21/22 now).

[11] IMPRECISE (not strictly false, flagged for the owner): src/ui/gm/ReferenceTables.tsx 'two tables sixty pages apart - adversaries on 95, environments on 159' and src/ui/shared/srdReference.ts 'a different chapter sixty pages away' — the gap is 64 pages (the SRD 1.0 text said 'thirty' for 29).

[12] The docs/handoff/AUDIT-2026-09-04.md §7 text is otherwise accurate (ids, partition, counts), but it inherits the false p53 statement above in the sweep bullet ('la frase dello scambio di carte è a p53 anche nell'SRD 2 (S8b-6 diceva p54)') — S8b-6 was right.

```

## 7. Le decisioni del 6 settembre (non riproporle)

Prese dal proprietario in una sessione sola, tutte sulla strada consigliata:

1. **Ordine**: prima #77 `leggibilita`, poi #78 `riparazioni`. Le due PR **confliggono in nove
   file**, un blocco ciascuno tranne l'ultimo (`parts.tsx`, `AddSheet.tsx`, `Encounter.tsx`,
   `MenuSheet.tsx`, `Scene.tsx`, `Play.tsx`, `Recovery.tsx`, `encounterBump.test.tsx`, e
   `DomainCardView.tsx` con due blocchi nello stesso docblock, aperto dalla chiusura dei residui):
   la rampa cambia il ruolo tipografico o annota la rampa, le riparazioni le parole e i conteggi.
   Le nove risoluzioni sono registrate da `rerere` (`git config rerere.enabled true` nel repo,
   `.git/rr-cache`), provate due volte sul ramo usa-e-getta `composizione` (worktree
   `~/Documents/dh-wt/composizione`, HEAD «Rehearse the composition again…»): suite composta
   206 file / 5013 test verdi, tsc pulito. Dopo il merge di #77: `git merge origin/main` dentro
   `riparazioni` (le risoluzioni scattano da sole: attendersi «Resolved … using previous
   resolution» nove volte e `git diff --diff-filter=U` che elenca i file finché non si fa
   `git add`), push, CI, secondo «vai».
2. **Allineamento** con un commit di merge, non rebase.
3. **Scene.tsx**, riga MOTIVES & TACTICS: solo prosa, nessun troncamento.
4. **Prossima ondata** (dopo le due PR, su rami puliti da `main`): i quattro S2 visibili al tavolo —
   S2-2 Combo Die, S3-2 Beastform Evolved, S6c-1 ATK +2d4, S8a-4 danno diretto/resistenza — una
   corsia ciascuno con test di regressione e verificatore. Poi B2-2, B2-3, B7-1. S5-1 con gli S3.
5. **S3/S4 rimasti (78)**: solo quelli con testo visibile all'utente; i docblock S4 in backlog.
6. **Foglio di stampa (R1-7)**: corpo 9pt, meta 7,5, etichette e piè 7, verbi restano 5,6 — e
   misurare che stia in una pagina prima del merge.
7. **Selezione (R1-10)**: `user-select: text` sui contenitori di lettura; spento su Play e tracce.
8. **CI dataset**: validare in CI il JSON committato e lo SHA del PDF contro il lock; la
   rigenerazione resta locale.
9. **Pulizia**: cancellare `fix-E1…E5` dopo il merge di #78 e `composizione` dopo l'allineamento.

Le ondate del 6 settembre (con l'output eseguito di ogni agente):
`~/.claude/projects/…/5d2588f6-6f8a-403a-965a-4933cd76685e/subagents/workflows/wf_74ba74a0-015/`
(giro 1), `wf_5aa5940a-db4/` (giro 2), `wf_d236a1bf-00c/` (giro 3). La verità di base delle corsie
(`GROUND-TRUTH.md`) era nello scratchpad di sessione e non sopravvive: le pagine si rifanno con il
comando in §3 di questo documento, i conteggi con una riga di node sul dataset.
