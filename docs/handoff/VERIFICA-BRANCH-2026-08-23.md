# Verifica avversariale del branch `beast-sheets` — recuperata dal disco il 24 agosto 2026

> **Questo documento non esisteva.** `wf_226cd659-47e` è stato ucciso dalla chiusura improvvisa
> della sessione del 23 agosto mentre girava. Delle sue cinque fasi, la quinta — quella che doveva
> *scrivere il rapporto* — **non è mai partita**. Per ventiquattro ore i suoi risultati sono
> esistiti solo come valori di ritorno grezzi in un `journal.jsonl` dentro una cartella di
> sessione, cioè in un posto che nessuno garantisce. Questo file è quel rapporto, ricostruito.

**Cosa è sopravvissuto e cosa no**, contato e non stimato: 110 agenti partiti, **102 rientrati**.
Fase 1 (le sette correzioni dell'audit) completa, 7 su 7. Fase 2 (otto lenti di caccia) completa,
8 su 8, 36 candidati. Fase 3 (verifica avversariale a triadi) arrivata a **29 candidati su 36**
puliti; 8 agenti uccisi a metà lavoro, uno slot mai partito, 4 candidati mai entrati in coda.
Fasi 4 (Critic) e 5 (Synthesis) **mai partite**. Primo agente 19:37:57Z, ultima attività 21:11:28Z.

---

## Le cinque cose da sapere prima di leggere il resto

**1. Sei delle sette correzioni dell'audit `c88bd21` sono incomplete, e una è una regressione.**
Solo la correzione del crash del party board è stata giudicata completa. La #4 — quella dei glifi
di spunta nella stampa — ha **sovracorretto**: ha tolto i `☑`/`☐` e li ha sostituiti con
un'etichetta `Taken`, così le sette opzioni non marcate ora stampano **un nome e nessuna casella**,
sotto un'intestazione che continua a dire «1 marked · 4 earned». La primitiva giusta era già nel
file e inutilizzata: `.dhc-tick`, `sheet.css:468`, già usata due volte nello stesso foglio.

**2. Le due regole di punta del branch non sono trattenute da nessun test.** Provato eseguendo,
non leggendo: `setTrait('spellcast')` per il compagno e `setTrait(form.attack.trait)` per il
Beastform si possono **invertire entrambe con la suite intera verde** (3266 test). Quella del
compagno è la frase che il commit `19ddfa8` esiste per implementare, e la sua asserzione è nata
non discriminante e non ha mai discriminato in tutta la storia del branch. In totale **cinque test
dimostrati vacui** con mutazioni reali a suite piena.

**3. Il difetto più forte del run, confermato da 9 verificatori su 9.** Un riposo breve che pulisce
Stress **riporta in scena un compagno che ne era uscito** (`src/engine/rest.ts:215`). L'app rompe
la promessa che ha appena stampato sullo schermo: il banner dice «OUT OF THE SCENE · BACK AT YOUR
NEXT LONG REST» e il riposo breve lo fa sparire. E **il test del branch fissa l'esito sbagliato**
(`tests/engine/rest.test.ts:367`). Il lato scomodo: la regola di folio 18 sul clear condiviso non
porta eccezioni, quindi rifiutare la pulizia scambia una deviazione con un'altra — una correzione
fedele vuole probabilmente che lo stato «fuori scena» **smetta di essere derivato**, il che
falsifica il docblock di `companion.ts:133` e tocca il codec.

**4. Il difetto vivo più visibile per chi gioca.** Cambiare forma mentre l'attacco del Beastform è
armato **lascia il tratto della forma precedente sul tiro** (`src/ui/player/Play.tsx:246`) — e non
è solo l'etichetta: il modificatore è sbagliato, dimostrato separando di proposito i tratti base.
È più largo di come era stato riportato: `declared` sopravvive al DROP, quindi ci si arriva anche
dal drop automatico all'ultimo Hit Point che questo stesso branch ha introdotto.

**5. Il bump di schema si è fermato prima del suo stesso precedente.** `Architecture.md:461-462`
dice ancora «*un elenco che deve restare di tre voci esatte*» mentre `codec.ts:34` adesso ne
dichiara **quattro** — e quella frase era stata scritta dal bump precedente (`18265c5`) proprio per
costringere questo aggiornamento. Questo branch **non ha toccato nessun `.md`**. In più resta un
crash vivo della stessa classe già corretta una volta: `attack.ts:373` passa
`companion.damageType` grezzo e `:578` gli chiama `.toUpperCase()`.

---

## Come leggere questo file

Il corpo qui sotto è **inglese verbatim**, come i valori di ritorno degli agenti — stesso idioma di
`PROGETTO-GM-2026-08-23.md` e `BACKLOG-PLAN-2026-08-23.md`. Non è stato riassunto: un riassunto di
una prova per mutazione non è una prova.

**Le prove per mutazione non sono state eseguite.** Erano il punto del disegno in sola lettura
(«*Mutation-proofs are run later, sequentially, by the orchestrator*») e l'orchestratore non ci è
mai arrivato. La §6 le elenca con il colore atteso. **Vanno eseguite una alla volta, mai in
parallelo**, perché mutare i sorgenti in parallelo nello stesso worktree si distrugge da sé.

**Un avvertimento operativo dal run stesso:** `scratchpad/mut/` veniva ripulito a metà corsa da un
altro agente che condivideva la cartella, quindi ogni risultato di mutazione preso da quel percorso
condiviso è inaffidabile. L'agente che se ne è accorto ha rifatto tutto in una directory dal nome
unico, verificando il mutante presente prima e dopo. Fai lo stesso.

**E un rilievo sul metodo del run**, che vale per come si leggono i suoi verdetti: i candidati non
erano deduplicati e il cancello era un nudo 2-su-3, quindi **difetti identici hanno ricevuto
verdetti opposti a seconda di come erano inquadrati** (candidato 4 KILLED contro candidato 10
SURVIVED; candidato 5 KILLED contro candidato 7 SURVIVED). Quei due stanno nella §3 come contesi,
non come chiusi.

---

# THE VERIFICATION REPORT — `wf_226cd659-47e`, recovered 24 August 2026

*Reconstructed from `journal.jsonl` (212 lines, 110 agent transcripts) after the run was killed mid-flight. The Synthesis phase that was supposed to write this file never started — this is that file, assembled from the raw per-agent return values.*


## 0. What the run was, and what died with it

NO. The script (/Users/simonealbanese/.claude/projects/-Users-simonealbanese-Documents-Daggerheart-Companion/b9563823-72a2-4816-be80-b8e823d1a7e4/workflows/scripts/verify-beast-sheets-audit-wf_226cd659-47e.js) declares five phases: Fixes, Hunt, Verify, Critic, Synthesis. Evidence from journal.jsonl (212 lines: 110 'started', 102 'result') plus the 110 agent-*.jsonl transcripts: phase 1 Fixes ran all 7 lanes and all 7 returned; phase 2 Hunt ran all 8 lenses and all 8 returned (36 candidate findings); phase 3 Verify got through 29 of 36 candidates cleanly, then died mid-flight — 95 verify agents started, 87 returned, 8 were killed while still working, and one lens slot (CONTEXT on candidate 32) never started at all. Phase 4 (Critic / label 'completeness') and phase 5 (Synthesis / label 'report') NEVER STARTED — no agent transcript exists with either prompt, and no run-level result file exists in the run directory (it contains only journal.jsonl and the 110 agent files). First agent start 2026-08-23T19:37:57Z, last agent activity 2026-08-23T21:11:28Z; the eight orphans all stop between 21:08:43Z and 21:11:28Z mid-tool-use with last assistant lines like "I'll verify this claim by reproducing it." — i.e. the session was terminated, the run did not complete. Consequently the single ranked report the script was written to produce was never generated; the fix verdicts, hunt findings and 87 adversarial verdicts exist only as raw per-agent return values in journal.jsonl.


### Lanes, and what each returned

- Phase 1 Fixes — fix:party-board-crash (agent a7ead75a5e2c577f7): try to refute the audit's claim that the GM party board crashed on pre-schema-5 companions via .toUpperCase() on undefined damageType, check the fix is complete across all PartyMember.sheet reads. RETURNED.
- Phase 1 Fixes — fix:false-docblocks (agent a1e45fe03a5bca223): verify the two rewritten docblocks are now TRUE and sweep the branch's new comments for other false factual claims. RETURNED.
- Phase 1 Fixes — fix:drop-edge (agent a80a71a00b30db464): verify the new dropFormOnLastHitPoint guard for every HP-marking route, and whether store `update` really is the single funnel. RETURNED.
- Phase 1 Fixes — fix:print-idiom (agent a2587f0b741566e75): render the printed sheet and check the ballot-glyph fix, sheet rules compliance, and whether EXPERIENCE_LINES is right for a companion. RETURNED.
- Phase 1 Fixes — fix:stable-empty-array (agent a7722958f94e2e8dd): verify the NO_EXPERIENCES referential-stability argument, look for other fresh-array-into-memo instances, check all three experiencesFor branches. RETURNED.
- Phase 1 Fixes — fix:weak-assertion (agent a25c13da87f068be4): verify the strengthened party-board assertion and sweep ALL branch tests for assertions that pass with the feature broken ("highest-value part of your task"). RETURNED.
- Phase 1 Fixes — fix:codec-decision (agent ae5e4654a43f9ef6a): work the version-nibble bit flips yourself for formats 2/3/4, check the loss is documented+asserted both sides, check the SCHEMA 4->5 bump is complete, check the v5 fixtures exercise the new field. RETURNED.
- Phase 2 Hunt — hunt:rules-fidelity (agent a062f5bfe6bfc603a): read folios 12 and 18-19 and check every rule implemented and not implemented. RETURNED, 5 findings.
- Phase 2 Hunt — hunt:persistence (agent afc01a92dd54655ce): trace every path a Character enters/leaves the app with schema-4 vs schema-5 records. RETURNED, 3 findings.
- Phase 2 Hunt — hunt:engine-edges (agent acd2dfd07143d314e): attack the boundaries of every new/changed src/engine function and the ADDITIONAL_OPTIONS regex. RETURNED, 5 findings.
- Phase 2 Hunt — hunt:react-lifecycle (agent a04aeb6b5af0bc2a1): hook order, effects, stale closures, state surviving a character switch, armed companion outliving its panel. RETURNED, 4 findings.
- Phase 2 Hunt — hunt:test-quality (agent ab8185d8f7981ec4a): for every new test, name the src/ mutation that leaves it green. RETURNED, 5 findings.
- Phase 2 Hunt — hunt:comments-lie (agent a32c137217185c34c): check every comment added on the branch and all 13 commit messages for false factual claims. RETURNED, 7 findings.
- Phase 2 Hunt — hunt:parser-dataset (agent a1f70cf49b8ff5ba0): diff data/srd-1.0.json main vs branch, rebuild it, check the five new sections against the PDF, check parser conflicts. RETURNED, 1 finding.
- Phase 2 Hunt — hunt:ui-rules (agent a5ec4a4f18b77fc33): 44px tap floor, colour-never-sole-signal, refusal-in-text, live regions, phone budget. RETURNED, 6 findings.
- Phase 3 Verify — 32 of the 36 candidates were dispatched to a CORRECTNESS/REPRODUCTION/CONTEXT triad each (95 agents; candidate 32 got only 2 of its 3 lenses). 29 candidates got all three verdicts back; candidates 30, 31 and 32 returned nothing.
- Phase 3 Verify — candidate 1 src/engine/character.ts:395 'Light in the Dark' Hope slot: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 2 src/engine/rest.ts:215 short rest returns away companion (rules-fidelity wording): 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 3 src/engine/companion.ts:125 allowance credits pre-companion level-ups: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 4 src/ui/player/Companion.tsx:520 EARNED exceeds the eight boxes: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 5 src/transfer/codec.ts:655 QR drops damageType, four surfaces state PHY: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 6 src/transfer/fileIo.ts:279 partial companion accepted then kills the GM board: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 7 src/ui/gm/PartyBoard.tsx:546 scanned companion always reads PHY: 3/3 verdicts, SURVIVED 2-of-3.
- Phase 3 Verify — candidate 8 src/ui/print/sheetModel.ts:284 printed sheet under-counts marked upgrades: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 9 src/engine/rest.ts:215 same short-rest defect (engine-edges wording): 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 10 src/engine/companion.ts:125 allowance 12 on a sheet with 8: 3/3 verdicts, SURVIVED 2-of-3.
- Phase 3 Verify — candidate 11 src/engine/companion.ts:125 multiclass allowance (engine-edges wording): 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 12 src/engine/companion.ts:146 companionIsAway level-triggered on a shrinking STRESS SLOTS stepper: 3/3 verdicts, KILLED 0-of-3.
- Phase 3 Verify — candidate 13 src/engine/companion.ts:116 ADDITIONAL_OPTIONS regex gaps: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 14 src/ui/player/Play.tsx:246 swapping the worn Beastform leaves the old trait on the roll: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 15 src/ui/player/Play.tsx:3737 phone Experiences fold gated on the wrong list: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 16 src/ui/player/Vitals.tsx:79 armed companion outlives its panel: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 17 src/ui/player/Companion.tsx:300 away companion cannot be disarmed and returns armed: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 18 tests/ui/companion.test.tsx:327 the SPELLCAST assertion is satisfied before arming: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 19 tests/ui/beastform.test.tsx:208 armed-trait assertion matches the default AGI chip: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 20 tests/gm/partyBoard.test.tsx:252 companion EVASION asserted against the PC's figure: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 21 tests/ui/companion.test.tsx:358 DualityRoll's experiencesFor caller uncovered: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 22 tests/ui/companion.test.tsx:535 training bonus pinned only in the engine: 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 23 src/engine/companion.ts:133 companionIsAway docblock's 'no second way' is false: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 24 src/ui/player/attack.ts:351 'Folio 19' citation for folio-18 text: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 25 src/ui/shared/srdReference.ts:648 'seventy bullet paragraphs' went stale: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 26 src/ui/player/Play.tsx:501 'SPELLCAST TRAIT on every subclass page': 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 27 src/ui/gm/PartyBoard.tsx:514 'reads by comparison and never by method call': 3/3 verdicts, KILLED 1-of-3.
- Phase 3 Verify — candidate 28 src/ui/player/Companion.tsx:190 arming prop comment still names non-existent surfaces: 3/3 verdicts, SURVIVED 3-of-3.
- Phase 3 Verify — candidate 29 shared/migrations.ts:75 MIGRATIONS header contradicts the 3->4 note: 3/3 verdicts, KILLED 0-of-3.
- Phase 3 Verify — candidate 30 shared/parsers/rules.ts:766 invented bullet list under STEP 3: all three agents (a7daa8f4bba935a9a, a790b26bbb0e98706, ad8ff4615fcfabb7e) STARTED AND NEVER RETURNED.
- Phase 3 Verify — candidate 31 src/ui/player/Play.tsx:3697 'Weapons & armour' fold header claims ARMED · <companion>: all three agents (a3df7ba6b80cb05c8, af4d4e094cb759de9, ae802d6ad894781ef) STARTED AND NEVER RETURNED.
- Phase 3 Verify — candidate 32 src/ui/player/Companion.tsx:322 armed companion invisible after WhoSwitch returns to YOU: CORRECTNESS (ae1a9b37a891b718d) and REPRODUCTION (a8bdb518413885b52) STARTED AND NEVER RETURNED; CONTEXT never started.
- Phase 3 Verify — four ui-rules candidates were never dispatched at all (the pipeline died before reaching them): the phone Experiences fold duplicate, the 'NO EXPERIENCES YET vs two UNNAMED chips' contradiction, the party-board ellipsis/colour-only out-of-scene signal, and the non-live-region out-of-scene banner.
- Phase 4 Critic — label 'completeness', asked 'what has still not been looked at?' and to go check the two or three most likely gaps itself: NEVER STARTED.
- Phase 5 Synthesis — label 'report', asked for one ranked list plus a browser-only list plus a what-is-clean list: NEVER STARTED. This is why nothing was ever written down.

### What never returned

- Verify triad for candidate 30 (shared/parsers/rules.ts:766, invented bullet list under STEP 3) — CORRECTNESS agent a7daa8f4bba935a9a, REPRODUCTION agent a790b26bbb0e98706, CONTEXT agent ad8ff4615fcfabb7e: all three started 21:00-21:01Z and were killed at 21:08:59Z / 21:11:02Z / 21:08:44Z mid-tool-use with no result.
- Verify triad for candidate 31 (src/ui/player/Play.tsx:3697, 'Weapons & armour' fold header claims ARMED · <companion>) — CORRECTNESS agent a3df7ba6b80cb05c8, REPRODUCTION agent af4d4e094cb759de9, CONTEXT agent ae802d6ad894781ef: all three started 21:01-21:03Z and were killed at 21:08:43Z / 21:09:13Z / 21:09:11Z with no result.
- Verify pair for candidate 32 (src/ui/player/Companion.tsx:322, armed companion invisible after the WhoSwitch returns to YOU) — CORRECTNESS agent ae1a9b37a891b718d killed at 21:11:28Z, REPRODUCTION agent a8bdb518413885b52 killed at 21:09:01Z. The CONTEXT lens for this candidate was NEVER STARTED — the only agent slot in the entire run that was never dispatched.
- Four hunt:ui-rules candidates never entered the Verify pipeline at all: the phone Experiences fold duplicate, the 'NO EXPERIENCES YET while the roll offers two UNNAMED chips' contradiction (src/ui/player/Companion.tsx:370), the party-board out-of-scene colour+ellipsis signal (src/ui/gm/PartyBoard.tsx:535), and the non-live-region out-of-scene banner (src/ui/player/Companion.tsx:262).
- Phase 4 'Critic' (label 'completeness', effort high): asked to name the files, behaviours, user journeys, data states and claims that no lens examined, then to go check the two or three most likely itself. NEVER STARTED — no transcript exists.
- Phase 5 'Synthesis' (label 'report', effort high): asked to produce a verdict on the audit itself, one ranked list of everything still wrong with file:line and the exact proving mutation, a separate list of browser-only claims, and a what-is-clean list. NEVER STARTED — no transcript exists, and no run-level result was ever written. This is precisely why nothing from this run reached the repo.

## 1. The seven audit fixes, re-checked — six are incomplete and one is a regression

### 1. FIX 1 re-check — party-board crash: real, fix correct AND complete, but the fix's own docblock is a new lie

**CONFIRMED (fixIsCorrect true, fixIsComplete true, regressionRisk true)**

Agent a7ead75a5e2c577f7. Proved empirically that readPartyMember (shared/campaigns.ts:617-635) does `sheet as unknown as PartyMember['sheet']` with no migration — fed readCampaignRecord a party row with `companion: {}` and it came back verbatim with zero warnings. So the pre-fix `{companion.damageType.toUpperCase()}` really was `undefined.toUpperCase()` for every board saved by shipped 0.5.0 with a Beastbound Ranger. The fix at src/ui/gm/PartyBoard.tsx:546 removes the method call and its 'phy' fallback matches both the migration seed and schema-4 behaviour. Completeness: v4->v5 added exactly one field, inside `companion`; `consecutiveShortRests` (v3->v4) is read only by Rest.tsx on the active character; an exhaustive grep of `\.sheet` across src/ gives only Names.tsx:163, gmStore.ts:1093 and PartyBoard's own Row. Print path is Settings-only and does migrate. NEW PROBLEMS: (a) PartyBoard.tsx:503 shouts 'NOTHING HERE MAY ASSUME A FIELD IS PRESENT' and the body then assumes four things — line 527 guards `companion === null` not `== null`, line 541 calls `companion.name.toUpperCase()`, line 546 reads `companion.stress.marked/.max`, and companionDamage->parseDamage calls `spec.replace()` on `companion.damage`; all four throw on objects readCampaignRecord accepts. (b) PartyBoard.tsx:545 `String(companion.range).toUpperCase()` converts a crash into a silent lie — it prints the literal word UNDEFINED to a GM. (c) The regression test uses `importParty(...)` and so bypasses readPartyMember/readCampaignRecord entirely — nothing in the suite proves the claim the finding rests on. One correction to the audit: ScreenBoundary exists, so the throw hit a boundary fallback rather than white-screening the app; and deriveStats(sheet,...) at PartyBoard.tsx:358 already throws first on a degenerate sheet, so CompanionLine is not the board's real tolerance ceiling.

### 2. FIX 2 re-check — false docblocks: corrected statement is TRUE, but the sweep is incomplete and the fix commit added three NEW false statements

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk true)**

Agent a1e45fe03a5bca223. Verified every mount site: `<Vitals` only at Play.tsx:2886 and Play.tsx:3577 (both pass `arming`); `<CompanionPanel` only at Vitals.tsx:124; neither PartyBoard.tsx nor print/CharacterSheet.tsx nor usePrintSheet.tsx imports either. So the rewritten notes are true. NEW LIES FROM COMMIT c88bd21: (1) PartyBoard.tsx:514 'this reads by comparison and never by method call, the way CompanionPanel already does' — both halves false (line 543 `companion.name.toUpperCase()` unguarded; line 546 `String(companion.range).toUpperCase()`; CompanionPanel calls .toUpperCase() on name at Companion.tsx:109, description at :226, range at :309). (2) print/CharacterSheet.tsx:417 says the Features section is 'two sections down' — it is ONE down (companion section opens at 364, Features at 432, nothing between); the same off-by-one is repeated in the new comment in tests/ui/printSheet.test.ts. (3) CharacterSheet.tsx:419 says the stylesheet 'opens by saying it draws outlines and never fills' — sheet.css says 'hairlines instead of filled panels, outlines instead of solid pips' and the page deliberately does fill (PrintDomainMark in marks.tsx:42 is documented as filled; CoinRow at CharacterSheet.tsx:253-261); repeated in printSheet.test.ts and in the commit message. MISSED BY THE SWEEP: (4) src/engine/levelUp.ts:494 'A tier achievement is the only place a character gains a *new* Experience' — false, ExperienceEditor has an 'Add an Experience' button (src/ui/build/parts.tsx:585-596, reachable from Edit.tsx:180 and Wizard.tsx:1605); it has a behavioural tail — folio 18's 'whenever you gain a new Experience, your companion also gains one' is wired ONLY to the tier achievement, so a hand-added Experience gives the companion nothing. (5) src/ui/print/sheetModel.ts:202 claims the companion's experienceLines is 'the same number as the character's' — line 283 is Math.max(EXPERIENCE_LINES, companion.experiences.length) and line 437 Math.max(EXPERIENCE_LINES, character.experiences.length), which diverge above 5; the new assertion `toBe(sheet.experienceLines)` passes only because the fixture sits under the floor. (6) src/engine/companion.ts:14 calls COMPANION_START 'four numbers' — it has seven fields. Claims checked and found TRUE (do not re-spend): the 80/42/38/7/3 census, 'four of the eighteen subclasses carry no Spellcast trait', the NO_EXPERIENCES docblock, the codec's 'the format number this would need is 4', readPartyMember's own docblock, the Play.tsx 'Unreachable' NameRefusal note.

### 3. FIX 3 re-check — drop-edge guard: correct for every route, but `update` is NOT the single writer and the log order is inverted

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk false)**

Agent a80a71a00b30db464. Checked every engine writer of hp.marked: markDamage (damage.ts:246) Math.min up-only; markStress (damage.ts:259-286) increments only while hp<max; Vitals pips (Vitals.tsx:328-332) write raw v, fire only at v===max; rest.ts:205,231 and death.ts:121-127 decrease only; levelUp.ts:431 raises max without marking; syncCounters (character.ts:549) is Math.min down-only — the false trigger the fix closed. Import/normalisation runs outside `update`. So `after.hp.marked > before.hp.marked && after.hp.marked >= after.hp.max` is right for every real route and the form cannot fail to drop where SRD folio 12 says it should. The one input where the new guard fires and the old did not (before {5,5} -> after {6,6}) is unreachable. NEW PROBLEMS: (a) CAUSALITY INVERSION IN THE LOG on the commonest route — state.ts:466-479 pushes 'Dropped out of Beastform / Last Hit Point marked' from inside update(), BEFORE the caller logs the cause; pushLog prepends (state.ts:665-673) and RecentLog renders newest-first (DualityRoll.tsx:2394-2429), so Vitals.tsx:628-637 produces 'Severe · 4 HP' on top and 'Dropped out of Beastform' below it — the drop filed as older than the damage. Same in Beastform.tsx:371-383 and recall.ts:46-48. No test pins log order (beastform.test.tsx:338 uses .find()). (b) `characters` state is written in SIX places in state.ts — :392 boot, :428 create, :468 update, :506 remove, :636 importCharacters, :659 resolveImport — five of which bypass dropFormOnLastHitPoint; safe today only because none can raise hp.marked, and nothing pins that invariant. (c) The false-trigger route is pinned only at engine level with a hand-built `after` object (beastform.test.ts:288-297); nothing exercises the real route, normalizeActive() at state.ts:859-862. (d) The guard omits hasFallen's `hp.max > 0` floor (damage.ts:294), so two predicates answering the same question now disagree in shape.

### 4. FIX 4 re-check — print idiom: the ballot-glyph fix OVER-CORRECTED and deleted the checkbox the SRD requires

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk TRUE — this is the most serious problem the fix lanes found)**

Agent a2587f0b741566e75, who actually rendered the component twice with npx vite-node under Node 24 and read the HTML. Codepoint scan of the whole rendered sheet returns only ['—','’'] — no U+2610/U+2611 anywhere; Stress renders as fill="none" stroke=currentColor outlined polygons at 12.4mm x 3.4mm; experienceLines 5 for both companion and character. So both defects the audit named are demonstrably gone, and the SRD does back the rule (folio 18: 'Whenever you gain a new Experience, your companion also gains one'; tierAchievementFor sets newExperience at levels 2/5/8; 2+3=5). BUT: (a) REGRESSION — CharacterSheet.tsx:414-424 replaced per-option `☑`/`☐` with `{u.marked && <span className="dhc-meta">Taken</span>}`, so the 7 unmarked options now print as a bare name with NO BOX AT ALL, on a section whose own heading prints 'Level-up options · 1 marked · 4 earned'. SRD folio 18 says 'mark it on your sheet'; sheet.css opens 'a printed sheet is played with a pencil, so it owes the player *room*, not a snapshot'. (b) The right primitive was already in the file and unused: sheet.css:468 `.dhc-tick` ('An ordinary checkbox. A border prints where a background does not', 2.4mm, no fill, no font dependency), already used at CharacterSheet.tsx:99 and :303. (c) The justification is a category error — the Features block's dhc-meta is `{f.source}`, a constant provenance label, not a state marker. (d) EXPERIENCE_LINES is right for a companion only BY COINCIDENCE: sheetModel.ts:237 builds it from STARTING_EXPERIENCES (a literal 2 whose docblock is entirely about the character), not from COMPANION_START.experiences; the new docblock at sheetModel.ts:200-207 claims a coupling that does not exist. MUTATION PROOF: set COMPANION_START.experiences to 3 — newCompanion makes three, the sheet still rules 5 lines, and nothing in tests/ui/printSheet.test.ts turns red. (e) Every fresh Beastbound Ranger prints two orphan '+2's with nowhere to write the name: newCompanion and the new applyLevelUp grant both write name:'', and the section emits `<li><span></span><span class="dhc-block-name">+2</span></li>` with no dhc-write class, therefore no dhc-rule and none of the 5.4mm floor sheet.css:430 exists for. Sheet rules otherwise compliant: break-inside avoid, hairlines untouched, mm sizing, no background fills.

### 5. FIX 5 re-check — stable empty array: correct, but the reasoning is inflated, nothing pins it, and it created a shared mutable singleton

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk true)**

Agent a7722958f94e2e8dd. (a) The referential-stability argument is literally true and materially overstated: `experiences` (DualityRoll.tsx:761) has exactly one identity consumer, the armedList useMemo at :765-768; there is no React.memo anywhere in DualityRoll.tsx or Play.tsx; armedList's identity is consumed only by .reduce (773), .length (782) and .map into the unmemoised armedMods literal (918/1025). So the cost is one filter over a zero-length array per render — invisible. Worse, BOTH branches the constant guards are unreachable inside DualityRoll: Play.tsx:309 returns null before rendering when character is null, and companionSource returns null when companion is null (attack.ts:378) while CompanionState.experiences is required (shared/types.ts:404). The case a reader would assume it is for — zero Experiences — goes through branch 3. So the fix commit's own new comment describes a path the running app does not reach: the same defect class the commit fixes elsewhere. (b) No second instance on the branch — every dep array in src/ was checked; companionUpgrades is correctly wrapped in useCompanionUpgrades memoised on a stable zustand slice. (c) All three branches are stable. NEW HAZARD: NO_EXPERIENCES is now exported from attack.ts:406 and handed to both DualityRoll.tsx:761 and Play.tsx:3457 typed `Experience[]` — mutable; any caller that sorts or pushes in place poisons every other caller globally. The codebase's own convention is readonly (main's COMPANION_UPGRADES was `readonly CompanionUpgrade[]`). Fix: `Object.freeze([])` plus a readonly return type. UNPINNED: tests/ui/attack.test.ts:531 asserts `toEqual([])`, which passes for a fresh literal. MUTATION PROOF PREDICTED: revert attack.ts:410/412 to `[]` and the full suite stays green — the regression can walk straight back in. PRIORITY MISJUDGEMENT flagged: useStats (state.ts:773-779) returns unmemoised deriveStats — a fresh object every render — and is a dependency of Play's `source` memo (Play.tsx:198) and DualityRoll's `modifier` memo (:787), so both recompute unconditionally. Pre-existing on main, but it makes the fixed item look inverted.

### 6. FIX 6 re-check — weak assertion: the strengthened assertion reproduces the defect it replaced, and FIVE more tests were proven vacuous by full-suite mutation runs

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk true) — the single highest-value lane in the run**

Agent a25c13da87f068be4, who rsync'd the branch to the scratchpad and ran REAL mutations (baseline 133 files / 3266 passed / 7 skipped). (a) The new assertion at tests/gm/partyBoard.test.tsx:241-244 replaces `not.toContain('EVASION 1')` with `not.toMatch(/STRESS \d+\/\d+/)` and justifies it with 'The companion line is the only thing on this board that writes STRESS as n/n' — FALSE: the probed board reads `...HP2/5STRESS3/6HOPE4/5ARMOR1/3...`, and `Pill` (PartyBoard.tsx:589-616) writes STRESS as n/n too; only the missing space keeps the regex off it — verbatim the flaw the audit condemned. Credit where due: the old assertion was value-dependent (would have missed Evasion 9) and reverting PartyBoard.tsx:546 does turn both new legacy tests red, so the crash fix IS pinned. (b) FIVE VACUOUS TESTS, EACH PROVEN BY A FULL-SUITE RUN THAT STAYED GREEN: (1) tests/ui/beastform.test.tsx:208-211 — replace the beastform branch of `arm` (Play.tsx:246-249) with `setTrait('knowledge')`: FULL SUITE 3266 passed, 0 failed. Arming a bear moves the chip to Knowledge and nothing notices. (2) tests/ui/companion.test.tsx:327 — Play.tsx:260-263 `setTrait('spellcast')` -> `setTrait('agility')`: FULL SUITE 3266 passed. This is the one sentence commit 19ddfa8 exists to implement. (3) attack.ts:386/410/412 — replace both NO_EXPERIENCES returns with `[]` (i.e. the audit's own fix #5): FULL SUITE 3266 passed. (4) tests/ui/companion.test.tsx:159-163 ('counts the boxes against the dataset, not against a literal 8') — Companion.tsx:520 `{upgrades.length}` -> `8`: 24/24 green. (5) tests/gm/partyBoard.test.tsx — invert PartyBoard.tsx:546 to `damageType === undefined ? 'PHY' : 'MAG'`: 14/14 green; nothing asserts a phy companion shows PHY. Plus, lower confidence: tests/engine/levelUp.test.ts:872-876 asserts only name==='' and bonus===2, both already true of newCompanion's blanks — pushing a duplicate of experiences[0] (duplicate id, duplicate React keys, delete-by-id removing two rows) stays green; and printSheet.test.ts:559-560 `html.slice(html.indexOf('>Bonded<'))` passes vacuously on one character if the marker text ever changes. (c) The new docblock at PartyBoard.tsx:502-517 is itself false: rendering a row with each field deleted in turn gives TypeErrors on name, stress and damage. (d) Root cause untouched: readPartyMember (shared/campaigns.ts:617-634) still casts without the migration chain, though it already has a warn channel and migrateCharacterRecord is importable.

### 7. FIX 7 re-check — codec decision: the version-nibble reasoning is CORRECT and unbreakable, but the schema bump stopped short of its own precedent and left a live crash

**CONFIRMED (fixIsCorrect true, fixIsComplete FALSE, regressionRisk true)**

Agent ae5e4654a43f9ef6a, who worked the bit flips by hand rather than trusting the comment. VERSION_MASK=0x0f, READABLE_CODEC_VERSIONS=[1,2]. From 2 (0b0010) the four nibble flips give 3,0,6,10; from 1 (0b0001) they give 0,3,5,9. Hamming(1,2)=2. Adding 3 (0b0011) puts it at distance 1 from BOTH 1 and 2, and a format-3 payload demoted to format 1 is parsed with NO in-payload checksum (`if (version >= 2)` guards the CRC, BODY_AT[1]=1) — the 30.9%-silent-corruption regime format 2 was created to close. 4 (0b0100) is distance 2 from both and its own flips give 5,6,0,12 — none readable. So 'the format number this would need is 4' is exactly right, and the property is genuinely pinned at tests/adversarial.test.ts:253. WHAT IS MISSING: (a) Architecture.md:461-462 still reads «Tre perdite … un elenco che deve restare di tre voci esatte» while src/transfer/codec.ts:34 now says 'FOUR DELIBERATE LOSSES' — that sentence was written by the previous bump 18265c5 precisely to force this update, and this branch changed NO .md file at all. Architecture.md §6.1 and HANDOFF.md:65-66 ('SCHEMA_VERSION is 4, with exactly one converter') are stale the same way. (b) tools/sampleCharacters.ts was not extended, so the 3240-sheet matrix is blind to damageType — every generated sheet is 'phy', which is also what readCompanion invents and what the migration seeds, so the matrix cannot tell 'carried exactly' from 'dropped and defaulted'. The previous bump handled exactly this for consecutiveShortRests. (c) tests/transfer/fixtures.ts::normalizeHandles got no damageType counterpart, so varying the generator would redden matrix.test.ts:218 for a deliberate-loss reason with nothing explaining it. (d) The v5 fixture's `damageType: "mag"` is INERT — the only reader is the generic loop in tests/store/migrations.test.ts:103-124, which checks name/level/loadout/experiences/hp and never touches companion; deleting the key from both v5 fixtures leaves the suite green. (e) NEW LIVE CRASH, same class the branch already fixed once: src/ui/player/attack.ts:373 passes companion.damageType through raw and line 578 calls damageTypeOf(...).toUpperCase() on it; readCharacterRecord spreads the file's companion wholesale, checkShapes only asserts object-ness, and the from:4 converter fills a MISSING damageType but never coerces a bad one — so a hand-edited .dhchar with `companion.damageType: 42` imports silently and throws on the first companion damage roll. Fifty lines earlier the same file normalizes the MORE trustworthy value (`sourceFromWeapon` writes `=== 'mag' ? 'mag' : 'phy'`); the trust levels are backwards. (f) The codec header's 'no single-bit flip of the version nibble can land on another readable format' overstates the guard — the test only flips payloads this build writes (format 2); versions 5 and 9 are one bit from format 1, which is real inbound traffic. (g) The stated cost of format 4 ('a phone that has not updated would stop being able to receive ANY sheet') is only true of an encoder that writes 4 unconditionally; a conditional encoder is never examined. (h) Minor: the v5 fixture is a bard with a hand-added companion, which no real build could export (hasCompanionFeature requires beastbound), so it is not this build's own output the way v4.dhchar was.


## 2. Defects that SURVIVED adversarial verification

### 8. SURVIVED (9 of 9 verifier votes) — a short rest's Clear Stress drags an out-of-scene companion back into the scene

**CONFIRMED — the run's strongest finding, raised independently by three hunt lenses and confirmed by all nine adversarial verifiers**

src/engine/rest.ts:215 (also framed at src/engine/companion.ts:133 and :146). Raised as candidate 2 (rules-fidelity), 9 (engine-edges) and 23 (comments-lie); every one of the nine verifiers returned refuted:false. Mechanism: `clear-stress` is declared a SHORT-rest move (rest.ts:44/50); its case at rest.ts:209-216 calls `alsoTheCompanion(next, cleared, log)` -> `clearCompanionStress` with NO away-guard, while the long-rest return at rest.ts:177 IS gated on `rest === 'long'`. companionIsAway (companion.ts:146) is purely derived (`stress.max > 0 && stress.marked >= stress.max`), so any clear at all ends the away state. Reproduced by at least five separate agents under project-local Node 24: companion 3/3 (away true) -> short rest with clear-stress -> log ['Clear Stress: cleared 5 Stress (d4 3 + tier 2)', 'Ashfoot: cleared 3 Stress alongside you'], companion stress {marked:0,max:3}, away false, attack pool offered again, consecutiveShortRests 1. SRD folio 18: 'When they mark their last Stress, they drop out of the scene… They remain unavailable until the start of your next long rest, where they return with 1 Stress cleared.' Consequences across all three consumers: attack.ts:366 (companionSource returns non-null, attack armable again), Companion.tsx:208/272 (the banner literally reading 'OUT OF THE SCENE · BACK AT YOUR NEXT LONG REST, WITH 1 STRESS CLEARED' disappears), PartyBoard.tsx:529/548 (GM board un-greys). The app breaks a promise it just printed. THE BRANCH'S OWN TEST PINS THE WRONG OUTCOME: tests/engine/rest.test.ts:367 builds withCompanion(3) — 3/3, i.e. already away — takes a SHORT rest and asserts marked === 0. The test titled 'does not bring anyone back on a short rest' (rest.test.ts:449-460) only picks tend-to-wounds, which clears no Stress and never enters the path. CONTEXT lens searched f1a1922's long commit message, all docblocks, BACKLOG.md and every handoff and found NO argument the other way — an oversight, not a decision. Severity votes: high, medium, medium, high, medium, high, high, high, medium. IMPORTANT CAVEAT raised by two verifiers: folio 18's second bullet ('when you choose a downtime move that clears Stress on yourself, your companion clears an equal number') carries no exception, so refusing the clear trades one deviation for another; a faithful fix probably requires availability to STOP being purely derived — which falsifies companion.ts:133's 'there is no second way to be out of the scene' and has transfer-codec implications the branch explicitly argued against. MUTATION PROOF: add `expect(companionIsAway(out.character.companion!)).toBe(true)` to tests/engine/rest.test.ts:367 — RED today. A guard as `alsoTheCompanion`'s first line turns rest.test.ts:379's `toBe(0)` red, which is the demonstration.

### 9. SURVIVED (6 of 6 verifier votes) — companionUpgradeAllowance credits level-ups taken before the companion existed

**CONFIRMED**

src/engine/companion.ts:125, raised twice (candidates 3 and 11, from rules-fidelity and engine-edges); all six verifiers confirmed. `return Math.max(0, c.level - 1) + extra` never asks when the companion sheet arrived. Reproduced against the shipped dataset under Node 24: a level-10 Wizard who multiclassed into Ranger/Beastbound at level 5 gets hasCompanionFeature=true and allowance=9; a level-5 Warrior who JUST multiclassed into Beastbound gets allowance=4 (identical to a Beastbound-since-creation level 5). Grant path verified link by link: levelUp.ts:342-347 gates multiclass at tier 3 and REQUIRES pick.detail.subclassRef; levelUp.ts:458-468 appends it to subclassRefs; LevelUp.tsx:897-918 renders every subclass with no filter so Beastbound is selectable; companion.ts:47-55 hasCompanionFeature scans all subclassRefs by feature name 'Companion'. SRD folio 9: 'Take the Ranger Companion sheet. When you level up your character, choose a level-up option for your companion from this sheet as well' — prospective. The wrong number is on glass (Companion.tsx:517-519 'N OF 8 MARKED · M EARNED') and on paper (sheetModel.ts:288 -> CharacterSheet.tsx:411). Not covered: all six cases at tests/engine/companion.test.ts:213-247 use creation-time subclasses. Notably the docblock DOES reason about the other timing question ('a tier-2 Beastbound is not credited with the Advanced Training they have not taken yet', with its own test at :233-239), which shows the author was thinking about grant timing and simply did not consider that the sheet itself can arrive mid-career. Severities corrected to low/low/low and medium/medium/low — display-only, gates nothing, but a materially wrong advisory number (4 of 8) reachable through supported UI. One verifier flagged that the 'correct' value is itself arguable by one (5 or 6 at level 5+multiclass), so a fix must pick a reading and record it. MUTATION PROOF: derive the base term from levelUpHistory (level-ups strictly after the level at which a multiclass/subclass entry first introduced a companion-granting subclass, falling back to level-1); assert 0 for the level-5 multiclassed Warrior — returns 4 today; the six existing it.each cases must stay green.

### 12. SURVIVED (3 of 3) — a .dhchar with a partial companion object is imported without a warning and then kills the GM party board persistently

**CONFIRMED (severities: medium / high / medium)**

src/transfer/fileIo.ts:279. checkShapes validates the interior of every other nested shape on a Character (gold, experiences, inventory, levelUpHistory, the four counters, thresholdOverride) but checks `companion` and `beastform` only for object-ness (fileIo.ts:277-281). Three independent agents reproduced it under Node 24 + jsdom: parseTransferFile on a real serializeCharacter output with `companion` replaced by `{}` (and separately by a full companion minus its `damage` key) returns warnings:[] and passes the shape straight through — versionOf treats current schemaVersion as current so no converter runs, and readCharacterRecord ends with `{...base, ...(value as Partial<Character>)}`. importParty then commits the row and the next PartyBoard render throws `TypeError: Cannot read properties of undefined (reading 'replace')` — parseDamage(companion.damage) at src/engine/dice.ts:245, reached from companionDamage at PartyBoard.tsx:528, the FIRST new line of CompanionLine (`git show main:src/ui/gm/PartyBoard.tsx | grep -c companion` = 0, so this is new on the branch). Persistence proven the hard way: flushGm() then readCampaigns() returns the campaign with `party: [{name:'Ilya', companion:{}}]` and warnings [] — readPartyMember (shared/campaigns.ts:617-628) accepts on `sheet.name` alone. PartyBoard is inside the single `<ScreenBoundary name="GM tools">` at App.tsx:528, and both controls that could remove the row (removePartyMember at PartyBoard.tsx:657, removeCampaign at MenuSheet.tsx:290) live inside that boundary; `gather` persists board.region so a relaunch returns to the same region. TWO VERIFIERS CORRECTED THE BLAST RADIUS DOWNWARD: the GM screen is NOT dead on every launch — PartyBoard renders only when `tool === 'party'` (Gm.tsx:282) and `tool` is component-local state starting at null, so the screen loads fine and dies each time the board is opened; ScreenBoundary offers 'Try again', the Header sits outside it, and Settings.tsx:482 has a GM-tools toggle. The trigger requires a hand-edited, third-party or truncated file (the app never writes one; the QR codec always emits a whole companion; the player's own library import throws harmlessly at state.ts:852 before persisting) — but that is exactly the input class checkShapes exists for, and its own docblock (fileIo.ts:227-239) states the rule being broken: copying a bad file in 'turns one bad file into a sheet that throws on every render… Anything present has to have the right shape.' The branch's audit commit c88bd21 fixed only the narrower damageType case and said the general hazard 'is worth its own item' — but no .md file was changed on this branch, so the item was never filed. FIX: extend the companion clause in checkShapes to require name/description strings, evasion number, damage string, range string, stress as a counter, experiences/upgrades arrays. The same gap remains for beastform and traitMarks.

### 13. SURVIVED (3 of 3) — the printed sheet silently under-counts a companion's marked level-up options

**CONFIRMED (all three verifiers: low)**

src/ui/print/sheetModel.ts:284. `upgrades: companionUpgrades(dataset.rules).map(u => ({...u, marked: companion.upgrades.includes(u.id)}))` builds the printed list by iterating the DATASET and flagging intersection; CharacterSheet.tsx:409-412 derives the header count from that same intersection, so the printed number is |persisted ∩ dataset|, never |persisted|. Reproduced twice by rendering the real pipeline with renderToStaticMarkup under Node 24: with `companion.upgrades: ['not-in-the-book','aware','vicious']` the header prints 'Level-up options · 2 marked · 4 earned' and the string 'not-in-the-book' appears nowhere; with `leveling-up-your-companion` filtered out of the dataset it prints '· 0 marked · 4 earned' over an empty grid — indistinguishable from a player who marked none. The Play screen diverges: Companion.tsx:520 prints the PERSISTED length ('3 OF 8 MARKED') and :737-746 additionally says out loud 'N already marked on this sheet cannot be shown.' Reachability is real: `rules` is in COLLECTIONS (src/store/dataset.ts:38) and resolveDataset applies overlay fields verbatim, so a layer can rewrite the folio-18 section and renaming an option changes slugify(name) and orphans the persisted slug; the binary codec writes/reads upgrade slugs as raw strings with no dataset validation (codec.ts:628-629, 644-645); toggleUpgrade (Companion.tsx:469-475) never prunes stale ids and no sanitizer exists. Before this branch the vocabulary was eight literals in src/engine/companion.ts, so the failure mode did not exist. srdReference.ts:756-757 documents the contract the print path violates. The strongest precedent, found by the CONTEXT verifier: sheetModel.ts:159-160 already declares `missing: string[]` — 'Refs this dataset could not resolve. Shown, never silently dropped.' — computed at :434 for loadout/vault; companion upgrades were simply not routed through it. Corrections to the claim: the Play screen's 'cannot be shown' sentence fires only when the WHOLE section is missing, not on a single renamed option; and there is no `unresolvedRefs` symbol in src/ — the real precedent is `missing`, which makes the inconsistency argument stronger. FIX + MUTATION PROOF: add `missingUpgrades: companion.upgrades.filter(id => !options.some(u => u.id === id))` to SheetCompanion, report it in the header and body, and delete the filter to redden.

### 14. SURVIVED (3 of 3, all severities high) — swapping the worn Beastform while its attack is armed leaves the previous form's trait on the roll

**CONFIRMED — the most serious live user-visible defect the run found**

src/ui/player/Play.tsx:246. `arm()` writes the trait ONCE at the tap (`if (declaration.kind === 'beastform') { const trait = stats.beastform?.form.attack.trait; if (trait !== undefined) setTrait(trait); return; }`) but stores the form-agnostic declaration `{kind:'beastform'}` (Play.tsx:1853), while the pool is re-derived every render from beastformSource(stats) (Play.tsx:186). No effect re-syncs the trait: the only effects are keyed on characterId and companionArmed. Beastform.tsx's picker calls enterBeastform unconditionally with no drop and no access to `arming` (Beastform.tsx:372-373; the phone chip's aria-label is literally '… — tap to change form'). The roll uses the STALE state: DualityRoll.tsx:786 computes `rollModifier(character, stats, trait)` from the trait state, not from source.trait. Reproduced by all three verifiers, twice through the full UI path with no store poking: 'ARMED: Pack Predator 4d8+2 ARMED · STRENGTH' -> open picker -> pick Agile Scout -> 'Agile Scout 4d4 ARMED · AGILITY' while the pressed trait tile still reads STRENGTH and the ROLL button reads 'ROLL 2d12 +3 · STRENGTH'. One agent deliberately patched the base traits apart to prove the MODIFIER is wrong, not just the label. THE CONTEXT VERIFIER FOUND IT IS BROADER THAN REPORTED: DROP nulls `source` but never clears `declared`, so `{kind:'beastform'}` survives a drop (including the branch's own new auto-drop at the last Hit Point, which only nulls character.beastform) and the next transform silently RE-ARMS with the previous form's trait — i.e. it is reachable through the ordinary shape-change flow, not only via an exotic armed-swap. Trait values genuinely differ across forms available at one tier (data/srd-1.0.json tier 1: agility, instinct, strength, finesse). NOT A DOCUMENTED DECISION: `arm`'s docblock enumerates every case it reasoned about (weapon, spellcast, unarmed, 'withdrawing does not touch the trait either') and a form-to-form change is not among them; the `source` docblock documents the form-agnostic declaration deliberately but only for the DICE POOL; the [characterId] effect at Play.tsx:146 exists precisely to clear this class of stale declaration+trait pairing; nothing in BACKLOG.md or the handoffs mentions arming caveats. tests/ui/beastform.test.tsx pins arming (196) and drop-takes-the-offer (209) but no form change while armed. MUTATION PROOF: with a re-sync in place (effect keyed on the resolved source.trait, or carry the form ref inside the declaration so a swap invalidates it), deleting it must redden a new test that arms a strength form, swaps the worn ref to an agility form, and asserts the PRESSED trait chip reads AGILITY; plus a second red test for the DROP-then-transform path asserting no ARMED row returns after a drop.

### 15. SURVIVED (3 of 3) — the phone's Experiences fold is gated on the character's own list but filled from the roll's list

**CONFIRMED (all three corrected high -> medium)**

src/ui/player/Play.tsx:3737. `rollExperiences = experiencesFor(character, arming.source)` at :3457 swaps to the companion's list when a companion attack is armed, but the `? :` deciding whether the Disclosure renders at all still reads `character.experiences.length > 0`, while its summary (:3744-3745) and its `<ExperienceRow experiences={rollExperiences}>` (:3749-3751) come from rollExperiences. Two failures. (a) A character with zero own Experiences gets NO fold, and on a phone the fold is the only route — DualityRoll.tsx:1081 passes `inlineExperiences={layout !== 'phone'}`, and the test file's own docblock says 'ExperienceRow is mounted by PlayPhone and by nothing else'. Reproduced: with `experiences: []` on the character and Ash carrying 'Sharp eyes +2', the complete list of disclosure headers is ARMEDSPELLCAST / MODS / Weapons & armour / Cards / Rest & downtime / Carried / Lineage — no Experiences fold; after opening every other fold, no button anywhere mentions Sharp eyes, and the only occurrence is a non-interactive SPAN.t-meta in the companion panel. The SRD rule the whole branch exists to implement ('Spend a Hope to add an applicable Companion Experience') becomes unreachable. (b) Converse: a companion with zero Experiences on a character who has some renders a fold headed 'Experiences 0' whose body is null (ExperienceRow returns null at length 0) — exactly the 'fold that opens onto nothing is a row spent on an empty room' the docblock immediately above line 3735 forbids. Precondition is reachable: parts.tsx:527 defaults minRows=0 and Edit.tsx:180 mounts ExperienceEditor with no minRows, so every row's ✕ stays enabled post-creation (only Wizard.tsx:1608 passes minRows={2}); Companion.tsx:719 removes a companion Experience with no minimum at all; an imported JSON with no `experiences` key inherits newCharacter()'s `[]` while the companion's list survives. One verifier rsync'd the branch, applied the fix (gate on rollExperiences.length > 0, and the same for stackedA at :3470) and confirmed the mutated test passes and the FULL suite stays green with the fixture restored — so NO existing test pins the current gate. git blame: the gate came from ec55841e on main (pre-companion); lines 3744/3745/3750 were rewritten on this branch by 19ddfa8 — the branch changed the contents and left the gate. Correction: stackedA at :3470 uses the identical expression so it currently agrees with the gate; it needs updating only as a consequence of the fix.

### 16. SURVIVED (3 of 3) — an armed companion attack outlives the panel that armed it; on the desktop cockpit nothing on screen says so

**CONFIRMED (medium / medium / low)**

src/ui/player/Vitals.tsx:79-80. `who` is `useState<Who>('you')` inside Vitals while `declared`/`source` live in Play (Play.tsx:104, 174-198) and are derived from character.companion, never from `who`. `grep -rn "kind: 'companion'" src/` returns exactly ONE call site — Companion.tsx:345 `arming.arm(armed ? null : {kind:'companion'})` — inside CompanionPanel, which Vitals mounts only in the `who === 'companion'` branch (:118-128). Tapping YOU therefore unmounts the only arm/disarm control while leaving the declaration live. Nothing else names it: DualityRoll's armSummary (:917) is Experiences/Hope/bonus dice only; armedMods (:1025) is REACTION/ADV/DIS/DIFF/+Dn/SPELLCAST and is phone-only; Equipped draws weapons, Beastform, unarmed and spellcast and never the companion. Reproduced on both layouts by all three: DESKTOP — 'buttons naming ARMED: []', the string ARMED appears nowhere in the screen text, the Experience chips have silently become the companion's (SHARP EYES present, RAN WITH THE WOLVES gone) with no label saying whose, and pressing ROLL returns the damage control 'IF IT HIT · 2d6+2' — the companion's pool, off a roll the player did not declare as the companion's. PHONE — the only two ARMED strings are 'ARMEDSPELLCAST' and 'Weapons & armourARMED · ASH', and opening that fold yields Broadsword/Shortsword/Unarmed/Spellcast/Gambeson with nothing pressed and no ASH — a POSITIVE MISSTATEMENT, not merely an omission. This breaks the rule quoted four lines above it at Play.tsx:3689-3691, 'A declaration you cannot see is not a declaration'. The companion is the only one of the five AttackSource kinds with no permanent statement on the desktop screen. CORRECTIONS raised by verifiers (why one voted low): WhoSwitch stays mounted in the non-companion branch (Vitals.tsx:134), so tapping the ASH segment restores the control in ONE tap ('returning shows the armed box again = true, pressed = true, label "Stop commanding Ash to attack — a Spellcast Roll, 2d6+2 damage"'); chooseTrait (Play.tsx:288-306) nulls `declared` on ANY trait tap, so the stale-damage-offer scenario needs the player to roll having touched no trait chip; and the post-roll damage offer does name the source via sourceName. One verifier warned the first suggested fix (auto-disarm when `who` leaves 'companion') would be a REGRESSION — it silently un-declares a command because the player glanced at their own HP — and that the proof must assert BOTH that some visible element names the armed companion AND that the declaration survives the switch, or a mutation that auto-disarms would make it pass for the wrong reason.

### 17. SURVIVED (3 of 3) — the only test of the branch's headline 'arming the companion arms the Spellcast Roll' is satisfied by the class line before anything is armed

**CONFIRMED (all three corrected high -> medium; proven by mutation)**

tests/ui/companion.test.tsx:322-328. `expect(text()).toContain('SPELLCAST')` runs against the whole Play screen; SpellcastLine (Play.tsx:515-523) renders 'SPELLCAST · <TRAIT>' unconditionally inside Identity, which PlayDesktop mounts unconditionally at :2820. Probed: BEFORE ARMING contains SPELLCAST true (3 occurrences), contains ARMED false, context '…LEVEL 3Ranger — BeastboundSPELLCAST · AGILITYClank · Highborne…'. MUTATION RUN TWICE INDEPENDENTLY IN SANDBOX COPIES: change Play.tsx:261 (the setTrait inside `if (declaration.kind === 'companion')`, NOT the one at :240) to `setTrait('knowledge')` — chosen over 'agility' because Beastbound's Spellcast trait IS agility, making 'agility' a weaker mutant — and the full suite passes (3265 passed, the only failure being a matrix.test.ts load timeout that passes in isolation). tests/ui/companion.test.tsx passes intact. So nothing in the repo pins the sentence commit 19ddfa8 exists to implement. IMPORTANT CORRECTION that makes it stronger: one verifier re-ran the same mutation at commit 19ddfa8 itself — which introduced both the behaviour and this test and PREDATES SpellcastLine (added later in ad78393) — and the mutant survives there too, because 'SPELLCAST' was already on screen twice pre-arm (the trait-grid hint 'TAP A TRAIT TO ARM THE ROLL · AGILITY IS SPELLCAST' and the SPELLCAST chip in the modifier row). The assertion was born non-discriminating; SpellcastLine merely added a third source. Not deliberate: thirty lines below in the SAME file, the `chips()` helper's docblock argues the opposite ('A screen-wide assertion passes for the wrong reason and then fails for the wrong reason, which is how the third of these was written the first time'). The shipped behaviour is correct, so this is verification quality, not a live bug. FIX: assert the SPELLCAST trait chip carries aria-pressed="true" after the click, or scope the text assertion to the roll bar the way tests/ui/playSheet.test.tsx:4239 already does.

### 18. SURVIVED (3 of 3) — the Beastform 'the trait it arms is the form's own' assertion matches the default Agility chip and never checks the trait

**CONFIRMED (all three corrected high -> medium; proven by mutation)**

tests/ui/beastform.test.tsx:203-212. The finder is `buttons().find(b => b.getAttribute('aria-pressed') === 'true' && /^[A-Z]{3}$/.test((b.textContent ?? '').trim().slice(0,3)))` followed only by `toBeDefined()`. Play.tsx:103 initialises `useState<RollTrait>('agility')` and TraitRow renders each chip with aria-pressed={active} and a three-capital label, so 'AGI +1' is pressed from first render and the finder is satisfied BEFORE the attack row is clicked. Probed: 'FORM: Massive Behemoth ATTACK TRAIT: strength / BEFORE CLICK -> "AGI +1" / AFTER CLICK -> "STR +5"'. MUTATION RUN: replacing Play.tsx:248 (`if (trait !== undefined) setTrait(trait);`) with a no-op while keeping the `return` leaves the armed chip at 'AGI +1' against a form whose attack trait is 'strength', and ALL 11 tests in beastform.test.tsx pass; widened to `npx vitest run tests/ui` -> 58 files / 1418 tests passed, 0 failed; a third agent ran the full suite in a synced copy -> 133 files, 3266 passed, 0 failed. `arm` exists only in Play.tsx and every test file that renders Play is under tests/ui, so nothing anywhere guards it. The SRD sentence being implemented is folio 12's 'you use the creature's listed range, trait, and damage dice'. Not a decision: the test's own comment states the intent it fails to check, and the sibling weapon case in tests/ui/attack.test.ts asserts `traitChip('STR')` aria-pressed === 'true' after arming a Battleaxe — the project's own standard. Shipped behaviour is correct today (unmutated probe: STR +5). OPERATIONAL WARNING left by one agent: files under scratchpad/mut/ were repeatedly reverted mid-run by something outside its session (Play.tsx restored to pristine twice), so mutation results from that shared path should not be trusted; its own results come from a uniquely-named directory where the mutant was verified present before AND after the run.

### 19. SURVIVED (3 of 3) — the party board's companion EVASION, the stated reason the line exists, is asserted against the character's own EVASION figure

**CONFIRMED (all three corrected medium -> low; proven by mutation)**

tests/gm/partyBoard.test.tsx:252. `expect(text()).toContain('EVASION')` runs against the whole board, which already renders `<Figure label="EVASION" value={stats.evasion}>` for the PC in both layouts (PartyBoard.tsx:387, rendered at :460/:465). Probed with a sheet that has NO companion at all: textContent reads '…EVASION IS THE DIFFICULTY OF ANY ROLL AGAINST THAT PCEVASION11MAJOR8SEVERE14MarekBARD · LV3 · PROF 2…' and the assertion passes at both widths. One verifier found it DOUBLY vacuous — the board's standing banner at PartyBoard.tsx:264 independently prints 'EVASION IS THE DIFFICULTY OF ANY ROLL AGAINST THAT PC', so even deleting the Figure entirely would leave it green. MUTATION EXECUTED in a synced copy: deleting ` · EVASION{' '}` and the `{companion.evasion}` span from CompanionLine (PartyBoard.tsx:540-546) while leaving name, dice, range, damage type and Stress intact leaves tests/gm/partyBoard.test.tsx at 14/14 GREEN, and the whole tests/gm folder (15 files / 444 tests) green. `grep -rn "EVASION" tests/` finds exactly one hit in the entire suite. The fixture's `evasion: 12` appears only at partyBoard.test.tsx:231. CORRECTION to the reported fix, verified: no markup change is needed — textContent already yields the contiguous 'EVASION 12' because the JSX `{' '}` supplies the space, while the PC's Figure concatenates as 'EVASION11' with no separator, so `toContain('EVASION 12')` is unambiguous; it is GREEN on HEAD and RED on the mutation. Not deliberate: CompanionLine's own docblock (PartyBoard.tsx:369-371) says 'Evasion leads because it is what the GM needs: it is the number an attack is rolled against, and it is not the Ranger's', the describe block's docblock repeats it, and the sibling assertion one line above is explicitly careful about this exact hazard for STRESS.

### 20. SURVIVED (3 of 3) — the Companion-Experience rule is asserted only where it is drawn, never where it is paid; DualityRoll's use of experiencesFor is uncovered

**CONFIRMED (medium / medium / low; proven by mutation AND by writing the missing test)**

tests/ui/companion.test.tsx:358. experiencesFor has two production callers: Play.tsx:3457 (draws the chip row) and DualityRoll.tsx:761 (computes armedList -> experienceBonus at :772 -> hopeCost at :781 -> the actual Hope debit inside resolve at ~:832 -> the log detail line -> the cockpit's inline chips at :1080). All three new chip tests call `usePhoneViewport()` and read the Experiences disclosure, i.e. only the drawing caller; `grep -rn experiencesFor tests/` returns only tests/ui/attack.test.ts unit calls, and no test file mounts DualityRoll with a companion armed. MUTATION EXECUTED twice in isolated copies (one via `git archive` after an rsync copy was found contaminated by another agent's in-flight mutation): change DualityRoll.tsx:761 to `character?.experiences ?? []` and the suite stays green — companion.test.tsx passes all 24, and the only full-suite failures are load-timeout flakes reproduced at baseline. tsconfig sets no noUnusedLocals, so the now-unused import does not fail typecheck either: the mutant is green on both gates. THE MISSING TEST WAS WRITTEN AND RUN: arming the companion at phone viewport and pressing 'Utilize Sharp eyes' gives, MUTATED, a roll bar reading 'ROLL2d12 +1 · SPELLCAST · AGILITY—' (no modifier, no Hope charged) and, UNMUTATED, 'ROLLSHARP EYES +2 · 1 HOPE—'. So the regression is real, user-visible, silent, and cheaply assertable. The claim is if anything understated — the untested path is not desktop-only, because hopeCost/experienceBonus feed rollDuality on every layout. Precedent the companion list never got: tests/ui/playSheet.test.tsx:3440-3480 arms 'Ran with the wolves' and asserts the roll bar reads 'NEXT: RAN WITH THE WOLVES +2' on both phone and cockpit.

### 21. SURVIVED (3 of 3) — companionSource cites 'Folio 19' for a sentence that is on folio 18, and claims the dataset carries folio 19 when the parser deliberately refuses it

**CONFIRMED (low / low / medium)**

src/ui/player/attack.ts:350-354. The docblock attributes 'On a success, their damage roll uses your Proficiency and their damage die.' to 'Folio 19' and then says 'The folio answers both, and the dataset carries the folio now.' Both halves false. Verified four ways: (1) the SRD extraction puts the sentence under ATTACKING WITH YOUR COMPANION in column one above the spread footer '18  Daggerheart SRD … Daggerheart SRD  19', with SYNDICATE/ROGUE material on the right page; (2) `d.rules.filter(r => r.sourcePage === 19)` returns `[]` and `working-with-your-companion` has sourcePage 18 with the sentence verbatim in its body; (3) shared/parsers/rules.ts:33-35 excludes 19 by design — 'Folio 19 is deliberately NOT here: it is the Rogue, and the companion text ends with folio 18's second column' — and RANGES contains [12,12] and [18,18] and no [19,19]; (4) the branch contradicts itself: tests/ui/srdReference.test.ts:927 is literally titled 'leaves folio 19 out: it is the Rogue, and no companion text is on it' and pins sourcePage 18. THE ERROR IS REPEATED, not isolated: src/ui/player/attack.ts:351, tests/ui/attack.test.ts:432, tests/ui/companion.test.tsx:270 and :354, plus commit message 19ddfa8. Every OTHER citation in the repo says 18 (shared/types.ts:397, engine/companion.ts:27/131/151, engine/levelUp.ts:488, engine/rest.ts:10, attack.ts:84/519, srdReference.ts:729, tests/engine/companion.test.ts:113, the handoff doc). The feared 'fix' (widening RANGES to [18,19], which would pull the whole Rogue class into a rules section) is already fenced by srdReference.test.ts:927, which is why two verifiers dropped it to low. Comment-only; no runtime, type or test behaviour changes.

### 22. SURVIVED (3 of 3) — blockParts' measured 'seventy bullet paragraphs' went stale, and commit 213b6c3 claims it updated the docblock

**CONFIRMED (all three corrected medium -> low)**

src/ui/shared/srdReference.ts:647-650: 'Measured against data/srd-1.0.json: all twelve table paragraphs and all seventy bullet paragraphs in the shipped file are already pure.' Three independent censuses reimplementing blockParts' own classification (ruleBlocks -> paragraphs split on /\n{2,}/ -> ruleList matching /^-\s+(.+)$/, a paragraph being a pure list when items === non-empty line count) give main {sections:75, table:12, list:70} and branch {sections:80, table:12, list:74}. Main landing on exactly 70 validates the method and proves the comment was accurate when written. The four new pure bullet paragraphs are beastform-options, ranger-companion STEP 3, companion-taking-damage and leveling-up-your-companion. `git show 213b6c3 --stat` touches four src/ files (srdReference.ts:690-695, RuleTableView.tsx:9, ReferenceTables.tsx:1028, SessionBody.tsx:97), all 38/75 -> 42/80, while its message claims 'the five docblocks in src/ that quote those numbers'. Nothing reddens: tests/ui/srdReference.test.ts:644-649 pins 38/7/3/42/12 and never the bullet-paragraph figure. TWO IMPORTANT QUALIFICATIONS FROM THE VERIFIERS: (a) the docblock's SUBSTANTIVE assertion is still TRUE — purity was measured on both datasets and paragraphs-with-any-bullet equals pure-bullet-paragraphs 70/70 on main and 74/74 on branch, so only the scale figure is stale and the decision it supports is unaffected; (b) the staleness is BROADER than the one line — srdReference.ts:993 ('The seventy-five sections are 131,127 bytes'), src/ui/gm/ReferenceTables.tsx:11 ('all seventy-five sections'), src/ui/gm/RuleSearch.tsx:12 ('seventy-five in the shipped SRD') and tests/gm/ruleSearch.test.tsx:193 all still say seventy-five against an 80-section dataset — the spelled-out forms escaped the numeric sweep. The CONTEXT verifier found docs/handoff/BACKLOG-PLAN-2026-08-23.md:79 already books a 'stale-prose sweep' listing those four but NOT srdReference.ts:648, so this is a genuine fifth miss the booked sweep would walk past. MUTATION PROOF: add `expect(bulletParagraphs).toBe(74)` to the ruleSection census describe — passes at 74, reddens against main's dataset.

### 23. SURVIVED (3 of 3) — SpellcastLine says the SRD prints 'SPELLCAST TRAIT' on every subclass page, then says four subclasses have none

**CONFIRMED (all three corrected medium -> low)**

src/ui/player/Play.tsx:498-508. Verified against the PRIMARY SOURCE by two verifiers: a fresh `pdftotext -layout Manuali/Daggerheart-SRD-9-09-25.pdf | grep -c "SPELLCAST TRAIT"` yields exactly 14 occurrences (per page 5=2, 6=2, 9=2, 10=1, 11=3, 12=2, 13=2) against 18 subclasses. The 'printed but empty / dropped by the extractor' alternative was specifically ruled out by reading the raw column layout: page 9's left column runs GUARDIAN SUBCLASSES -> STALWART -> blurb -> FOUNDATION FEATURES with no heading, likewise VENGEANCE; page 13's left column runs WARRIOR SUBCLASSES -> CALL OF THE BRAVE -> FOUNDATION FEATURES and CALL OF THE SLAYER; the two SPELLCAST TRAIT hits on each of those pages belong to the Ranger/Wizard columns of the same spread. A subclass that has one (Warden of the Elements) reads blurb -> SPELLCAST TRAIT -> Instinct -> FOUNDATION FEATURE. The dataset corroborates exactly: 18 subclasses, exactly 4 with spellcastTrait null (stalwart, vengeance, call-of-the-brave, call-of-the-slayer), 14 non-null matching the 14 printed headings one-to-one. Commit ad78393 carries the same false sentence, and c88bd21 did not catch it. Lowered to low by all three because the false clause is contradicted and corrected three lines below it in the same docblock, the null branch of SpellcastLine is already exercised against the real dataset by tests/ui/playFeatures.test.tsx:353, and the fix is a few words ('on every subclass page' -> 'wherever a subclass has one').

### 24. SURVIVED (3 of 3) — CompanionPanel's `arming` prop comment still names surfaces the same file says do not exist: the audit's own fix landed one docblock short

**CONFIRMED (all three: low)**

src/ui/player/Companion.tsx:190 still reads `/** Absent on the surfaces that show a companion without a roll behind them. */`. `git log -S` shows the line was added by this branch in 19ddfa8 and never modified; commit c88bd21's own message says 'CompanionPanel carried the same invented justification' and claims to have removed it, but its diff touches only the BODY docblock in that file (~line 286, about 96 lines below) plus the Vitals.tsx prop comment. The file now contradicts itself: the body docblock says 'In src/ that case does not arise today - Vitals is the only thing that draws this and it always has a roll behind it', and Vitals.tsx:61-65 agrees with the body, not with line 190. Mount census: `grep -rn "CompanionPanel" src/` -> declaration at Companion.tsx:194 and one mount at Vitals.tsx:124 (forwards arming); `grep -rn "<Vitals" src/` -> Play.tsx:2886 and Play.tsx:3577, both passing arming. PartyBoard draws its own companion line from companionDamage/companionIsAway and print/CharacterSheet.tsx:366 renders from SheetCompanion — neither imports CompanionPanel. The only arming-less mounts are in TESTS: screens.test.tsx:741, dialogs.test.tsx:269, companion.test.tsx:128 and :523 — which is the true reason for the `?` and exactly the reason the sibling Vitals prop comment was rewritten to give. No test, docblock or BACKLOG entry argues the wording should stand. Comment-only; the fix is to restate line 190 as 'optional only because tests mount the panel alone.'


## 3. Split verdicts — treat as OPEN

### 10. SPLIT VERDICT — the EARNED readout can exceed the eight boxes the sheet draws (12 EARNED on a sheet with 8)

**CONTESTED: the same defect was KILLED as candidate 4 (1 of 3) and SURVIVED as candidate 10 (2 of 3). Treat as OPEN.**

src/engine/companion.ts:125 / src/ui/player/Companion.tsx:520. All six verifiers agreed the arithmetic: a level-10 Beastbound with Expert Training (+1) and Advanced Training (+2) gets 9+3=12 while the dataset carries exactly 8 options and toggleUpgrade is set-membership over unique slugs, so '8 OF 8 MARKED · 12 EARNED' is reachable and unsatisfiable; sheetModel.ts:288 prints the same number on paper. One REPRODUCTION agent mounted the real CompanionPanel, clicked all eight buttons and read back the literal header string '8 OF 8 MARKED · 12 EARNED', and found the claim UNDERSTATED — with levelUpHistory:[] a plain level-10 Beastbound already reads '8 OF 8 MARKED · 9 EARNED', so no training features are needed and every level-10 Beastbound Ranger sees a self-contradictory header. THE REFUTERS' CASE (2 of 6): the docblock at src/engine/companion.ts:98-104 says 'A READOUT AND NOT A GATE. Nothing here refuses a box'; Companion.tsx:516-521 repeats it; BACKLOG.md:635-641 marks the Beastbound training bullet 'settled, and deliberately not built, d5d56b9' with 'Do not re-open this as same shape, different quantity'; folio 18 (checked against the page image f18-10.png) prints eight prose bullets with no per-option checkbox counts, so the SRD itself over-grants at high level and the app is faithfully reporting that surplus — clamping at 8 would invent a cap and erase the only evidence Expert/Advanced Training did anything, while per-option counts would invent repeatability. Both sides agree the honest reading question must be settled first; the disagreement is whether a display that is arithmetically unsatisfiable is a defect or a faithful transcription.

### 11. SPLIT VERDICT — a companion handed over by QR silently arrives dealing physical damage, and the receiving surfaces state it as fact

**CONTESTED: KILLED as candidate 5 (1 of 3, framed at the codec) but SURVIVED as candidate 7 (2 of 3, framed at the GM party board). Treat the board-side half as OPEN.**

src/transfer/codec.ts:655 / src/ui/gm/PartyBoard.tsx:546. Facts undisputed by all six: writeCompanion (codec.ts:617-630) never writes damageType; readCompanion (:655) hardcodes 'phy'; DecodeResult.warnings (:795-801, built at :1019-1028) is populated ONLY for unresolved registry refs, so no import notice fires. A REPRODUCTION agent ran the real chain — framesForCharacter -> characterFromPayload — and printed: QR frames 1, sent 'mag', received 'phy', warnings [], companion otherwise intact; a second ran encodeCharacter/decodeCharacter then importParty(..., 'code') and rendered the board, getting the exact predicted row 'ASHFOOT · EVASION 12 · 2d6+2 CLOSE PHY · STRESS 0/3' with source 'code' on the row and no caveat anywhere. THE REFUTERS' CASE: codec.ts:33-70 documents this as the third of 'FOUR DELIBERATE LOSSES' by name with the format-4 cost; writeCompanion:609-615 repeats it; tests/transfer/codec.test.ts:638-686 pins it in the opposite direction ('is lost when it is magic, and comes back physical'), so the finding's suggested proof would flip a test written on purpose; the player's own Companion.tsx:310 and print/CharacterSheet.tsx:377 and Transfer.tsx:315-320 are equally silent, so the board is not held to a lower standard. THE SURVIVORS' CASE, which the refuters did not address: (i) the codec ALREADY HAS the disclosure channel and it is wired to both receive surfaces (Transfer.tsx:318 -> ImportConflicts.tsx:57, and PartyBoard.tsx:73-74 which even flips the Note to tone:'warn') — used today for unresolved refs, a loss that is ALREADY VISIBLE on the sheet, while this invisible loss gets nothing; (ii) the project has already paid this exact duty once — Rest.tsx:546 prints 'COUNTED HERE · A SHEET THAT ARRIVED BY QR ARRIVES AT ZERO'; (iii) the GM has no toggle to correct it and no reason to suspect the row while adjudicating physical resistance, whereas the player has a PHY/MAG chip two taps away at Companion.tsx:620; (iv) PartyBoard's own header rule says everything on the board is a 'sighting' and it 'must never print a number in a way that implies it came from the phone in the player's hand' — this PHY came from readCompanion's default. Both sides agree the fix is a display decision, not a format change, and that member.source is already stored (shared/campaigns.ts:597 PARTY_SOURCES). CORRECT MUTATION PROOF (from the reproduction agent): append a warning in codec.ts around :1019 when the decoded character has a non-null companion, then assert warnings is non-empty for a companion sheet — that assertion fails today; do NOT write the naive round-trip test, which would flip a deliberate pin.


## 4. REFUTED — do not re-chase these

### 25. REFUTED (1 of 3) — 'Light in the Dark' is a box the app draws and a Hope slot the app then refuses to give

**REFUTED as a branch finding; real as a pre-existing product gap**

src/engine/character.ts:395. All three verifiers reproduced the arithmetic under Node 24: for a character with `companion.upgrades = ['light-in-the-dark']` and `hope: {marked:0, max:7}`, deriveStats gives maxHope 6 and syncCounters clamps back to {marked:0, max:6}; maxHope is `Math.max(0, BASE_HOPE - c.scars.length)` with no companion term, no sumOf(modifiers) term (unlike maxHp/maxStress immediately above) and no override field anywhere. But the causal story is false: `git diff main..beast-sheets -- src/engine/character.ts` is EMPTY and line 395 is byte-identical to main; `git show main:src/engine/companion.ts` already carried all eight options including Light in the Dark's verbatim sentence, and `git show main:src/ui/player/Companion.tsx:558-597` already rendered all eight as toggle rows with the text underneath and printed 'N OF 8 OPTIONS MARKED'. Ticking it already did nothing on main. It is also a uniformly applied documented stance: src/engine/companion.ts's opening paragraph (unchanged from main) says 'Everything else on the companion sheet is a choice the player records… The app holds the sheet; it does not play the animal'; none of the eight is applied arithmetically; src/engine/modifiers.ts is an explicitly hand-authored register with a 'WHAT IS DELIBERATELY ABSENT' section carrying no Hope term; tests/engine/matrix.test.ts:783-793 pins the formula across the whole matrix and tools/sampleCharacters.ts:152 already seeds light-in-the-dark into that generator. WHAT SURVIVES AS A BACKLOG ITEM: of the eight, Light in the Dark is the ONLY one whose effect lands on a track the app owns (the character's Hope max) rather than on an editable companion-sheet field — Aware maps to the Evasion stepper, Resilient to Stress slots, Vicious to damage/range, Intelligent to the Experience bonus control — so it is the only one a player cannot record even by hand. The CORRECTNESS verifier also noted the branch DOES newly aggravate it: print/CharacterSheet.tsx:415-427 now prints 'Light in the Dark / Taken / Use this as an additional Hope slot your character can mark.' on the same page as a Hope row of six solid boxes with zero growth boxes.

### 26. REFUTED (0 of 3) — companionIsAway is level-triggered, so lowering STRESS SLOTS puts a companion out of a scene nothing removed them from

**REFUTED unanimously**

src/engine/companion.ts:146. The arithmetic reproduces — one verifier mounted the real CompanionPanel and clicked the actual button aria-label='Lower stress slots' on a companion at 2/3, getting {marked:2, max:2}, companionIsAway true, and 'OUT OF THE SCENE' in the DOM. But every premise the finding rests on is false. (1) NOT SILENT — the only thing that happens is a loud red banner saying exactly what the app believes. (2) THE RECOVERY CLAIM IS FALSE — Stepper (Companion.tsx:139-180) has a `min` but no max and the raise button is `onChange(value+1)`, so pressing '+' restores {marked:2, max:3} losslessly (the clamp Math.min(2,2) never changed marked); the editable STRESS Track sits immediately above it too. 'The player's only route back is a long rest' is wrong. (3) THE DERIVED SENTENCE IS TRUE — the companion really has 2 slots and really has 2 marked; per folio 18 that is out of the scene, and the marks exist. (4) THE BEASTFORM ANALOGY DOES NOT TRANSFER — dropFormOnLastHitPoint needed an edge because it PERFORMS a stored destructive transition triggered by an AUTOMATIC recomputation (syncCounters on a dataset reload); companionIsAway is a pure predicate read by four call sites, triggered only by the player's own deliberate tap, writing nothing and logging nothing. (5) The suggested proof `expect(companionIsAway({marked:2,max:2})).toBe(false)` directly contradicts the pinned test at tests/engine/companion.test.ts:126-140, whose comment argues for the inclusive comparison on purpose ('>= and not ===, so a companion at 4 of 3 is out of the scene rather than quietly still in it'). (6) The offered alternative (have the stepper leave marked strictly below the new max) would silently unmark a Stress the companion genuinely took — handing a clear nobody earned, which the module's own tie-break ('between two readings the app takes the one that invents nothing') forbids. (7) The Math.min clamp is pre-existing and unchanged: `git show main:src/ui/player/Companion.tsx` has it verbatim at 437-444.

### 27. REFUTED (0 of 3) — the MIGRATIONS header contradicts the 3->4 converter's own note

**REFUTED unanimously**

shared/migrations.ts:75. All three verifiers found the two sentences have DIFFERENT SUBJECTS and are complementary. The header says both converters 'seed a field with the value the older build already behaved as if it had' — a claim about the OLD BUILD'S BEHAVIOUR. The 3->4 note says 'the app does not know what the TABLE did' — a claim about the fiction at the players' table. Both true. And the header is literally correct for 3->4: consecutiveShortRests has exactly ONE behavioural consumer, mustTakeLongRest (src/engine/rest.ts:310-311, `n >= 3`), surfaced at src/ui/player/Rest.tsx:385-391 ('NONE COUNTED' at 0, 'N SHORT IN A ROW' at 1-2, 'LONG REST DUE' at >=3). A schema-3 build had no field, no gate and no count display — observationally identical to a schema-4 sheet at 0 and distinguishable from one seeded at 1, 2 or 3. So 0 is uniquely the value the old build behaved as if it had. The repo already draws this exact distinction on main in mustTakeLongRest's doc-comment (rest.ts:306-308): "'false' from this function means 'this sheet has not counted three', never 'this table has not taken three'" — the header's phrasing is deliberately on the sheet/build side of that line. The finding's own suggested proof undercuts it: 'the field did not exist, so no schema-3 behaviour depended on its value' is precisely the condition under which the old build's behaviour matches the seeded 0. The note's closing clause ('rather than refusing a rest nobody recorded') describes what a NON-zero seed would do — restating the header's rule rather than denying it.

### 28. REFUTED (1 of 3) — CompanionLine's 'reads by comparison and never by method call, the way CompanionPanel already does' is false

**REFUTED as a correctness defect (1 of 3); the prose inaccuracy is conceded by all three at low**

src/ui/gm/PartyBoard.tsx:514. The REPRODUCTION verifier actually rendered the board with fields deleted one at a time: damageType deleted -> renders (the fix works); NAME deleted -> `TypeError: Cannot read properties of undefined (reading 'toUpperCase')`, board fails to render; range deleted -> renders, printing 'UNDEFINED'; companion key absent entirely -> `companion === null` is false and companionDamage throws on .damage. And `git log -p -S"String(companion.range)"` shows c88bd21 changed damageType to a comparison AND added String() around range in the SAME hunk while leaving name.toUpperCase() untouched — so the file demonstrably applied the hardening to one string field and not the other. WHY TWO VERIFIERS STILL REFUTED IT: the failure is unreachable from any shipped schema. shared/migrations.ts has exactly two converters (3->4 consecutiveShortRests, 4->5 companion.damageType), SCHEMA_VERSION was 3 from the first commit (8c83f78), OLDEST_READABLE is 3, and CompanionState has carried name/description/evasion/stress/damage/range since schema 3 — so damageType is the ONLY field that has ever been absent from a stored sheet, and no version of this app has ever written a companion object missing `name`. The binary codec also always writes and reads a name string. The docblock sentence is true of ITS SUBJECT — the paragraph it closes is entirely about damageType, and CompanionPanel does read damageType by comparison in all four of its uses (Companion.tsx:310, 620, 622, 627-628); pointing at Companion.tsx:309's `range.toUpperCase()` is a category error because CompanionPanel is mounted only from Vitals.tsx:124 on the player's own MIGRATED store and never receives an unmigrated sheet. Finally, the general hazard is a documented OPEN decision, not an oversight: docs/handoff/HANDOFF-beast-sheets-2026-08-23.md §6 ('Il pericolo che resta in piedi, ed è tuo da decidere') states the rule, names the structural fix (running readCharacterRecord inside readPartyMember) and records that it is 'un cambio di design, non una svista, e la decisione è del proprietario'. What survives: the ALL-CAPS 'NOTHING HERE MAY ASSUME A FIELD IS PRESENT' is broader than the code enforces, and `String(companion.range).toUpperCase()` six lines from `companion.name.toUpperCase()` is a visible inconsistency — a one-clause prose fix (`String(companion.name)`, or scope the rule to fields added after schema 3).

### 29. REFUTED (1 of 3) — ADDITIONAL_OPTIONS does not do what its docblock says

**REFUTED (1 of 3); the three mechanical gaps are real and unreachable**

src/engine/companion.ts:116. All three verifiers reproduced the regex behaviour exactly: 'Additional level-up options for your companion: choose two.' -> 0; 'You may never take an additional level-up option while Restrained.' -> 1 (a refusal counted as a grant); 'Choose an additional… Then choose two additional…' -> 1 (non-global `exec` returns only the first match). The REPRODUCTION verifier found a FOURTH the claim missed: a digit count word ('gains 2 additional level-up options') matches `\w+`, misses COUNT_WORDS and falls through to `?? 1`. WHY IT WAS REFUTED: (1) the docblock misreading is load-bearing — 'a feature that says neither is counted as one rather than as none' means neither of the two NUMBER WORDS, which the `?? 1` fallback implements exactly ('several additional level-up options' -> 1); (2) unreachable — grepping data/srd-1.0.json and the SRD extraction gives exactly two hits for 'additional level-up option(s)', Expert Training and Advanced Training, both parsed correctly as 1 and 2; every failing case is invented text; (3) THE SCENARIO ALREADY HAS A DELIBERATE TRIPWIRE — tests/ui/cardAllowance.test.ts:246-259 scans the whole dataset with the LOOSER regex /additional level-up option/i (no preceding-word requirement) and asserts a pinned EXACT LIST ['beastbound · specialization · Expert Training', 'beastbound · mastery · Advanced Training'], with the written rationale 'so that a revision adding a third companion-training feature lands here, in front of somebody, rather than in neither guard' — and the finding's own hypothetical MATCHES that looser regex and would fail the pinned equality; (4) blast radius zero, gates nothing; (5) the suggested fix does not fix its own scenario — requiring a granting verb /\bchoose\b|\bgain\b/ still counts 'you can't choose an additional level-up option'. Useful by-product from the reproduction agent: perturbing the regex reddens tests/engine/companion.test.ts:186-249 (5/10 -> 4/7), which shows the suite pins only the two happy paths and nothing pins the documented fallback, the multi-grant case or the negated mention.

### 30. REFUTED (1 of 3) — a companion who leaves the scene while armed cannot be disarmed, and returns armed without a tap

**REFUTED (1 of 3); the mechanism is real, the premises are not**

src/ui/player/Companion.tsx:300. All three verifiers reproduced the round trip: armed -> away (attack control becomes a div, ARMED text gone, OUT OF THE SCENE shown) -> back (control returns with aria-pressed='true' and ARMED text, no tap). One drove the REAL takeRest(c, stats, 'long', []) to make the return happen. setDeclared is called only at Play.tsx:147 (character switch), :234 (arm) and :303 (chooseTrait) — there is no null-resolution effect. WHY REFUTED: (1) 'no control anywhere can withdraw it' is empirically FALSE — chooseTrait (Play.tsx:288-305) sets declared to null for any non-unarmed kind, so tapping ANY trait chip while away clears it (verified: aria-pressed flips to 'false'); the Equipped weapon rows are live too, and clicking Broadsword does the same. (2) It is the documented architecture, and this exact scenario is NAMED in the code: Play.tsx:152-172 says 'What is remembered is a ref; what is rolled is worked out from it here and nowhere else. That is what makes a level-up, a Beastform or a weapon unequipped in Build move the dice - or take the offer away altogether', and experiencesFor's docblock spells out 'a declaration that resolves to nothing - a companion who has walked out of the scene - is not a companion roll, and the chips go back to the character in the same render the offer does'. (3) The behaviour is uniform and PREDATES the branch — `git show main:src/ui/player/Play.tsx` has the identical `held` guard for weapons, and tests/ui/playSheet.test.tsx pins the offer disappearing while deliberately NOT asserting the declaration is destroyed. (4) The docblock accusation misreads it: 'the offer goes at the moment they do' is a claim about the OFFER, which is true. (5) No incorrect roll is reachable — while away `source` is null so there is no damage offer, and the companionArmed effect at Play.tsx:209-212 clears armedExperiences on both edges. The dissenting verifier's residue: what is new is that the round trip is driven by a REST rather than a deliberate Build action, and the returned state is visibly labelled ARMED; a fix would also cover the beastform auto-drop added by ba4c6af.

### 31. REFUTED (1 of 3) — the Beastbound training bonus is proven only in the engine; the sheet and printout are pinned by a non-Beastbound fixture

**REFUTED (1 of 3); the observation is true, the harm is hypothetical**

tests/ui/companion.test.tsx:535. Facts confirmed by all three: tests/ui/fixture.ts:30-31 resolves dataset.classes[0] = Bard and its first subclass = Troubadour, so `extra` is 0 in every CompanionPanel assertion ('0 EARNED', '4 EARNED' at level 5, '1 EARNED' at level 2 — all exactly level-1); tests/ui/printSheet.test.ts:534's ranger uses makeCharacter with the synthetic 'test-class' resolving to no shipped subclass; `grep -rn "Expert Training|Advanced Training" tests/` hits only tests/engine/companion.test.ts:192-224. WHY REFUTED: both call sites (Companion.tsx:463, sheetModel.ts:288) are pure one-line delegations with no arithmetic of their own; the extra term is exercised by six engine cases including the 'has not taken it yet' trap; and the claimed failure opens with 'Either call site is replaced with…', i.e. it is a mutation-resistance observation about a delegation. The CONTEXT verifier added that the cited file's own header docblock states the division of labour explicitly ('tests/engine/companion.test.ts is the arithmetic… this file is the wiring between them… what this file asserts is what reached the DOM'), that the wiring IS proven at the DOM by three fixtures rendering three different numbers, that tests/ui/fixture.ts:1-14 explicitly refuses fixtures pinned to a shipped SRD id ('a fixture pinned to wizard-school-of-knowledge would rot the first time it moves'), and that the project already chose where this is guarded — tests/ui/cardAllowance.test.ts:228-250's pinned list. TWO VALUABLE CORRECTIONS FROM THE REPRODUCTION AGENT WHO ACTUALLY RAN IT: the suggested proof as written (inline BOTH call sites, expect green) is WRONG — tests/harness/orphans.test.ts fails with 'these are exported, and nothing in src reaches them: engine/companion.ts::companionUpgradeAllowance'; the mutation must inline ONLY Companion.tsx:463. Doing that, the suite shows no allowance failure, and a scratch test on the shipped Beastbound at level 5 with the specialization card renders '5 EARNED' unmutated and '0 OF 8 MARKED · 4 EARNED' mutated. Also: removing the only use in Companion.tsx leaves an unused import that npm run typecheck rejects. The residual fix matching the repo's own habit is a source-scan assertion that both files contain the call.


## 5. Never verified — the pipeline died before or during

### 32. NEVER VERIFIED — the ranger-companion body invents a bulleted list under STEP 3 that the SRD page does not print

**OPEN — all three verify agents started and were killed mid-work**

shared/parsers/rules.ts:760-766, raised by hunt:parser-dataset (agent a1f70cf49b8ff5ba0) at severity low. The `listy` promotion loop (`const listy = p => p?.indented === true && !p.glyph; … if (listy(p) && (listy(paras[i-1]) || listy(paras[i+1]))) p.bullet = true`) turns STEP 3's two ordinary indented paragraphs into markdown list items because they are the only pair of ADJACENT indented paragraphs on the page, while STEP 1/2/4 render as plain paragraphs. Evidence gathered: dumping page.runs for folio 18 via tools/loadSrd.ts shows column 0 at x=68.0 with no `•` run at those coordinates (`[y=245.2 x=68.0] Create two Experiences…`, `[y=272.9 x=68.0] Start with +2…`, and `[y=155.1 x=68.0] Work with the GM…` for the unbulleted STEP 1), whereas column 2 carries real bullet glyphs as text runs (`[y=69.9 x=310.6] • When your companion would take any amount of damage,`). A 150dpi pdftoppm render of the spread confirms visually. The app stamps 'SRD 1.0 · P.18' beside the reproduced text, so the page is rendered in two different shapes with formatting the licensed source does not have; nothing in tests/ui/srdReference.test.ts pins those two lines. The proposed proof: delete the promotion loop at rules.ts:764-767 and run `npm run build:srd -- --check` — it must fail with 'data/srd-1.0.json is out of date' and the freshly built dataset must lose exactly those two '- ' prefixes. The verify agents (a7daa8f4bba935a9a CORRECTNESS, a790b26bbb0e98706 REPRODUCTION, ad8ff4615fcfabb7e CONTEXT) reached 42-44 transcript lines each before being cut off; the last CORRECTNESS line reads '`indentX` confirms x is column-relative. Let me visually verify the source page.'

### 33. NEVER VERIFIED — the 'Weapons & armour' fold header claims ARMED · <companion> for a control that is not inside it

**OPEN — all three verify agents started and were killed mid-work (but independently CONFIRMED as part of surviving finding 16)**

src/ui/player/Play.tsx:3694-3702, raised by hunt:ui-rules (agent a5ec4a4f18b77fc33) at severity high. `summary={arming.source !== null ? `ARMED · ${sourceName(arming.source).toUpperCase()}` : …}` is now true for a companion declaration, but the companion's control lives in CompanionPanel inside Vitals behind the YOU|COMPANION switch, not in Equipped. `sourceName` (attack.ts:503) falls through to source.name for kind:'companion', and companionSource sets `name: companion.name === '' ? 'Your companion' : companion.name`. `grep -rn sourceName src/` returns exactly one UI call site, Play.tsx:3698. Every previously armable kind is rendered inside Equipped, and playSheet.test.tsx:3964/4049/4240 pin 'ARMED · BATTLEAXE' / 'ARMED · UNARMED' / 'ARMED · SPELLCAST' on that same header for exactly that reason. NOTE: although this candidate's own triad never returned, the SAME behaviour was independently reproduced and confirmed by all three verifiers of finding 16 (src/ui/player/Vitals.tsx:79), who probed the phone layout and got `buttons naming ARMED: ["ARMEDSPELLCAST", "Weapons & armourARMED · ASH"]` and, on opening that fold, a body of Broadsword/Shortsword/Unarmed/Spellcast/Gambeson with nothing pressed and no ASH — one of them calling it 'a positive misstatement, not merely an omission'. So the substance is effectively confirmed even though this triad is formally OPEN. Proposed proof and fix: assert the 'Weapons & armour' header does not contain 'ARMED' after arming the companion on a phone; make the summary read `arming.source !== null && arming.source.kind !== 'companion'` and confirm playSheet.test.tsx:3964/4049/4240 stay green.

### 34. NEVER VERIFIED — an armed companion attack leaves no declaration anywhere on screen once the WhoSwitch returns to YOU

**OPEN — 2 of 3 agents started and were killed, the CONTEXT agent never started (but independently CONFIRMED as part of surviving finding 16)**

src/ui/player/Companion.tsx:322, raised by hunt:ui-rules at severity high. Vitals renders CompanionPanel only in the `who === 'companion'` branch (Vitals.tsx:117-127); on desktop there is no Disclosure summary at all, and DualityRoll's armSummary (:917-924) lists only Experiences, spent Hope and bonus dice — never the attack source. `grep -rn "sourceName" src/` -> attack.ts:503 (definition), attack.ts:579 (post-roll log detail), Play.tsx:3698 (phone fold only). This is a near-duplicate of surviving finding 16, whose three verifiers all reproduced the desktop half: after arming Ash and tapping YOU, 'buttons naming ARMED: []', the string ARMED appears nowhere in container.textContent, SHARP EYES (the companion's Experience) is on the roll bar with RAN WITH THE WOLVES gone and no label saying whose, and pressing ROLL returns 'IF IT HIT · 2d6+2'. Agents ae1a9b37a891b718d (CORRECTNESS) and a8bdb518413885b52 (REPRODUCTION) were killed at 40 and 24 transcript lines; ad8ff-style CONTEXT was never dispatched — this is the single agent slot in the whole run that was never even started.

### 35. NEVER DISPATCHED — the companion panel says NO EXPERIENCES YET while the armed roll offers two UNNAMED chips that each cost a Hope

**OPEN — raised by hunt:ui-rules, the Verify pipeline died before reaching it**

src/ui/player/Companion.tsx:370, severity medium. CompanionPanel filters unnamed Experiences out of its summary (`const named = companion.experiences.filter(e => e.name !== '')` at Companion.tsx:207, rendered at :369-371) but experiencesFor hands the roll `companion.experiences` UNFILTERED, and ExperienceChip substitutes the literal word 'Unnamed' (DualityRoll.tsx:1688). Every companion created through CreateForm starts in exactly this state — src/engine/companion.ts:66-70 builds COMPANION_START.experiences (=2) entries with `name: ''` and bonus 2. So on the DEFAULT sheet the panel reads 'NO EXPERIENCES YET' while the Experiences fold directly below reads '2' and offers two 'UNNAMED +2' chips, each of which spends a Hope: the panel contradicts the control that spends the player's Hope. Proposed proof: seed a beastbound whose companion is a plain newCompanion('Ash','') with damage 'd6+2', phone viewport, arm the attack, open the fold, and assert text() does not contain both 'NO EXPERIENCES YET' and 'UNNAMED' — it contains both today.

### 36. NEVER DISPATCHED — on the GM's party board an out-of-scene companion is signalled by colour plus a trailing phrase the line's own ellipsis truncates first

**OPEN — raised by hunt:ui-rules, and explicitly flagged as UNSETTLEABLE WITHOUT A REAL BROWSER**

src/ui/gm/PartyBoard.tsx:531-549, severity medium. CompanionLine is the only line in the row's identity column that forbids wrapping — `whiteSpace:'nowrap'; overflow:hidden; textOverflow:'ellipsis'` — while its siblings wrap (compare line 626, `<span className="row" style={{gap:5, flexWrap:'wrap'}}>` for Experiences). It packs six facts into that one line and puts the out-of-scene marker LAST (`{away && ' · OUT OF THE SCENE'}`), which is the first thing an ellipsis removes; what survives truncation is `color: away ? 'var(--stress)' : 'var(--muted)'` — colour alone carrying 'this creature is not on the battlefield', which the project's standing rule (Architecture.md / BACKLOG.md:1627, prefs.shapeCoding) says colour may never do. The agent itself said: 'CANNOT BE SETTLED IN JSDOM — the truncation point is a layout measurement. Prove it in Chrome at 393x852 with a long companion name and stress.marked === stress.max, reading el.scrollWidth > el.clientWidth on the CompanionLine span and checking whether the OUT OF THE SCENE substring is inside clientWidth. The structural half (marker last + nowrap + ellipsis + colour as the only survivor) is settled from source and holds at any width narrow enough to clip.'

### 37. NEVER DISPATCHED — the out-of-scene banner is conditionally mounted and is not a live region, so nothing announces the companion has left the scene

**OPEN — raised by hunt:ui-rules, the Verify pipeline died before reaching it**

src/ui/player/Companion.tsx:259-274, severity low. Marking the companion's last Stress changes the panel three ways at once — a red banner appears, the attack box stops being a `<button>` and becomes a `<div>`, and the reason text swaps — and none of it is announced. The banner is a plain `<div className="t-meta">` rendered behind `{away && …}`, so even adding role="status" later would not help. The branch's own NameRefusal, used ten lines below in the same dialog, states the rule being broken (src/ui/shared/NameRefusal.tsx:26-28: 'role="status", on a region mounted **empty** and filled when the name collides … because a live region has to exist before its contents change for the change to be spoken'). `grep -rn 'role="status"\|aria-live' src/ui/player/` shows Companion.tsx has neither. A screen-reader user hears '3 of 3' from the track and nothing about the companion dropping out or the command button ceasing to be a button — Tab now steps straight past it to SHEET. Proposed proof: seed stress {marked:2,max:3}, assert `container.querySelector('[role="status"], [aria-live]')` exists BEFORE the last pip is clicked; it does not.


## 6. The mutation-proof programme, the browser-only claims, and what is clean

### 38. Mutation proofs the run said must be run — and the ones it already ran

**OPEN (the orchestrator never ran the sequential mutation pass; the Critic and Synthesis phases that would have collected these never started)**

ALREADY EXECUTED IN SANDBOX COPIES, full suite green (i.e. these tests are proven vacuous): (1) Play.tsx:246-249 beastform branch -> setTrait('knowledge') or a no-op: 3266 passed / 1418 tests in tests/ui / 133 files — run three separate times by three agents. (2) Play.tsx:261 companion branch -> setTrait('knowledge'): 3265-3266 passed, run twice, and also survives at commit 19ddfa8. (3) attack.ts:410/412 NO_EXPERIENCES -> []: 3266 passed. (4) Companion.tsx:520 {upgrades.length} -> 8: companion.test.tsx 24/24. (5) PartyBoard.tsx:546 inverted to `damageType === undefined ? 'PHY' : 'MAG'`: partyBoard.test.tsx 14/14. (6) PartyBoard.tsx:540-546 delete ` · EVASION` and the evasion span: partyBoard.test.tsx 14/14 and the whole tests/gm folder 444/444 green. (7) DualityRoll.tsx:761 -> `character?.experiences ?? []`: green on both suite and typecheck. (8) Companion.tsx:463 inlined to Math.max(0, level-1): no allowance failure. ALSO EXECUTED: reverting PartyBoard.tsx:546 to `.toUpperCase()` DOES redden both new legacy tests (2 failed / 12 passed) — the crash fix is genuinely pinned. STILL TO RUN: (a) READABLE_CODEC_VERSIONS = [1,2,3] with BODY_AT[3] must redden tests/adversarial.test.ts's flipped-nibble test on bit 0, then [1,2,4] with BODY_AT[4] must stay GREEN — that green is what makes 4 the available number. (b) Change damageType 'mag'->'phy' in both tests/fixtures/schema/v5.* — the whole suite must stay green, proving the fixture is inert. (c) Add `damageType: n % 2 === 0 ? 'mag' : 'phy'` to tools/sampleCharacters.ts:672 — tests/transfer/matrix.test.ts:218 must redden, proving the 3240-sheet sweep was blind and normalizeHandles lacks its counterpart. (d) `damageLogEntry(companionSource({...newCompanion('a',''), damageType: 42 as never}, stats))` must throw TypeError today and must not after applying `=== 'mag' ? 'mag' : 'phy'` at attack.ts:373. (e) Set COMPANION_START.experiences to 3 — printSheet.test.ts must stay green while the printed sheet silently drops a blank line. (f) shared/campaigns.ts readPartyMember running migrateCharacterRecord — nothing should redden, proving nothing pins the 'never migrated' behaviour. (g) PartyBoard.tsx:527 `=== null` -> `== null` — nothing should redden, proving no test covers the absent-companion shape. (h) Delete the `listy` promotion loop at shared/parsers/rules.ts:764-767 and run `npm run build:srd -- --check` — must fail 'out of date'. (i) tests/engine/rest.test.ts:367 + `expect(companionIsAway(out.character.companion!)).toBe(true)` — RED today.

### 39. Claims the run itself flagged as unsettleable without a real browser

**N-A (explicitly out of reach of the test suite)**

Every hunt lane restated rule 3 ('jsdom measures nothing') and declined to claim pixels. The specific measurement-only claims left standing: (1) the party-board CompanionLine truncation point — whether ' · OUT OF THE SCENE' falls past the ellipsis at 393x852, leaving colour as the only surviving signal (hunt:ui-rules, finding 5, never dispatched to Verify); (2) the pixel notes added by the branch that nobody could check — Companion.tsx:73's '44 plus Vitals's own 6px phone gap - 50' and Play.tsx's 'A 46px row on a 393px phone' (fix:false-docblocks confirmed only that Vitals's phone gap is in fact 6); (3) the new WhoSwitch height and its 50px budget, the BeastformSeal's effect on the loadout, the new Beastform attack row's height, and the companion section's fit on the printed page (hunt:react-lifecycle and hunt:rules-fidelity both declined these); (4) SpellcastLine as an unmeasured pixel surface for Chrome — the only branch note about it, per one verifier. Also unverifiable for a different reason: COMPANION_START.stressSlots = 3 — folio 18's prose never states how many Stress slots a companion has (the count lives on the physical sheet the SRD does not reproduce), so hunt:rules-fidelity flagged it as unverifiable rather than as a finding.

### 40. What the run found genuinely CLEAN — where not to spend time

**CONFIRMED clean**

hunt:parser-dataset (the most thorough clean report): data/srd-1.0.json main vs branch is byte-identical for layers, domains, domainCards, classes, subclasses, beastforms, ancestries, communities, weapons, armors, loot, consumables, adversaries, environments, revision and generatedAt; rules goes 75->80 with the five new ids inserted contiguously at indices 5-9 and all 75 pre-existing sections unchanged in id/title/sourcePage/body/order; the only other change is schemaVersion 4->5. The non-writing rebuild `npm run build:srd -- --check` reports 'pages 68 pdf -> 135 book pages, rules 80, validation clean, data/srd-1.0.json matches the source.' The five new bodies were reconstructed word for word from page.runs: 896 words expected, 896 parsed, zero mismatches; no sub-9pt runs on either folio; `{start:'TIER 1', drop:true}` correctly stops folio 12's preamble before the stat cards (TIER 1 sits at y=511.7). No parser conflict with classes.ts (folios 8-26) or beastforms.ts (12-15). No duplicate ids or titles among the 80; tools/validate.ts reports no issues at all. hunt:persistence: every entry path runs the migration chain or provably needs none (IndexedDB, .dhchar/.dhbackup, pasteboard, daily backup, QR); v5 fixtures exist and are stamped 5; readableVersions() = [3,4,5]; the eight upgrade slugs are reproduced exactly by slugify so no saved sheet is silently unmarked; CAMPAIGN_SCHEMA_VERSION correctly did not move; the registry needed nothing (rules are outside BANDED_COLLECTIONS and upgrades travel as raw strings). hunt:engine-edges: beastformDamage is right for all 22 shipped forms; Proficiency 0 and negative clamp to 1 die; unparseable strings return null; degenerate dice ('0d6','d0','2d6-99') are gated by isRollableDamage at DamageRoll.tsx:388; clearCompanionStress cannot go negative; the long-rest ordering is deliberate and covered; rollModifier('spellcast') returns 0 not NaN when spellcastTrait is null. hunt:rules-fidelity: armor is correctly untouched by a Beastform; traits/Evasion are layered at read time; SpellcastPanel's seal is the right reading of 'you can still use other features'; folio 19 is correctly excluded; all deliberate silences are correctly silent. hunt:ui-rules: every new control declares an inline tap floor (Beastform row minHeight var(--tap) byte-identical to the weapon row; companion attack button minHeight var(--tap) plus width 100%; PHY/MAG chips minWidth 52 / minHeight 44); every new armed state carries a literal 'ARMED · ' prefix beside the colour; contrast computed from tokens.css (--sage on --panel 8.8:1 dark / 5.4:1 light; --stress on --panel 6.3:1 / 5.1:1); refusal-by-text obeyed in both places it could have failed; NameRefusal mounted unconditionally with refusal={null} and aria-describedby wired; the phone's folded-column budget is untouched. hunt:test-quality found genuinely sound: tests/ui/srdReference.test.ts's folio assertions (exact SRD sentences, pinned sourcePage, pinned slugs, negative boundaries in both directions), tests/engine/beastform.test.ts's four-quadrant edge-vs-level tests, tests/engine/rest.test.ts's ordering test whose fixture makes both orders differ, and the chips() helper that deliberately scopes to the disclosure subtree.


## 7. A finding about the run itself

### 41. Duplicate-detection failure inside the run's own design

**CONFIRMED (methodological finding about the run itself)**

The script flat-maps all lens findings into `candidates` with no de-duplication, so the same defect was verified up to three times under different wordings and — crucially — got DIFFERENT AGGREGATE VERDICTS. The short-rest defect was raised three times (candidates 2, 9, 23) and survived 9/9. The multiclass allowance was raised twice (3, 11) and survived 6/6. But the '12 EARNED on 8 boxes' defect was KILLED as candidate 4 (1 of 3) and SURVIVED as candidate 10 (2 of 3); and the QR damageType defect was KILLED as candidate 5 (1 of 3, framed at the codec) and SURVIVED as candidate 7 (2 of 3, framed at the party board). Since the script's `survived` gate is `survivors >= 2`, framing alone decided whether two real defects would have appeared in the final report. Anyone re-running this must merge candidates before the Verify phase, or read both verdicts together. Similarly, the phone Experiences fold was raised by BOTH react-lifecycle (candidate 15, verified 3/3 SURVIVED) and ui-rules (never dispatched), and the armed-companion-invisible defect by BOTH react-lifecycle (candidate 16, SURVIVED 3/3) and ui-rules (candidates 31/32, killed mid-flight) — so two of the three lost triads are substantively covered by findings that did survive.


## 8. What existed ONLY in the journal until this file

- The seven fix re-check verdicts in full: 5 of 7 fixes are INCOMPLETE (false-docblocks, drop-edge, print-idiom, stable-empty-array, weak-assertion, codec-decision — six, in fact; only party-board-crash was judged complete) and 5 of 7 carry regressionRisk:true. None of this is anywhere in the repo.

- That fix #4 (print idiom) is a REGRESSION: replacing the ballot glyphs with a 'Taken' label deleted the checkbox the SRD tells the player to mark, on a section whose own heading still prints '4 earned', while the correct primitive (.dhc-tick, sheet.css:468, already used twice in the same file) sat unused.

- That the branch's TWO HEADLINE RULES are pinned by nothing: setTrait('spellcast') for the companion and setTrait(form.attack.trait) for the Beastform can both be inverted with the full 3266-test suite green — proven by real mutation runs in sandbox copies, twice each, plus the discovery that the SPELLCAST assertion was born non-discriminating at 19ddfa8 and never discriminated at any point in the branch's history.

- That the audit's own fix #5 (NO_EXPERIENCES) is held by nothing — replacing both returns with [] leaves 3266 tests green — and that it also turned a file-local constant into a shared mutable singleton crossing module boundaries with no readonly marker.

- The short-rest/Clear-Stress defect (src/engine/rest.ts:215), confirmed by 9 of 9 verifiers and reproduced by at least five agents, together with the crucial design note that a faithful fix probably requires the away state to STOP being purely derived — which falsifies src/engine/companion.ts:133's justification and has transfer-codec implications the branch explicitly argued against. tests/engine/rest.test.ts:367 currently pins the wrong outcome; tests/engine/rest.test.ts:449 is titled for this invariant and never exercises it.

- The Beastform trait-swap defect (src/ui/player/Play.tsx:246), reproduced through the full UI path with the modifier deliberately differentiated to prove the ROLL is wrong and not just the label — plus the CONTEXT verifier's discovery that `declared` survives DROP, so the stale trait is reachable through the ordinary shape-change flow and through the branch's own new auto-drop at the last Hit Point.

- The partial-companion .dhchar -> permanent GM-board poisoning chain (src/transfer/fileIo.ts:279), reproduced end to end including the persistence half (flushGm, readCampaigns, the row coming back with companion:{} and warnings []), with the corrected blast radius (the board dies on open, not on launch; ScreenBoundary's Try again and Settings' GM-tools toggle are exits) and the specific fix list for checkShapes (name/description/evasion/damage/range/stress/experiences/upgrades — and the same gap for beastform and traitMarks).

- That the codec bump stopped short of its own precedent (18265c5): Architecture.md:461-462 literally says the loss list 'deve restare di tre voci esatte' while codec.ts:34 now says FOUR; Architecture.md §6.1 and HANDOFF.md:65-66 are stale; tools/sampleCharacters.ts was never extended so the 3240-sheet matrix is blind to damageType; tests/transfer/fixtures.ts::normalizeHandles lacks its counterpart; and both v5 fixtures' 'mag' is inert.

- The live crash the branch already fixed once and left standing a second time: src/ui/player/attack.ts:373 passes companion.damageType through raw and :578 calls .toUpperCase() on it, while sourceFromWeapon fifty lines earlier normalizes the MORE trustworthy value — a hand-edited .dhchar with damageType:42 imports silently and throws on the first companion damage roll.

- The full independent verification of the version-nibble argument (2 -> 3/0/6/10; 1 -> 0/3/5/9; Hamming(1,2)=2; 3 is distance 1 from both and gets demoted to the checksum-less format 1; 4 is distance 2 from both and its own flips give 5/6/0/12), plus the observation that the codec header's blanket claim overstates the guard because the test only flips payloads THIS build writes.

- The multiclass allowance defect and the '12 EARNED on 8 boxes' contested verdict, with the counter-case (BACKLOG.md:635-641's 'Do not re-open this', the 'READOUT AND NOT A GATE' docblocks, and the folio-18 page image showing eight prose bullets with no per-option counts) that a future session would otherwise have to rediscover from scratch.

- Six false statements added or missed by the audit commit c88bd21 itself — PartyBoard.tsx:514, CharacterSheet.tsx:417 ('two sections down' is one), CharacterSheet.tsx:419 (sheet.css never says 'never fills'; PrintDomainMark and CoinRow do fill), sheetModel.ts:202, levelUp.ts:494 ('the only place a character gains a new Experience', with its behavioural tail that a hand-added Experience gives the companion nothing), and Companion.tsx:190 — none of which any mutation proof can catch, because they are prose.

- The causality inversion in the log: the store logs 'Dropped out of Beastform' from inside update() BEFORE the caller logs the damage that caused it, and pushLog prepends, so the newest-first log lists the drop as older than its cause on the commonest route (Vitals.tsx:628-637), in Beastform.tsx:371-383 and in recall.ts:46-48. No test pins log order.

- That `characters` state is written in six places in state.ts and five bypass dropFormOnLastHitPoint, safe today only because none can raise hp.marked and nothing pins that.

- That srdReference.ts:648's stale 'seventy' is a FIFTH miss not on the sweep list already booked in docs/handoff/BACKLOG-PLAN-2026-08-23.md:79 — and that the docblock's substantive purity assertion is still true, so the fix is one word plus an optional census pin.

- The four ui-rules candidates that were never verified at all, including the two that need a real browser (the party-board ellipsis) and the two that are cheap DOM assertions (the NO EXPERIENCES YET vs UNNAMED contradiction on the DEFAULT companion sheet; the non-live-region out-of-scene banner).

- The full mutation-proof programme — nine still-unrun proofs with their exact expected colours — which was the whole point of the read-only design ('Mutation-proofs are run later, sequentially, by the orchestrator') and which the orchestrator never got to.

- The operational warning that scratchpad/mut/ was being reverted mid-run by another agent sharing the scratchpad, so any mutation result from that shared path is untrustworthy; the agent that noticed re-ran everything in a uniquely-named directory with the mutant verified present before and after.

- The methodological finding that the run's own design let framing decide survival: identical defects got opposite aggregate verdicts (candidate 4 KILLED vs candidate 10 SURVIVED; candidate 5 KILLED vs candidate 7 SURVIVED), because `candidates` is flat-mapped with no de-duplication and the gate is a bare 2-of-3.
