# Handoff — 26 agosto 2026

**Autorità su `main`.** `main` è `ab53c10`, pubblicato. **Ma non è lo stato del lavoro:** quattro
rami locali portano undici commit che non sono stati spinti né uniti, e la loro integrazione è
stata provata verde. Leggi la §3 prima di fare qualunque cosa.

Leggi, in quest'ordine: questo file, poi `DECISIONI-2026-08-25.md` (**dieci** decisioni del
proprietario, non nove — la §10 è stata aggiunta ieri sera), poi `STEP5-RIPROGETTATO-2026-08-25.md`
se tocchi la reference, e `HANDOFF-2026-08-25.md` per l'ondata precedente.

---

## 1. Cosa è uscito il 25 agosto, in cinque merge

| PR | cosa |
|---|---|
| #11 | la lane 0: otto siti di prosa stantia in sei file, e il registro delle nove decisioni |
| #12 | il service worker: il docblock di `pruneAssets` e la prova contro il prune |
| #13 | l'atterraggio della ricerca a granularità di part, e la marcatura nel corpo |
| #14 | `PROGETTO-GM §6 step 3`: l'aritmetica del motore che raggiunge il GM |
| #15 | la voce `## Unreleased` del CHANGELOG |

Tutti col deploy verificato. `main` è passato da 143 file / 3697 test a **147 / 3789**.

---

## 2. Le decisioni prese, e quelle che invalidano un passo scritto

Il registro sta in `DECISIONI-2026-08-25.md`. Le §1-§9 sono del 25 agosto, la §10 è nata
guardando il codice: la lane del catalogo ha messo QUESTIONS sopra `SOME`, si è accorta che
**nessuna decisione lo autorizzava**, e l'ha segnalato invece di lasciarlo passare.

**Quattro decisioni nuove sono state prese il 26 e NON sono ancora nel registro.** Vanno
scritte lì, o si perdono:

1. **`ShowDoor.body` resta, ed è la formulazione canonica** dell'app per ciò che ogni strumento
   *non* è; Settings la parafrasa. → **Serve un commento che dica perché non è un orfano**, o
   qualcuno lo cancella per pulizia. Non fatto.
2. **Il `--control` su `hybrid` segue `coarse`**, cioè sale a 44px. **Non implementato.**
3. **Un test pinna il margine da 0.3px** dello stato vuoto di SHOW. **Non implementato.**
4. **Il fixture del rig porta un gruppo di Minion**, così quella carta è misurabile. **Fatto** —
   vedi §5.

---

## 3. I quattro rami non spinti, e la loro integrazione

**Nessuna PR è aperta. Niente di tutto questo è pubblicato.**

| ramo | commit | cosa |
|---|---|---|
| `wave5-carta` | 4 | la piega sulla `CombatantCard`, lo stepper dei Minion nella banda, e i numeri misurati |
| `wave5-catalogo` | 4 | il catalogo delle domande, i chip momento, le porte a una riga, la §10 |
| `wave5-pin` | 2 | il test di `LINK → Rule` e la frase di `BlockView` che ne conseguiva |
| `wave5-step5-replan` | 1 | la ri-progettazione dello step 5 (solo documento) |

**Zero sovrapposizioni di file fra i quattro.** L'integrazione è stata provata il 26 agosto in un
worktree usa-e-getta: i quattro si uniscono senza conflitti, `tsc --noEmit` pulito,
**148 file / 3832 test verdi**, `vite build` verde. Il conto è esattamente additivo — 3789 + 2 +
26 + 15 — quindi nessun test è stato perso o contato due volte nell'incrocio.

**La procedura decisa dal proprietario** è: quattro PR, unite dalla più piccola alla più grande,
con l'**ultima riallineata su `main` prima del merge** — perché il 25 agosto la CI della PR #14
aveva girato su un base ormai vecchio di due merge, e quel verde non provava l'incrocio. Il merge
è ciò che pubblica: `deploy.yml` parte su `push: branches: [main]`.

---

## 4. Due lane sono morte a metà lavoro, ed è una lezione operativa

- La lane **catalogo** si è **bloccata**: watchdog dopo dieci minuti di silenzio.
- La lane **carta** è stata uccisa da un errore API — **il computer è andato in sospensione**
  mentre rispondeva.

Entrambe avevano lavoro **non committato e mai passato dal cancello**. Prima di subentrare è stato
verificato che fossero davvero morte e non solo lente — stato dell'agente **più** mtime dei file,
17 minuti di immobilità. Una lane in stallo non è una lane morta, e subentrare a una viva significa
due scrittori in un worktree.

**Il difetto che è emerso subentrando è quello che questo repo paga più spesso: il docblock era
avanti al codice.** Diceva di aver spostato in Settings la promessa del tabellone — non era vero —
e che Settings disegna `choice.body` — non lo fa, ha parole sue. La prima è stata resa vera, la
seconda corretta.

E togliere le frasi dalle porte ha mandato rossi **quattro test**, quelli che asserivano «ogni
porta dice cosa lo strumento non è». La lane non era mai arrivata a vederli. Sono stati riportati
dove la promessa è andata — `tests/ui/settingsHints.test.tsx`, lette attraverso `aria-describedby`,
così una frase presente ma irraggiungibile dal controllo fallisce come una mancante.

---

## 5. Cosa è stato misurato in Chrome, e cosa resta a un tavolo

Tutte le misure a **393×852, insets 47/34, puntatore coarse**, sul deploy o sul ramo servito.

### 5.1 La carta del combattente

| | pannello | una carta |
|---|---|---|
| prima dello step 3 | 498 | **504.00** |
| dopo lo step 3 | 498 | **558.00** |
| **dopo la piega** | 498 | **471.00** |
| **gruppo di Minion, dopo lo spostamento** | 498 | **484.00** |

Il difetto era **pre-esistente**: sforava già di 6px, e il campo danno l'ha portato a 60. La
risposta al cancello di `PROGETTO-GM §7` voce 3 era più dura della domanda — non ci stava
**una** carta, non due.

**Lo stesso modello a nove termini ha predetto 558.00, 534.50, 471.00 e 484.00, e Chrome li ha
confermati tutti e quattro.** La derivazione resta nei docblock apposta: è ciò che dice quale
termine guardare quando uno di loro si muove.

I contatori costano **188px**, non 90: `repeat(auto-fit, minmax(170px, 1fr))` con gap 8 vuole 348
in una colonna di 341, quindi le due celle si impilano. Non è recuperabile — 170 è il min-content
di 165.81 misurato in `Vitals.tsx`.

### 5.2 Lo stato vuoto di SHOW

La finestra di lettura è **308.3px** e paga 14 di padding: **294.3** alla colonna. Tre porte con le
loro descrizioni ne prendevano **273.7**, lasciandone **20.6** — e nulla che questa app disegni è
alto 20.6px. **I chip non erano il problema: erano le porte a riempire la finestra da sole.**

Tolte le descrizioni, ogni termine è tornato esatto: griglia **96.00**, una porta **56.00**, tre
porte **188.00**, colonna **294.00**. THE MERCHANT finisce a **688**, che è il bordo della
finestra: la terza porta è intera, per nulla. E **lo stato vuoto non scorre più** — prima sforava
di 138px.

I chip sono **115.66px** e tutte e sei le etichette stanno su **una riga**. L'etichetta più larga
è **`BEFORE THE ROLL` a 114.00**, non `BETWEEN SCENES` a 106.41 — due frasi del docblock dicevano
il contrario ed erano sbagliate.

### 5.3 L'atterraggio della ricerca

Cercando `soft move hard move` e aprendo *Making GM Moves*: lo scroller scende di **1491px** e il
sottotitolo del blocco d'atterraggio si ferma a `top: 378.2` contro il bordo a `379.7`.

La marcatura del **corpo** è a sfondo **trasparente**, peso 700, colore `rgb(232,230,225)` contro
il `rgb(200,197,190)` del testo intorno — due segnali, non uno. Quella dell'**anteprima** ha un
piatto `var(--line)`. Otto marcature in una finestra da 308px.

### 5.4 Il pavimento del tocco

| profilo | `--control` | bottoni sotto i 44 |
|---|---|---|
| **hybrid** (portatile touch) | **34px** | 11, fra cui *Play / Cards / Build / GM* |
| **fine** (mouse) | 34px | gli stessi 11 |
| **coarse** (telefono) | **44px** | solo i ✕, e per **larghezza** |

**Un portatile touch riceve i controlli da mouse.** È la misura su cui la voce di BACKLOG era
ferma, ed è ora presa. La decisione (hybrid segue coarse) **non è implementata**.

### 5.5 Cosa un rig non può dire

Due cose restano a un tavolo, e sono scritte così nei docblock:

- se il `<mark>` a sfondo azzerato sia trovabile a colpo d'occhio in una stanza buia;
- se **102px di mono da 10px a metà spaziatura** si leggano a distanza di braccio — che è ciò per
  cui la spaziatura dei chip è stata dimezzata.

---

## 6. Il rig di misura

Vive fuori dal repo, in
`~/.claude/projects/-Users-simonealbanese-Documents-Daggerheart-Companion/audit-harness/`.
Accetta **`AUDIT_ORIGIN`**, quindi misura tanto un `vite preview` locale quanto il deploy vero.

**Il suo fixture ora porta un gruppo di quattro Giant Rat** (`comb-minions`, `minionsRemaining: 4`)
nella riga `row-encounter`. È ciò che ha reso misurabile la carta dei Minion: **tre tentativi di
portarcene uno pilotando il builder non hanno mai superato il passaggio del roster.** La copia
precedente è in `fixtures.json.bak-pre-minion`.

---

## 7. Cosa resta aperto

**Deciso e non fatto:**

1. Il commento su `ShowDoor.body` che dice perché non è un orfano.
2. `--control` su `hybrid` a 44px. Tocca un token globale: undici bottoni crescono su portatile
   touch, non solo sullo schermo GM. Merita un ramo suo.
3. Il test che pinna i 0.3px dello stato vuoto di SHOW.

**Aperto e non deciso:**

4. **La lista delle sezioni di mezza scena** — 35 su 69, scritta in `tests/gm/ask.test.ts`, e il
   proprietario la deve correggere. Tre scelte da contestare: `downtime` è fuori,
   `engaging-your-players` è dentro per la tabella d'obiettivi a 1d12, `pitfalls-to-avoid` e
   `session-rewards` sono fuori anche se delle voci ci puntano.
5. **Le voci 13-48 del catalogo**: 27 sezioni di mezza scena scoperte, **pinnate come `toEqual`
   esatto**. È una lista di cose da fare che va rossa da entrambi i lati.
6. **I ✕ «Remove X from the scene» sono 34px di larghezza** contro un pavimento di 44, su
   un'azione irreversibile. Pre-esistente, misurato identico prima e dopo.
7. **Lo step 5 è ri-progettato ma non eseguito.** `STEP5-RIPROGETTATO-2026-08-25.md`.
8. **`PROGETTO-GM §7` voce 6** — se un terzo gruppo QUESTIONS spinga il primo risultato sotto la
   piega — è ora misurabile: il catalogo esiste. Va fatto sui 308px.
9. **La riga `ENCOUNTER` legacy che diventa scena** (§2.1 del 25 agosto): decisa, mai eseguita, e
   il proprietario l'ha esplicitamente **non** scelta nell'ultimo giro di priorità.

---

## 8. Cosa non toccare, e sono decisioni

Le voci di `WAVE3 §8` e di `HANDOFF-2026-08-25 §4` restano in vigore. Ripetute quelle che il
lavoro imminente sfiora:

- **`REFERENCE_TOPICS` non si ritira** — è l'unica porta ai due chip compositi, e i pin seminati
  sono **sei**, non otto (§1 del registro).
- **Il docblock di `Countdowns.tsx` non si revoca**: l'hint si corregge con le parole dell'app.
- **`combatantHit` prende il flag del Massive come opzione obbligatoria**, mai un `false` scritto
  dentro.
- **La marcatura accende solo il blocco d'atterraggio**, e non si spezza il `<p>` per riga.
- **`encounter` non torna in `SESSION_ITEM_KINDS`**; `DB_NAME` non si muove; `gmPartySize` non si
  deriva da `party.length`; la schermata Play scorre.
- **Le sezioni rilasciate di `CHANGELOG.md` non si riscrivono**, e la voce `## Unreleased` la
  scrive **una sola** lane, quella che spedisce.
- **Mai `git add -A`.** `node_modules`, `.tools` e `Manuali` sono directory vere nella radice e
  symlink dentro i worktree.
- **Le prove per mutazione in una copia isolata**, col mutante verificato presente prima e dopo.
- **`. ./env.sh` prima di ogni comando npm.** Il node di sistema è 26 e nasconde `localStorage` a
  jsdom: un verde sotto quello è più debole di quello di CI.
- **`build:srd -- --check` è il cancello vero** per qualunque cosa tocchi dataset o parser.

---

## 9. Per riprendere

Il prossimo gesto è **spedire i quattro rami**: l'integrazione è provata, la procedura è decisa, e
undici commit verdi che esistono solo su questa macchina sono la cosa più fragile in giro.

Poi, nell'ordine deciso: il commento su `ShowDoor.body`, il test dei 0.3px, e il `--control` su
`hybrid` in un ramo suo perché tocca un token globale.

Le due domande vive per il proprietario, in ordine di quanto bloccano:

1. **La lista delle sezioni di mezza scena** — 35 voci da correggere, e il rot test le aspetta.
2. **Se le due cose che un rig non può misurare** — il `<mark>` in una stanza buia, i chip a metà
   spaziatura — vadano guardate a un tavolo prima di spedire, o dopo.
