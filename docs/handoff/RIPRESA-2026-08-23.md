# Messaggio di ripresa — 23 agosto 2026

Da incollare in una sessione nuova.

---

Riprendiamo il Daggerheart Companion.

Leggi `HANDOFF-2026-08-23.md` nella radice, e dentro quel file vai a **§1 e §2
prima di ogni altra sezione**: §1 dice che non c'è niente in volo, §2 è lo stato
misurato e dice che `0.5.0` è online **verificata sul sito**, non solo sul
workflow. Poi **§6**, che è l'unica cosa che riguarda il futuro.

`HANDOFF-2026-08-19.md` resta in albero e porta un cartello: **non è stato
riscritto di proposito**, ed è ancora l'autorità su §5 (le quattro decisioni del
19), §6, §7 e §8. `HANDOFF-2026-08-18.md` e `HANDOFF.md` sono più vecchi ancora
— di `HANDOFF.md` **è mantenuto solo il cartello**, e tre frasi sotto sono false
apposta.

Le sei cose da sapere subito:

1. **NON C'È NIENTE IN VOLO.** Nessun workflow, nessun worktree, nessun ramo da
   unire. `main` = `eb4c60e`, albero pulito, `main == origin`. Suite a **3140
   test su 132 file**, verdi, `tsc --noEmit` pulito, build verde, su Node
   **v24.19.0** via `. ./env.sh`.

2. **`0.5.0` È PUBBLICATA E LA PUBBLICAZIONE È STATA CONTROLLATA.** Non solo CI
   e deploy verdi: il `sw.js` servito porta lo stampo del commit corrente e il
   bundle che il browser scarica contiene `0.5.0` e tutte le stringhe nuove. Un
   deploy verde dice che il workflow è finito, non che il sito serve quei byte —
   §2 del handoff spiega come è stato verificato, rifallo allo stesso modo.

3. **L'AUDIT DELLE 80 VOCI DI BACKLOG È ANCORA FATTO E ANCORA NON APPLICATO.**
   Sta in `docs/handoff/BACKLOG-AUDIT-2026-08-19.md`. **30 rette** da applicare,
   **10 marcate `[NON verificato]`** che **non si spuntano** senza rifare quel
   giro, e `:1653` che **va spezzata, non spuntata** — `AppBoundary.tsx` porta
   ancora il difetto identico. `BACKLOG.md` è **intatto** e quindi mente. La
   sessione del 23 agosto **non lo ha toccato**: il proprietario aveva chiesto
   altre cinque cose prima. È ancora il lavoro col miglior rapporto
   valore/fatica.

4. **IL LAYOUT B È ANCORA A ZERO RIGHE.** La home del DM a schede (THE NIGHT /
   REFERENCE), decisa il 19 agosto e mai costruita. È il pezzo più grande e più
   chiaramente approvato. Prima di scrivere codice conviene un giro di
   progettazione contro `GmBar` e `Reference.tsx`.

5. **NON FIDARTI DI UN RAPPORTO, RIESEGUI — E NON FIDARTI DI jsdom.** Le due
   lezioni che il 23 agosto ha ripagato: un test che non hai visto fallire non è
   un test (il revisore dei modificatori è stato mutilato tre volte apposta), e
   **jsdom non misura niente**, quindi una decisione sui pixel si misura in
   Chrome o non è misurata. §7 del handoff ha le altre quattro, compresa quella
   in cui una mia affermazione al proprietario è stata smentita dal righello e
   corretta.

6. **I test si lanciano SOLO dalla radice del worktree**:
   `. ./env.sh && npx vitest run` e `npm run typecheck`. Il Node di sistema è 26
   e nasconde `localStorage` a jsdom: una suite verde sotto quello è **più
   debole** di quella di CI.

Cosa fare, nell'ordine di §6:

- **applicare l'audit** del punto 3;
- **il Layout B**;
- **③**, ma solo dopo aver rifatto la stima: la sua premessa era falsa e il
  difetto era chiuso da `4bdf694`;
- e una voce nuova, piccola: **il tiro dei danni non legge le riserve di dadi**,
  benché Slayer e Rally dicano entrambi «damage roll».

Cosa non toccare, e sono decisioni, non sviste (§8 del handoff del 23, che
richiama §8 del 19):

- **il cancello H-9 resta chiuso**: inset orizzontali, rail dei Cards, overlay,
  `--control`, voci 7, 8 e 19;
- le sezioni rilasciate di `CHANGELOG.md`;
- di `HANDOFF.md` si mantiene **solo il cartello**;
- la regola di `SESSION_ITEM_KINDS` non è esaurita;
- **mai `git add -A`.**

E cinque fatti sul codice nuovo che costano un pomeriggio se li riscopri da solo
— §8 del handoff li ha per esteso:

- **`armorScore` è l'unico numero derivato che si riscrive addosso**, e sommarci
  un bonus sul ramo sbagliato gonfia una scheda salvata a ogni salvataggio;
- **Simiah *Nimble* è `features[1]`**, quindi con ancestry mista dipende dallo
  slot;
- **`evasionOverride` non è scritto da niente in `src/`** e sostituisce tutto;
- **la riserva di dadi chiede a chi**, perché i Prayer Dice si spendono anche su
  un alleato e l'app non deve scrivere la scheda sbagliata;
- **il vassoio generico `+ DIE` resta**, per il Rally Die che ti dà il bardo.
